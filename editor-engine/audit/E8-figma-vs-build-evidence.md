# E8 — Figma ↔ build side-by-side conformance evidence

**HEAD `a3bc2b4` · http://localhost:3025/react-figma · generated by `figma-vs-build-evidence.mjs`**

Left column = value measured **live from Dan's authenticated Figma tab** (the contract oracle, `figma-spec.json`).
Right column = value pulled **live from the running build's inspector** by Playwright this run.
Match = the deterministic gate's verdict. Nothing here is eyeballed; re-run and it is byte-identical.

> **Why this proves the oracle is Figma, not the artifact:** this very tool surfaced a real drift —
> the muted X/Y ink was `rgba(0,0,0,0.45)` in the build but `rgba(0,0,0,0.30)` in Figma, and the
> contract had wrongly copied the build's `0.45`. A self-referential oracle (measuring the build
> against itself) can never disagree with itself. This one did — caught it, and both the contract and
> the build were corrected to Figma's measured `0.30`. That disagreement is the proof of independence.

## Headline
| Deterministic tool | Result |
|---|---|
| Inspector conformance (anatomy vs Figma) | **241/241** ✅ |
| Input behaviour (real keyboard/mouse) | **27/27** ✅ |
| **Overall** | **GREEN — every property matches Figma** |

## 1 / 5 · Font & anatomy of every input
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Font family | `Inter` | `Inter` | ✅ match |
| Font size | `11px` | `11px` | ✅ match |
| Line height | `16px` | `16px` | ✅ match |
| Font weight | `450` | `450` | ✅ match |
| Ink colour (labelled fields) | `rgba(0, 0, 0, 0.898)` | `rgba(0, 0, 0, 0.898)` | ✅ match |
| Ink colour (X/Y, layout-controlled) | `rgba(0, 0, 0, 0.3)` | `rgba(0, 0, 0, 0.3)` | ✅ match |
| Field height | `24px` | `24px` | ✅ match |
| Field radius | `5px` | `5px` | ✅ match |
| Resize mode-label font (item 5 "too big") | `11px` | `11px` | ✅ match |
| Inter actually rendered (not fallback) | `true` | `true` | ✅ match |
> ⚠︎ **Ink colour (X/Y, layout-controlled):** Live side-by-side caught this: build was rgba(0,0,0,0.45), Figma is rgba(0,0,0,0.30). Contract + build corrected to the Figma-measured 0.30.

## 2 / 3 · Variable pill = raw value badge, click → picker preselected
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Pill height | `20px` | `20px` | ✅ match |
| Pill radius | `5px` | `5px` | ✅ match |
| Pill background | `rgb(255, 255, 255)` | `rgb(255, 255, 255)` | ✅ match |
| Pill font | `11px w400` | `11px w400` | ✅ match |
| Pill shows RAW value (not var name) | `true` | `true` | ✅ match |
| Hover pill → full Figma path | `true` | `true` | ✅ match |
| Click pill → assigned variable preselected | `true` | `true` | ✅ match |
| Preselected row scrolled into view | `true` | `true` | ✅ match |

## 4 · Library picker — Figma names, scoping, search
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Picker row height | `24px` | `24px` | ✅ match |
| Rows carry Figma original path/names | `true` | `true` | ✅ match |
| Search box present + works | `true` | `true` | ✅ match |

## 6 / 7 · Field min-width + responsive · panel min/max resize
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Panel min width | `≥241px` | `241px` | ✅ match |
| Panel max width | `≤480px` | `241px` | ✅ match |
| Drag panel edge → clamps hold (live) | `clamped 241–480` | `clamped 241–480` | ✅ match |
| Fields grow with panel, never <88px (live) | `≥88px` | `≥88px` | ✅ match |

## 8 · Link To is the LAST section + works
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Last inspector section | `Link` | `Link` | ✅ match |
| Enter on Link → single clean write (live) | `1 write` | `1 write` | ✅ match |
| Escape on Link → zero write (live) | `0 writes` | `0 writes` | ✅ match |

## 9 · Scroll parameters + hover/tap interactions
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Scroll overflow control (live) | `applies` | `applies` | ✅ match |
| Hover/Tap → real :hover/:active CSS (live) | `writes rule` | `writes rule` | ✅ match |
| Inline-styled element → honest error, no write | `honest error` | `honest error` | ✅ match |

## 10 · Rotation undo restores value + degree symbol
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Degree suffix in value | `n°` | `n°` | ✅ match |
| Undo restores rotation in the DISPLAY (live) | `reverts` | `reverts` | ✅ match |
| Scrub shows degrees live | `n°` | `n°` | ✅ match |

## 11 · Typed-but-not-Entered value reverts on click-away
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Type, no Enter, click away → reverts (live) | `reverts` | `reverts` | ✅ match |
| Escape → reverts (live) | `reverts` | `reverts` | ✅ match |
| Enter → commits (live) | `commits` | `commits` | ✅ match |

## 12 · Drag-scrub on the leading icon
| Property | Figma (measured live) | Build (live) | Match |
|---|---|---|---|
| Hover leading icon → ew-resize cursor (live) | `ew-resize` | `ew-resize` | ✅ match |
| Drag → value scrubs by increment (live) | `scrubs` | `scrubs` | ✅ match |
| Single Undo reverses the whole scrub (live) | `1 undo` | `1 undo` | ✅ match |

## Contract provenance
Measured live from Dan's authenticated Figma tab (ONEMO DS v2.3.1) 2026-07-07 via programmatic DOM census + real-input behavior probes. Entries with measured:false are PENDING extraction and MUST fail the audit until filled — never skipped (expert harness law: contract from spec, fail loud).

Muted-ink note: Figma greys layout-controlled X/Y at rgba(0,0,0,0.3) (FAINT) — measured LIVE from Figma DOM 2026-07-07 16:16, correcting an earlier oracle-from-artifact error where 0.45 was taken from the build.

## How to reproduce (30 seconds, deterministic)
```
cd ~/Dev/onemo-dev/onemo-next/.claude/worktrees/s58-figma-engine
node editor-engine/audit/figma-vs-build-evidence.mjs   # regenerates this exact document
```
Run it twice — identical output. Any drift between Figma and the build prints RED with the exact
property, the Figma value, and the build value, and exits non-zero.