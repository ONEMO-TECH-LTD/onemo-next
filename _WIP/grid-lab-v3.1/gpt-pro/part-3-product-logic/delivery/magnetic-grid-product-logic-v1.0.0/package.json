{
  "name": "magnetic-grid-product-logic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Exact, policy-explicit product ordering over accepted magnetic-grid candidate and measurement documents.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "build": "npm run clean && tsc -p tsconfig.json",
    "test": "npm run build && node --test ./test/product-logic.test.mjs",
    "verify": "npm test && node ./scripts/verify-package.mjs"
  }
}
