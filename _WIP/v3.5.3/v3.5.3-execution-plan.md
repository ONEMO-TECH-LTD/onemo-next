# v3.5.3 execution plan — final state

Status: execution authority, 2026-08-23. Authority is the v3.5.3 master only (`v3.5.3-master-contract.md`); this file sequences it and is self-sufficient for the builder together with that master. Prior rounds (the recovery to `2c043257`, the exact-scaling attempt, T1/T2 packets) are history under `../v3.5.2/`.

## Operating contract

- One builder, one fresh clean worktree at product `2c043257`. Each step begins with one semantic rollback commit; every accepted QA/Meta correction lands as its own forward commit. Compile and run focused tests after each step; the authoritative live-tab gate is F1, except when a step cannot be validated without runtime interaction.
- No amend, rebase, reset, push or merge without Dan.
- QA and Meta review each step on the same branch and deliver paste-ready fixes (Dan's rule); no finding-only handoffs.
- Master §9 stop rules are a hard stop.

## Steps

| # | Step | Master | Gate |
|---|---|---|---|
| W1 | Wrap on the ruler: one signed material clearance per lattice node over outer+holes, one conversion, seated iff ≥ 0, belt derived from seated, belt required flap, co-nearest witnesses, one private `{seated, belt, wrapMeasurement}` result; one verdict for Free/manual/Auto/band (`seat.ts` gains `pointInMaterial` and the all-ring nearest helper) | §5.2 `measureWrap`, §5.3 `evaluateWrap`, §5.1 types | fixture 2 (signed ruler; square 24/26, Weight squircle 72 residue reads 0, −0.49/−0.51, outside/hole anchor negative, diamond 34/36, 0.5 reads 1, holed cutout, Auto minimum, Free = rung, Coverage/MagnetPlan invariance) |
| W2 | Delete the exact layer, circle path, dead float Wrap helpers, `parityHolds` (Centre's integer seat prescreen stays); rename `contact-root.ts` → `wrap-measurement.ts`; guard asserts absence | §3 table | fixture 4; all remaining tests green; Centre fixture 1 unchanged |
| S1 | Even sizes, four bands, slider snaps to 2 mm; delete sub-mm constants, `seatMarginMM`, B5 | §5.1 values | tab shows four bands, even sizes only |
| S2 | `computeGrid(contour, requestedSizeMM, cfg)` returns all four render-complete placements; Compute measures frozen parity and report-only centre miss; Logic returns concessions; worker passes its already-owned requested size | §5.2, §5.3, §5.4 | `candidates` length 4 with phase+lattice; 0.001 parity identity mutation; four concession cases; offset bbox 82 with requested size 72 publishes 72 for all candidates |
| S3–S5 | Atomic vertical cut-over (one commit, one gate): ladder records; `reduceBandLadders(candidates, policy)` + synthetic reducer mutations; `solveBands`/stored-only `fitSizeInBand`/`autoFlapInBand` replacing `bandWalk`/`bandSnapPoints`/`snapRange`/`below`, `BandSnapPoint`/`solveCache` deleted; worker storage/calls cut to `BandSolveResult` (its second size-prefetch loop removed — `solveBands` is the sole production loop); LawPanel cut to final rungs/co-lawful layout selection and honesty copy; no adapter, projection, dormant helper or second loop | §5.1, §5.3, §5.4, §5.5 | fixture 3 (synthetic + square/squircle ladders), fixture 6 (zero compute calls on chip select; offset contour present in every rung), fixture 4 one-loop guard |
| F1 | Full-system gate on the live tab | §7 | all six fixtures; fixture 5 timing |

After F1: compare completed rungs against frozen Centre; a measured material Centre-caused change is a stop and a separate amendment — no repair is authorised here. Otherwise delivery closes at F1.

## Allowed files

`src/lib/magnetic-grid/{spec,compute,logic,engine}.ts`, `src/lib/magnetic-grid/compute/{seat,centre-evidence,contact-root→wrap-measurement,identity}.ts`, and named fixtures under `src/lib/magnetic-grid/__tests__/*`; `law.worker.ts` is allowed in W1 for the Wrap-field rename, S1 for the even-size request deletion, S2 for the requested-size pass-through, and S3-S5 for final storage/cut-over; `LawPanel.tsx` is allowed in W1 for the Wrap-field/copy rename, S1 for even-size controls, and S3-S5 for final UI cut-over; `magnetic-grid-bridge.ts` is S3-S5-only. A measured F1 finding may touch only its directly affected allowed runtime owner plus one biting fixture, then must re-enter QA and Meta; the recorded F1 disposition is `logic.ts` + `scaling.test.ts` at `64d73365`. Nothing else; no new runtime module.

## Closure

Builder self-audit → QA → Meta on one clean immutable HEAD; Dan reviews the tab. Engineering closes on the Meta verdict.
