# KAI-10216 builder ledger

## Authority

- Linear: KAI-10216 and Brief KAI-10230.
- Contract: `../../QA-space/contracts/v1-polish-optimisation-production-contract.md`.
- Canonical contract SHA-256: `367e2d270cac4e0027fe2271f26b3f0c5511654f22a450138fb952f3a3ed6c91` (177 lines).
- Baseline: `050d557e2ddbe99520c008e2090c202c554f03f8`.

## Required outcome

- Characterize accepted V1 behavior and later-owned defects.
- Keep the seven Cutout files in place.
- Remove Cutout debug edges and dead `lastFileRef`.
- Put reusable primitive and outline-producer ownership behind product-owned paths without duplicate implementations.
- Classify every current Cutout file and dependency into headless, browser/React, studio-shell, or route-diagnostic destination.
- Preserve the existing UI-facing flow API unchanged; do not create a new shell/interface project.
- Prove static gates and the real Upload to Save journey on current code.

## Non-goals

- No detector, lifecycle, output, or OpenCV repairs owned by later tasks.
- No Cutout relocation, final package, UI work, v2 transplant, or `onemo-effects-engine` edit.

## Evidence log

- Path, branch, HEAD, contract hash, contract length, and tracked-tree state verified at task start.
- Full-read all seven Cutout files plus the current v5.3.1 primitive and outline-producer owners.
- `page.tsx` is the existing React studio shell and gesture renderer; KAI-10216 removes only its `PerfHUD` debug mount and preserves all UI behavior.
- `flow.ts` is browser/React orchestration; `finish.ts` and `v531seg.ts` combine pure engine calls with Canvas/Image/worker adapters.
- `EditorOverlay.tsx` is React gesture/presentation; `history.ts` is headless state; `ui-config.ts` is shell configuration.
- Cutout consumes only `runCutout` from dev primitives and outline-control functions/types from dev producers. Those are the only owner seams KAI-10216 needs to cut over.
- The executable boundary probe rejected `src/lib/effect/mask.ts` as headless: it owns ImageData, canvas decode, and WebGL device probing. Its direct engine destination is the browser adapter layer.
- The same probe rejected `src/lib/mask-tools/index.ts` as headless because its mask rasterization and paint swath own Canvas; only its data types remain headless.
- `onemo-effects-engine` confirms `src/lib/` is headless and `src/app/` is the studio shell; its current `image-pipeline` placeholder names the image-to-contour seam but contains no implementation.
- Post-compaction live recheck preserved the exact branch, baseline, tracked-tree state, active goal, and per-task gate. No scope change.
- Product cutover implemented as exact moves plus compatibility re-exports; no duplicate implementation or seven-file relocation.
- Final relevant suite reports 38 passing files, 431 passing tests, and six expected-failing later-owner reproductions; typecheck, scoped lint, diff hygiene, and the production build pass.
- Live Playwright fallback proof completed on the current worktree: Upload → u2netp Detect → Detail edit → Preview → Save; transparent RGBA output recorded with dimensions and hash in the personal checkpoint.
- QA corrected the scope before handoff: UI is not a V1 problem. The added interface/type and return-shape change were removed; the existing UI-facing API and behavior remain unchanged.
- Final authority is contract SHA `367e2d270cac4e0027fe2271f26b3f0c5511654f22a450138fb952f3a3ed6c91`; live Linear KAI-10216 pins the same hash and remains `Building`.
- Concrete UI coupling: the existing route consumes `state`, `actions`, `view`, plus top-level `measureNode`. KAI-10216 preserves that exact surface; it does not redesign it.
- Bounded de-slop disposition: deleted live PerfHUD/perfGesture edges and dead `lastFileRef`; collapsed the two reusable dev owners behind single product-owned implementations and compatibility re-exports; retained route diagnostics and the stale architecture file for their contract-owned later gates; found no justified extra deletion.
- Authorized rollback snapshot committed and pushed at `26d37579c0a119c2482212b6b84c482918937d75`; local and remote branch heads match exactly. Builder stopped before KAI-10217.
- Final exact visual artefacts supersede the earlier proof record: screenshot SHA `6c05723fb3e07dacff21f268f5d7e0b2e9d8071befcfc63a9dbd21e9aab9dfee`; saved 1330x621 RGBA PNG SHA `d7a28a6976223e9f82f73f16d3a77f3bbec770f727805dcb85d4041d9c0daf28`; port 4001 PID 34639 serves this exact worktree.
- Rollback snapshot committed and pushed at `26d37579c0a119c2482212b6b84c482918937d75`; local HEAD and exact remote branch resolve to the same SHA.
- QA returned REVISE on the proof package only. Product code, owner moves, debug/dead-edge deletion, UI/API preservation, build, and primary journey cleared.
- Replaced the broad behavior source-string block with exact executed-oracle citations, direct Paint/history behavior tests, a real `prepareEffect` fallback-callback test, and the committed existing-Playwright real-route oracle approved by QA. No product seam or framework was added.
- The route oracle now covers primary u2netp, forced lazy Silueta, Frame opposite-edge anchoring, Nodes drag, Paint with pointer-leave cancellation, real OpenCV GrabCut, exact same-byte replacement output, Preview/Save, and Clear/Undo/Redo.

## QA rework 2

- QA independently cleared adoption, fallback, viewport reconciliation, full static/build gates, and the real route at snapshot `78a21d9d0e93f5aaf81fc9c22ac05ae462c1a30e`.
- Two oracle obligations remain: prove all eight Frame resize targets plus one corner's fixed opposite anchor; assert the fixed-viewport post-GrabCut PNG at 1415x660 RGBA, SHA-256 `55e6178e24616933bba926474da07a6e8340dc50938af494663152a1176e158d`.
- Minimal diff: edit only `scripts/verify-cutout-v1-preservation.mjs`. Keep product code, production seams, frameworks, and KAI-10217 untouched.
- Final snapshot: `88dede13066dd7e22db365568943150f90e22e0a`, pushed with exact local/upstream equality. The fixed-viewport route oracle, full tests, typecheck, scoped lint, diff hygiene, and production build pass.
- Corrected the page adoption record to `selective-donor`; stale `?seg`, eruda `?debug=1`, and `?admin=1` calibration remain in the current oracle UI but are excluded from the final product shell destination.
- Reconciled visual evidence without changing output: fixed 1280x720 emits 1330x621 / `d7a28a...`; QA's repeated 1200px-wide run emits 1329x622 / `20ea0230...`. QA assigned this viewport-dependent one-pixel output variation to KAI-10219.
- Final repair gates before commit: 57 test files pass plus one skipped file; 524 tests pass, six expected failures, ten skipped; typecheck, scoped lint, diff hygiene, production build, and the current-code real-route preservation script pass.
- QA repair rollback snapshot committed and pushed at `78a21d9d0e93f5aaf81fc9c22ac05ae462c1a30e`; local and remote branch heads match exactly. `_WIP/` remains untracked and no KAI-10217 work exists.

## KAI-10216 necessity review

- Necessity — **no unnecessary elements.** The repair adds only the approved real-route oracle, the smallest stable fallback-callback test, the corrected adoption classification, and its npm entry; no production abstraction, UI edit, framework, relocation, or later-task fix exists.
- Sufficiency — **delivers KAI-10216 in full.** Every line-52 behavior maps to an executed oracle; every remaining Increment 1 requirement maps to current source/test/build/runtime evidence. The existing UI-facing API is preserved, all later defects remain explicit expected failures, no file is relocated, no intermediate package exists, and no KAI-10217 work is built ahead.
