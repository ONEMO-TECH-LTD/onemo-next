# KAI-10285 Builder Ledger

## 2026-08-12 final repair audit

- Authority: Paint erase may carve only a smooth local boundary from one solid blob. Internal, near-loop, or destructive splitting gestures are no-ops. The accepted AI/GrabCut shape, CLASSIC recipe, original-resolution output, history, and GrabCut product source remain unchanged.
- Root cause after 482bac6c: mask-only subtraction could prove no holes but could not preserve the accepted Bezier outline. Re-tracing changed untouched geometry; Paper subtraction could also return small detached residual paths differently in Chromium and WebKit.
- Minimal implementation: use the exact accepted mask for output truth, a solid zero-offset proxy only for erase legality, and the current visible shape for local Paper subtraction. Retain the main blob only when discarded residual area is no larger than the eraser itself; reject larger splits. Preserve resolved shape/mask and recipe across Undo/Redo.
- Oracle: default CLASSIC standalone GrabCut; internal erase exact no-op; Dan's near-returning destructive gesture exact no-op; valid shallow boundary carve changes only the narrow erase region; CLASSIC remains selected; exact Undo/Redo; Chromium and WebKit.
- Current visual evidence: `output/playwright/KAI-10285-chromium-grabcut-base-before-paint-erase.png`, `KAI-10285-grabcut-base-after-paint-erase.png`, `KAI-10285-grabcut-base-undone-paint-erase.png`, `KAI-10285-grabcut-base-redone-paint-erase.png`.
- Gates before source audit: 551 pass, 10 declared skip; typecheck; scoped lint; diff check; production build; preservation, detector, flow, output, and GrabCut Chromium/WebKit oracles; stable 40-file closure SHA `880d5a401f4906953bbcd1ed280de8b7b2ffc87aa75c01195529dd6b11ff9a64`.
- One failed intermediate gate and correction: Paint-add unintentionally inherited the exact raw-mask base, changing the preserved OpenCV output hash. The exact-mask base is now erase-only; Paint-add retains its prior solid-shape base. Preservation returned to the locked `64c79250…` result.
- Source audit: every builder-changed file and the complete diff were read. Runtime wiring is live through `paintStroke → resolvePaintMask → acceptMask`; no CutoutStudio UI, route, GrabCut product source, provider, or second engine changed. The adoption map classifies the two existing dependencies and the generated closure is byte-stable.
- Necessity: no unnecessary elements. The delta is one mask topology/locality guard, one operation in the existing Paper kernel, exact history state, and proof updates.
- Sufficiency: delivers the complete live directive set: internal and destructive near-loop no-op; one smooth local boundary carve; no holes/detached visible result; active Paint/Vector finishing on the negative only; untouched CLASSIC main/recipe; exact Undo/Redo; original-resolution output; unchanged GrabCut behavior.
- Visual gate: production build from this worktree on port 4011, real `/cutout-lab?admin=1` route via established Playwright fallback. Standalone GrabCut → CLASSIC → internal/near-loop rejection → shallow edge carve → Undo → Redo was observed in Chromium and WebKit. The captured Chromium frames show only one smooth rear-edge notch and exact restoration/reapplication.

## 2026-08-12 owner-reopened correction plan

- Dan rejected the prior repair on the real Paint-created shape. Required behavior is cumulative: Paint erase is inverse Paint; no holes/gaps/splits/fragments; Autotune the open erase stroke; normalize only the new cut boundary with Paint Mask smoothing; preserve the subject outside that band exactly.
- Paint ownership is also corrected in the same cutover: a newly accepted Paint shape is created only by brush geometry, loop close, Paint Autotune, and Paint Mask smoothing; shared Vector state starts at exact `0/0/0/0/0`; later deliberate Detail/Offset/Simplify/Smooth/Radius changes use the existing shared resolver equally.
- QA-agreed lineage: `b34b9b69` is the live shape-relative smoothing ancestor; `e8cf49b9` is the last pre-experiment architecture donor but not a shippable rollback; `982504db..b2734220` is the rejected resolved-negative/Paper repair chain. No wholesale rollback.
- QA-agreed minimal diff: erase begins from the exact accepted mask. Delete `solidBase`, `baseShape`, `acceptedShape`/`visibleShape`, `ResolvedPaintMask`/`resolvePaintMask`, `finishResolved`, Paper subtraction, `maskWithinTopology`, erase-specific Vector recalculation, and `retainPrimaryMaskBlob` if orphaned.
- Local erase math: open tuned swath; raw subtract; heal holes; actual removed-pixel delta determines the affected band; expand that delta by the full `smoothMask` influence radius; splice the existing whole-result `polishMask` candidate only inside that band; preserve base data/soft bytes outside; inside, zero erased soft alpha and retain original alpha for surviving/restored foreground; validate the final splice for non-empty, one component, no holes, and no destructive loss.
- Necessity: one existing Paint mask owner, one existing shared Vector resolver, net deletion of the parallel path; no new engine, smoother, history owner, provider, framework, or abstraction.
- Sufficiency target: fresh Paint Vector zero state; all five shared Vector controls visibly respond after acceptance; internal/near-loop/split/destructive erases are exact no-ops; valid boundary erase is one smooth solid shape with byte-exact off-band data/soft; exact Undo/Redo and original-resolution Save; unchanged GrabCut.

## 2026-08-12 owner-reopened implementation checkpoint

- Implemented the QA-agreed cutover: exact accepted mask base; one mask-tools erase owner; actual subtraction delta expanded by `smoothMask` influence; off-band binary/soft bytes exact; raw requested cut cannot be healed; internal/hole/split/destructive results return exact base.
- Deleted the rejected Paper/finished-negative route and its helpers/tests/imports. Fresh Paint alone resets shared Vector to `0/0/0/0/0`; accepted Paint add/erase and Paint-config replay retain the selected recipe; all later Vector controls use the existing shared resolver.
- Static gates: 549 pass + 10 declared skip before the latest focused proof; focused mask suite 12 pass; typecheck, scoped lint, diff check, and production build pass. Preservation deterministically changed only its intentional Paint-ownership golden to `1832×813`, SHA `aa86aa7c…`, then passed.
- Route diagnostic: the destructive/no-op guard is correct. A geometry-derived 15px shallow notch across the actual largest accepted mask component settles as `erased — auto-tuned`. Exact row is y218, run x149..378 (230px); stroke canvas x385.5→370.5 across boundary x378.
- Open proof issue: whole rendered-canvas RGBA changes broadly after the accepted local notch (10,707 off-box pixels, diff x54..403/y163..272). This metric includes the full post-acceptance bake and may not be the geometry invariant. Product is frozen while Builder/QA determine whether the exact route proof must compare overlay-derived mask plus blue outline locality instead. No further product change before that agreement.
- Final source-coordinate falsification proxies the native `Path2D` used by CutoutStudio and samples its exact SVG curve. ZERO is effectively local (10 samples over a 1.1px threshold, max `1.69px`); retained CLASSIC moves globally outside the cut band (127 samples, max `89.82px`). This proves current shared-resolution of the changed mask violates the owner’s untouched-main requirement; the earlier Nodes result was representation-only and is retired. Product remains frozen at the QA HOLD pending an agreed correction to the constraint conflict.
