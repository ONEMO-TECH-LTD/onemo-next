# KAI-10285 — 9330a693 de-slop QA

## Verdict

**CLEAR.** The snapshot is a dead/stale-code cleanup over the owner-accepted `c4f17f47` golden build. No product-behavior, exported API, default, range, route, package, or architecture change is present.

Reviewed commit: `9330a6935eee284824051f04a3bc80dddae8e773`  
Baseline: `c4f17f47f7f12c6f0c958699cd158619c07eb970`  
HEAD == upstream: yes.

## Independent source verdict

- `scripts/cutout-lab-verify.mjs` was only a three-line import wrapper. At baseline its only tracked references were the closure generator and generated closure inventory; no package script, caller, or public entry used it. Deletion is safe.
- `acceptMask` never read `erase`. The property and its three call arguments were removed; the live branches, values that affect acceptance, and Paint/GrabCut behavior remain unchanged (`flow.ts:319-324,431,609,637`).
- The cursor ternary returned `brushRef.current / 2` on both branches. Its collapse is exact (`CutoutStudio.tsx:192-200`).
- Only the unused `simplifyPaper` binding was removed from `outline-resolve.ts:24-30`. The exported implementation remains in `paper-kernel.ts` and its public/test consumers remain tracked.
- The `mask-tools` change is comment-only and now describes the accepted direct-subtraction erase behavior (`index.ts:82-84`).
- The closure generator dropped only the deleted wrapper exception (`generate-cutout-studio-closure.mjs:76-78`). A clean temporary generation after the production build was byte-identical to the committed closure: `a06cb4a5fac0edf0e6353e57e8dca141cb18b62672e793e5054807971b8ea7d9`.
- Golden oracle values now match the running accepted build. Builder's phrase “literals/hashes only” is slightly too narrow: `verify-cutout-v1-grabcut.mjs:434-438` also changes one stale assertion from “smoothing changes erase output” to equality. That is an oracle consistency correction for the locked direct-inverse behavior, not a product change or new architecture.
- No modified runtime file changes a barrel/export declaration, package manifest, route mount, default/range constant, or public closure API. The only modified `index.ts` contains a comment-only diff.

## Gates run independently

- `git diff --check c4f17f47..9330a693`: pass.
- TypeScript: pass.
- Focused ESLint on all changed source/oracle files: pass.
- Focused unit set: 31 pass.
- Full Vitest: 546 pass, 10 declared skip.
- Production build: pass.
- Preservation route: pass; exact outputs `1725×777 / 9a21d00e…` and `1833×815 / 4b003b17…`.
- Detector: pass in Chromium and WebKit.
- Output: pass in Chromium and WebKit.
- GrabCut: pass in Chromium and WebKit, including accepted edge default `12`, vector default `CLASSIC`, Paint recipe, direct-inverse erase, undo, provider count, and frozen output hashes.
- Current-code route provenance: production build made from `9330a693`, served from this worktree on port 4219. Captured surface: `../evidence/KAI-10285-9330a693-current-production.png`, SHA-256 `f3e2681c6c095be0067f107f355d4f7e77c8c206ecc61a36e353235b4ae80931`.

## Disclosed baseline residual

`verify-cutout-v1-flow.mjs` independently reproduced the known timeout at line 232 waiting for the second `/painted shape created/` status. The oracle is byte-identical at baseline and snapshot (SHA-256 `edaa05791eee673fabbc5bee3fb35d9ba2a8764e65633c691a0eb3590fcaf61e`), and the reviewed runtime diff does not alter that behavior. It is not introduced by this cleanup and is outside the strict golden-lock edit scope.

## Necessity / Sufficiency

**Necessity:** no unnecessary production element remains in this diff; each runtime edit deletes an unread binding/argument or collapses identical code, and the remaining edits repair deletion-driven or already-stale tracked evidence.

**Sufficiency:** delivers the locked de-slop scope in full. The dead wrapper, unread option/arguments, identical branch, unused import, stale comment, stale golden evidence, and generated closure are all resolved; no product rework or protected public surface is included.
