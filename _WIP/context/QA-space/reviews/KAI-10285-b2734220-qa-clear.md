# KAI-10285 QA CLEAR — `b2734220…`

Exact snapshot: `b2734220e08d33fc05a34a6e2325c0d52d70afe1` (local = upstream).

## Verdict

**CLEAR.** The accepted raster mask, stored/history mask, exported mask identity and visible Paper shape now share the approved retained-primary topology. The prior hidden detached binary/soft residual cannot survive export.

- `paintMask` reuses the already-approved `primaryCarved` decision and gates the original accepted mask before `acceptMask`.
- Independent helper proof removes the detached residual from binary and soft channels, preserves surviving bytes exactly, and leaves one component.
- `acceptMask` stores that exact mask; history clones it; `exportResult` clones and hashes the same binary and soft bytes.
- Destructive split rejection, internal/near-loop no-op, visible Paper subtraction, CLASSIC/history behavior and raw GrabCut are unchanged.

## Gates

- Focused: 12/12 product tests plus 1/1 QA topology probe.
- Full: 59 passed files + 1 skipped; 552 passed + 10 declared skips.
- Typecheck, zero-warning scoped lint, diff check and production build: pass.
- Generated closure: byte-exact after regeneration; SHA-256 `945716a7a1d3816e5268b3137d4f3d56867bfd3f89c5101da81cf52d6645e625`.
- Browser: preservation, detector, flow, output and GrabCut/Paint oracles pass across their declared Chromium/WebKit coverage.
- Visual gate: exact production build served from this worktree at `b2734220…` on port 4018. On the real `/cutout-lab?admin=1` route via the established Playwright Chromium fallback, accepted standalone GrabCut → CLASSIC → shallow Paint erase produced one local smooth notch with no fragmentation; Undo restored the exact backing canvas and Redo produced the exact same original-resolution Save PNG. Evidence: `_WIP/context/QA-space/evidence/KAI-10285-boundary-erase-b2734220.png`, SHA-256 `443040d37aaadf336618daca0723e509ba1c80c0ce0bd15425f2b427410e2e50`.
- Rejected near-loop: zero loop-interior loss, no history step, CLASSIC retained, and `inside stays solid` shown. The only console warning was emitted by QA's repeated canvas-readback probe.

Necessity — **no unnecessary elements.** One existing topology gate, one helper and its proof are the smallest correction; no engine, UI, route, provider, geometry owner or GrabCut product change.

Sufficiency — **delivers the directive in full.** The visible one-blob law and the hidden accepted/history/export mask now agree, while all accumulated erase/history/output/GrabCut behavior remains intact.
