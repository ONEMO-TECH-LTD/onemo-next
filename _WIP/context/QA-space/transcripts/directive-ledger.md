# S62 directive ledger

Only source-exact transcript evidence belongs here. Compaction summaries are not evidence.

## Read status

- Lead 2026-08-08 `_day.md`: complete, lines 1–2006 read sequentially.
- Claude Meta 2026-08-04 `_day.md`: complete, lines 1–1309 read sequentially.
- Claude Meta 2026-08-06 `_day.md`: complete, lines 1–760 read sequentially.
- Claude Meta 2026-08-07 `_day.md`: complete, lines 1–2416 read sequentially.
- Pixel Meta 2026-08-08 `_day.md`: lines 1–472 complete; V1 crash-authority lines 612–637 targeted and re-read.
- Pixel QA 2026-08-08 `_day.md`: exact current V1-contract authority windows 2036–2660 re-read; current live turns appended from the pane.

## Governing directive set

### Lead transcript — 2026-08-08

1. **06:30 — useful current-v2 scope.** Report only behavior present in current v2 that was tuned correctly in v1 and still needs repair; retired SAM history is not the deliverable.
2. **07:07–07:10 — task discipline.** File the work, run `/o-necessity` before it, `/o-deslop` after it, self-audit, and preserve clean rollback snapshots. Show the exact full plan before coding.
3. **07:40 — compose fill.** Clamp is the default and only fill. No mirror and no tile surface.
4. **11:47 — vector contract.** Detail, Simplify, Smooth, Radius, and Offset are one collective workflow; all must contribute when used together.
5. **12:28 — module provenance audit.** Audit every module claimed to be built from existing code against the real v1 source, the extracted pool, and integrated v2. Headers and transcript claims are not proof.
6. **13:04 — historical artifact.** Keep the `v1-addons` worktree as a separate historic artifact for now.
7. **13:21 — code-first comparison.** Re-read the lead and Meta transcripts, then re-read and compare clean v1, the extracted modules, and final integrated v2. Keep durable notes under v2 `_WIP/context`.
8. **13:43–14:16 — correction set.** Decode the Frame behavior plainly. Determine bridge/compositor truth from code. Clone inferior Frame behavior from v1. Prove the full v1 vector setup is a twin clone. Delete the approximate GrabCut provider and restore real v1-equivalent GrabCut without unused OpenCV bulk. Treat Pixel observations as notes until proper QA runs.
9. **14:29 — complete approximation inventory.** Diff integrated v2 against v1 and compile every retained keeper, missed keeper, approximation, regression, and misplaced policy into repair tasks. Opinion from transcripts without code is unacceptable.
10. **14:36 — plan artifact.** Create the complete `v2 fixes` sprint plan in `_WIP`, with each task naming the problem and exact repair.
11. **14:42–14:43 — no invented decisions.** “Clone” is literal. GrabCut is extraction and assembly of OpenCV-owned logic, not new algorithm construction. Finish the plan first; QA runs separately. Run `/o-necessity` and `/o-deslop` over both v2 and the plan, then verify by rereading the code in full.

### Current Dan corrections in this lane

12. Fix the plan itself before any build execution.
13. Separate transcript context from QA evidence; do not use QA observations as transcript history.
14. Base the corrected plan on actual v1/v2 code reads, not compaction summaries or inherited audit claims.
15. Apply `/o-necessity --review` to the entire plan and `/o-deslop --review` to the cleanup/kill-list.
16. Store all Pixel working files and permanent rehydration ledgers in `context/QA-space`, separated by category.
17. Independent Meta must review the corrected plan before builder handoff.

### Meta transcript sequence

18. **2026-08-04 — closed-loop verification failure.** A contract and its oracle can agree while both omit the real applied behavior. Verification must use the canonical implementation and adversarial product cases, not definitions written by the same work.
19. **2026-08-06 — Cutout layer boundary.** The engine and portable tool modules sit below the flow. The flow owns bridge semantics, cadence, single-flight/coalescing, history/session policy, failure/degradation, and shell bindings. The shell is render and interaction only. Performance and the real caller are part of the gate.
20. **2026-08-07 — rebuild and module-pool evolution.** Dan repeatedly required an untouched v5.3.1 base, full code reads, literal v1 behavior where directed, no parallel bridge, and add-ons grouped by engine/tool/bridge/shell. Later claims that the extracted pool was complete and the assembled increments were clear were builder/reviewer claims, not current proof; the next-day lead audit and the present source must decide what survived.
21. **2026-08-07 — exact failure class.** Meta admitted that earlier “engine clean” and “fully wired” verdicts were false because they checked sanctioned-file labels and engine-call existence rather than file contents and the canonical bridge usage pattern. The corrected plan must make those checks structural.
22. **2026-08-07 — rollback and QA cadence.** Each implementation increment requires a clean snapshot, code review, and independent live observation before the next product/device gate. Final acceptance is full current v1 behavior without crashes, not a subset declared complete.
23. **2026-08-08 Pixel Meta — independent lane state.** Pixel Meta fully read the lead and Pixel QA transcripts and the context pack, then began its own v1/v2 code audit under `context/meta-space`. Its future verdict is independent evidence; this ledger does not inherit it in advance.

## Superseded v2 planning deliverables

These were the complete deliverables while v2 remained the candidate base. They are retained only as transcript history. The V1 production-plan extension below—especially items 24 and 32—superseded them; they are not current contract authority.

- A complete, code-accurate v2-fixes plan, rewritten in place rather than duplicated.
- Literal v1 clone tasks only where clone was directed; separately label non-clone correctness repairs.
- Full inventory of present v2 approximations, regressions, missed keepers, misplaced policy, and deletion work.
- Real OpenCV GrabCut replacement expressed as measured extraction/assembly, with no approximate provider or heavy silent fallback.
- Complete cleanup disposition backed by re-export-aware production tracing and tracked-tree checks.
- Contract regression tests and one stable-head QA closure after implementation.
- Plan-level necessity and sufficiency verdicts.
- Independent Meta verdict and repair loop before handoff.

## V1 production-plan extension — 2026-08-08

24. **18:04 — base decision, later narrowed by item 32.** Dan asked whether to finish v2 or fix v1. This lane recommended v1 as the base. Its earlier v2-donor qualification is superseded.
25. **18:10 — OpenCV and structural question.** Dan required an answer on slimming OpenCV to the GrabCut closure and on whether v1's bridge/compositor divergence is useful structure or substantive slop.
26. **18:15 — proposal accepted as planning input.** The recorded answer preserves exact v1 GrabCut and legitimate lab-only capability, but replaces oversized runtime packaging and duplicate history/generation/queue/final-pixel ownership. It prohibits a new GrabCut algorithm and a whole-lab rebuild.
27. **18:31 — current deliverable.** Dan: “ok time to plan v1 polish and optimisation and clean up sprint then - all the above must be contract plius linear sprint - before you produce the contract save verbatim your report above in your space please so we have memory of what you proposed on the intro level”. The verbatim prerequisite is complete at `../reports/v1-polish-intro-proposal-verbatim.md`.
28. **18:38 — mobile, de-slop, and liftability.** Dan required the v1 plan to cover mobile/performance, `/o-necessity`, `/o-deslop`, modularity, and a structurally clean production package that can be lifted from `onemo-next` into a clean production repository.
29. **2026-08-07 15:22 — exact detector owner statement.** Dan: “remove sam and wand brushes delete completely deslop / use u2 net and lets test the grab cut as additional brush feature for u2 net if it doesnt work we fallback to paint shape and node system”. Lead read this back as “u2net becomes the only cut engine.” This proves deletion of SAM/Wand and selection of the u2net family; it does **not** explicitly name Silueta or distinguish u2netp-only from the canonical lazy u2netp→Silueta fallback. Silueta removal is therefore not source-proven by this statement alone.
30. **Current — joint consolidation directive.** Dan: “ok now exchange the plans with meta review each other and consolidate the final plan made of the best parts you both agree with”. Both independent plans are frozen inputs; each lane must adversarially cross-review before either plan is edited, then both must reread the same consolidated bytes.
31. **Current — lane identity correction.** Dan: “you are not pixel qa - you are s62-pixel-qa use your id”. This lane's exact identity is `@s62-pixel-qa`; future records and messages must use that ID. Frozen review artifacts remain byte-stable under their recorded hashes.
32. **21:26 — v2 is not a donor.** Dan: “v2 donour for what exactly if it was rebuild from v1 ?” Lead's source comparison confirmed that v2's reusable modules are v1 extractions while its new GrabCut, Simplify, and Frame implementations are inferior approximations. Dan then corroborated: “yes”. Current rule: fix and modularise v1 directly; do not copy v2.
33. **Current — universal Necessity Law.** Dan: “o-necessity law applies to all”. The law binds the whole contract, every increment and Linear task, every later implementation diff, and every Builder/QA/Meta verdict. No plan-level clearance exempts a smaller unit: each unit must state both necessity and sufficiency against the full still-live directive set.
34. **Current — stop process churn and re-ground the contract.** Dan required every contract change to be necessary, sufficient, and tied to a real v1 problem; warned that Meta's early mistakes and compaction may have driven an overengineered cycle; and directed this lane to reread its own transcript, zoom out, and remove process without product meaning. The current contract must therefore lead with verified v1 problems and prescribe only the smallest repair that closes each one.
35. **20:59 — one drafting owner.** Dan kept contract drafting in `@s62-pixel-qa`, required prior notes and current v1 code to be matched under `/o-necessity` and `/o-deslop`, and prohibited another parallel drafting cycle.
36. **21:21–21:23 — exhaustive and precise, not line-count theatre.** Dan rejected line reduction as the objective. The contract must be exhaustive and precise against the audited roughly 1,700-line product, while measurements remain measurements rather than invented certainty.
37. **21:23 and 21:37 — independent review inputs.** Dan required Meta QA of the frozen proposal and then reciprocal Builder review. These verdicts are advisory evidence; neither reviewer owns contract, Linear, or product edits.
38. **21:42 — verify reviewer corrections.** Dan required this lane to check whether Meta's proposed changes make sense and are source-verified before adoption. Reviewer agreement alone is not authority or proof.
39. **Current — direct clean-repository adoption, no premature intermediate move.** Dan: “we need to make sure we are not prematurely doing the move unless this prepares us to later move the entire engine to be adopted in the clean '/Users/daniilsolopov/Dev/onemo-dev/onemo-effects-engine' repo from which we will compile the final prod without carrying the entire onemo-next prototyping history”. Current rule: polish v1 in place; no wholesale React/DOM move into `onemo-next/src/lib/`; prove the exact direct-adoption boundary into the existing clean engine repository.
40. **Current — final Figma React shell and canonical magnetic-grid handoff.** Dan: “we need to swap the current admin mock ui in the v1 lab onto the proper final ui we have in figma to be converteed into react - and also importantly shaped effect grid-lab part for the magnetic grid match for manufacturing and sizing of the free shaped sticker like effect to haver mountable magnetic grid on the back surface based on the default grid laws and sizing”. Current rule: the mock/admin UI is a prototype harness and does not move into production; later clean-repository adoption uses the pinned owner-designated Figma frame as the React shell. Cutout hands the exact final manufacturing contour to the canonical Grid engine; Grid alone owns physical size/rung and magnetic backing under the accepted default laws.
41. **Current — portable final V1 package is the deliverable.** Dan: “current goal is fix the v1 - if we need to relocate it as part of it - fine - what i need is portable final packaged engine that can be supplied with additional logic and new ui and moved and used in the new repo as product webapp studio module”. Current rule: relocation is allowed and required when it creates the final portable package; only premature wholesale movement of the current mixed prototype is forbidden. The sprint ends with one product-owned engine/browser-studio closure, a stable UI/downstream-result contract, and no duplicate route-owned implementation.
42. **23:38 — no Figma work in the V1 fixing sprint.** Dan: “No figma yet we are doing v1 fixing dude”. This supersedes Figma lookup, pinning, mapping, conversion, evidence, and acceptance work in the current sprint. Final UI work is separate.
43. **23:42–23:43 — UI is not a problem or deliverable.** Dan: “UI is already there just fucking reminder UI never an issue.” Current rule: preserve the existing UI unchanged. Do not create a shell-neutral-interface project. Preserve the existing UI-facing `state`/`actions`/`view` API during modularisation; change it only if relocation reproduces concrete coupling, using the smallest behavior-preserving correction.
44. **2026-08-11 — portable boundary now; Grid binding later.** Dan confirmed the active Grid engine is still being built by the Grid lanes and agreed that this sprint must make Cutout portable, liftable, and usable with any UI while stopping at a deterministic output boundary. Current rule: publish the exact final Cutout result without importing, adapting to, or claiming compatibility with an unfinished Grid contract. Grid integration is a downstream consumer task after the new Grid interface is frozen.

### Current contract obligations

- V1 is the selected repair baseline; v2 is not a parallel implementation lane.
- Preserve the current v1 product behavior and exact OpenCV GrabCut output as the oracle.
- Remove obsolete EdgeSAM/SAM and the complete comparison/query/Transformers tail. Preserve the canonical u2netp → lazy Silueta → visible flood-fill chain; no owner statement authorizes deleting Silueta.
- Repair real lifecycle defects only: atomic replacement upload, artwork invalidation, cancellation/settlement, bitmap/cache ownership, lossless FIFO gestures, atomic history restore, truthful output failures, one-point Paint, the source-proven selected-node rebase, and pointer cancellation. Preserve current node insertion/deletion/selection unless a failing journey proves otherwise.
- Keep the canonical compositor and necessary Cutout clip/crop/coordinate adapter. Remove Cutout Mirror/dormant glue; preserve shared Tile/non-Cutout callers. Do not add Cutline or raw natural-resolution Save; preserve truthful capped transparent Preview/PNG.
- Measure exact OpenCV 5.0.0 against one official same-version `core + imgproc` candidate. Retain one provider only after mask, footprint, responsiveness, and physical-iPhone proof; add a worker only if the selected provider otherwise fails the responsiveness gate.
- Polish the current flow/tool unit, then cut it over into one product-owned portable package with headless engine and browser/React studio layers. Do not stage the current mixed React/DOM unit wholesale inside `onemo-next/src/lib/`; move/re-export existing cleared owners only after their boundary is proven, delete old route-owned copies in the same cutover, and map the exact package into `onemo-effects-engine` without copying v2, debug/admin surfaces, or repository history.
- Preserve the current Cutout UI and its existing UI-facing API unchanged as the behavior oracle. Do no Figma, UI redesign, replacement, or new interface-design work in this sprint. Exclude only route-specific calibration/debug UI from the portable engine closure; change the UI boundary only if relocation proves concrete coupling.
- Preserve one vector/manufacturing truth. Publish the final edited Cutout shape, exact manufacturing contour, scale, artwork/mask identity, and deterministic inputs as a stable output boundary. Do not bind the package to the legacy or unfinished Grid engine, duplicate Grid law, or make browser preview the manufacturing authority. A later adapter may consume this exact result after the new Grid contract freezes.
- Delete obsolete/duplicate/stale code in the owning cutovers. Delete stale architecture prose without replacing it; replace the stale verifier and emit one generated/hashable closure record.
- Keep the historical phone crash as an active bounded investigation. Record actual device/OS/Safari/caps/Low Power evidence and either a causal result or `cause unproven`; do not invent a broad support floor.
- Every implementation increment passes Builder → independent QA → independent Meta; the integrated candidate additionally passes independent desktop and physical-iPhone gates.
- Contract and Linear sprint must map every obligation exactly, with necessity and sufficiency verdicts.
- Necessity Law is universal across the programme and every contained plan, task, diff, and verdict; both necessity and sufficiency are required at each applicable gate.
