# Package Contract

The neutral candidate-record and enumeration contract is:

- `enumerator/CONTRACT.md`

The accepted measurement-kernel contract remains unchanged at:

- `magnetic-grid-measurement-kernel/CONTRACT.md`

The root test command verifies the accepted kernel against its original SHA-256 manifest before and after running both suites.
