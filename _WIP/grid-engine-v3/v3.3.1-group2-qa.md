# v3.3.1 implementation — Group 2 QA

**Audited commit:** `3e7577fdf582f2b0aa843bf92f792b0519a6e79e`

## Verdict

**REVISE — one authority boundary remains open.** The certified solver, compound comparison, anchored restriction, representable tie-break and per-rung offer construction pass. The public ManufacturingSpec constructor still accepts heuristic or missing-rung evidence directly.

Group 3 must not start until that boundary is closed and rechecked.

## Passing work

1. Compound `min/min` uncertainty produces anchor `([0,0],[100,100])`; both A and B survive; symmetric candidate comparison returns uncertain rather than using the later component.
2. Final registration enumerates the boundary box's complete representable set and selects `(0.25,0)` after 40,401 points instead of returning a false below-quantum result.
3. The heuristic witness solver and its false `EXACT` authority are deleted. `solveOutline` independently calls `certifySizeSolution` for every profile rung.
4. `buildCertifiedBandOffers` blocks missing rungs and unresolved smaller rungs before applying `SMALLEST_ACCEPTED_PER_BAND`.
5. Independent execution: build hashes reproduce; full suite 44/44 with zero skips; public compound and tie-break probes match the contract; diff check passes.

## Blocking finding — ManufacturingSpec authority is adapter-only

`magnetic-next/src/certification.ts` checks selected-rung and smaller-rung evidence. The exported Logic constructor `createEngineManufacturingSpec` does not. It checks `decisionProof` only when `profile.productionReady` is true, and it never verifies that the supplied solution is the certified offered rung backed by `solve.evaluated`.

The exported `magnetic-next/src/selection.ts::bindSelectedBand` calls this unguarded constructor directly, so it remains a parallel bypass.

Independent public-API probe against the repaired build:

```text
createEngineManufacturingSpec(empty evaluated/offers, heuristic solution, reference profile)
→ ACCEPTED as REFERENCE_PROFILE_NOT_PRODUCTION

createEngineManufacturingSpec(empty evaluated/offers, certified solution, reference profile)
→ ACCEPTED despite no smaller-rung evidence
```

This contradicts the Group 2 contract that heuristic authority, score uncertainty and missing smaller-rung proof cannot create an Engine ManufacturingSpec. A non-production proof-status label does not turn an Engine ManufacturingSpec into something else.

Required repair: enforce at the public Logic ManufacturingSpec boundary that the supplied solution is `CERTIFIED_CONTINUOUS_OPTIMUM`, is the actual certified `OFFERED` solution reconstructed from `solve.evaluated`, and has complete rejected evidence for every smaller rung in its band. The existing Next adapter may retain its early checks, but it cannot be the authority boundary. Do not implement source-identity/tamper recomputation or historical resolution here; those remain Group 5.

Add direct Logic regressions for both probes above. No new package, module or alternate constructor is needed.

## Necessity and sufficiency

**Necessity — no scope growth:** retain all solver repairs. Add the missing checks to the existing public constructor and focused tests only; do not begin Group 3 or Group 5.

**Sufficiency — partial:** certified optimisation and offer construction are closed. ManufacturingSpec authority remains bypassable until the public constructor enforces the same certified-rung evidence.

## Boundary-correction recheck

**Audited commit:** `fef3318da69302c1ac73cb385f49afb12f74c330`

**CLEAR.** The existing public Logic constructor now rejects heuristic authority, missing evaluated/offer evidence, a supplied solution that differs from the reconstructed and declared offer, and a later accepted rung without complete smaller-rung evidence. `bindSelectedBand` reaches the same repaired constructor. A valid certified single-rung reference spec still succeeds.

Independent public-call results:

```text
heuristic-empty → MECHANICAL_OPTIMUM_NOT_CERTIFIED
certified-empty → CERTIFIED_OFFER_EVIDENCE_MISSING
solution-mismatch → CERTIFIED_OFFER_SOLUTION_MISMATCH
missing-smaller → CERTIFIED_OFFER_EVIDENCE_MISSING
Next missing-evaluated → CERTIFIED_OFFER_EVIDENCE_MISSING
Next valid → REFERENCE_PROFILE_NOT_PRODUCTION
```

Full suite: 45/45, zero skips. Two independent builds reproduced Compute `28afb40d95b141a66dcd52f37d0881fe1242013f33071c5f71225cd32ac96cc3` and Logic `3518727c5c958cc640ecc922f22ecd36443eea4fcb7872db0dbc2faaa53d284f`.

Runtime gate: `npm run demo` from this exact commit exercised the real CLI caller. It emitted a certified B1 reference/non-production ManufacturingSpec and withheld B2–B5 as `DECISION_INDETERMINATE`.

**Necessity — no unnecessary elements:** the correction changes the existing constructor, one direct Logic regression, and the generated Logic digest only. No new API, module, duplicate policy, Group 3 work or Group 5 work exists.

**Sufficiency — delivers Group 2 in full:** the certified solver, global/compound restriction, complete representable tie-break, per-rung offer construction and public ManufacturingSpec authority boundary now satisfy the Group 2 outcomes.
