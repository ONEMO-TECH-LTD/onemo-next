# v3.3.1 implementation — Group 4 QA

**Audited commit:** `742554a26909f3ed91335ad8113cdeeeeb474b3b`

## Verdict

**CLEAR.** Group 4 closes the governed-mechanics defects identified by the implementation audit. Population parity is an applied and persisted frame identity; every specified PD-19 permission dimension is executable and fail-closed; M02 follows the registered top direction; canonical comparisons are code-unit based; and certified outputs contain executable M09/M10 decision evidence after M01–M08.

Group 5 may start. This verdict does not clear identity/history/tamper verification, Next integration, delivery/performance or the unresolved product values owned by later groups and the ballot.

## Contract evidence

1. **Population stride and origin parity:** `frames-registration.ts:12-29` enumerates every registered parity, derives population-stride frame capacity, applies the master-node parity offset to base-cell coordinates, and converts the resulting cells to physical offsets. `certified-solver.ts:115-163` creates a separate candidate identity for every parity. `manufacturing-spec.ts:28-30` persists population ID, stride and parity.
2. **PD-19 permissions:** `profile-schema.ts:111-133` requires exact bands, X/Y class pairs, approved populations, marginal-node policy, required major-region coverage, alternative-orientation policy, primary/fallback eligibility and rank. `patterns-permissions.ts:4-18` applies the discrete permission dimensions. `certified-solver.ts:72-90` applies major/marginal-region rules and `:166-168` applies primary/fallback eligibility.
3. **Registered top direction:** `mechanics.ts:4-27` projects region bounds on the registered unit vector and uses the same vector and lateral axis for M02–M04.
4. **Canonical ordering:** `selection.ts:4-19`, `patterns-permissions.ts:18` and `geometry-compute/src/components.ts:4,94` use explicit code-unit ordering; no production-source `localeCompare` remains.
5. **M09/M10:** `mechanics.ts:32-33,50-58` defines the complete discrete identity and final registration descriptor. `certified-solver.ts:307-364` runs M01–M08 first, then M09 discrete selection, then M10 final registration inside the surviving optimum set and records both trace rows.

## Independent execution

- Build succeeded twice with identical generated identities: Compute `741121c590dcb96f49703bdbb7a8c49a0796aa55ca2aa03155a9e36d0572bb76`; Logic `aee44d923d62cf71277ccf1df8cca9991a169362b79ee018e4e785b2c570f349`.
- Full suite: 61/61 pass; zero failures, skips, todos or cancellations.
- Independent conditional-96 probe registered a four-cell-stride population with all four master-node parities. It produced base-cell origins `(0,0)`, `(2,0)`, `(0,2)`, `(2,2)` and 96 mm node spacing; every physical node satisfied `masterIndex mod 2 = registered parity`.
- Independent permission probes removed or malformed each PD-19 field and observed fail-closed registration. Exact X/Y class and population filtering, major coverage, marginal-node, primary/fallback and orientation-family behavior were exercised separately.
- Independent top-direction probe with `(-1,0)` selected the left region rather than the high-Y region. The `ä` versus `z` identity probe selected `z`, matching raw code-unit order rather than locale order.
- Independent accepted-size probe produced all ten trace rows in registered order. Its Engine ManufacturingSpec retained stride/parity, the six-part M09 identity key and the M10 registration.
- Generated reference JSON was structurally identical to `createReferenceProfile()`. The production template remained non-production and registered successfully.
- Real CLI from this exact commit returned B1–B5 as `DECISION_INDETERMINATE` and emitted no ManufacturingSpec, correctly preserving the current reference profile's certified uncertainty.

The tracked benchmark report remained at its pre-existing dirty hash `e1f968fe45c29a2d7e78b6cb7cc54acabeef27d331de23de2f4d9d083a297cd0`; it was not staged or attributed to Group 4.

## Necessity and sufficiency

**Necessity — no unnecessary elements:** the change extends the existing profile, frame, permission, mechanics, solver and ManufacturingSpec paths. It adds no package, alternate solver or Group 5–7 implementation. The large profile JSON delta is the deterministic export of the now-complete schema, not a parallel policy source.

**Sufficiency — delivers Group 4 in full:** all four original audit defects—missing population parity, incomplete/unapplied PD-19 authority, axis-hard-coded M02 and locale-dependent identity—are closed, and the required M09/M10 runtime and trace boundary is executable and persisted.
