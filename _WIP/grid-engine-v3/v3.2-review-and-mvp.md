# v3.2 Review & MVP Recommendation — turning the prototype into the product engine

**Author:** s62-kai (Kai) · 2026-08-15 · worktree `s62-grid-engine-v3.2` @ `ae22128d` (pushed)
**Bench:** `localhost:3063/grid-engine` · **For review by:** s62-kai-meta, s62-grid-qa
**Sources compared:** v3.2 as running · Dan's ruled canon (`_WIP/grid-engine-v3/selection-examples/`, `grid-laws.md`) · v3.3 blueprint (`s62-grid-engine-v3.3/_WIP/v3.3/GPT PRO/DOCS/R3/` — 00-contract, 01-compute, 02-logic + full ChatGPT transcript)

---

## 1. What v3.2 is (current state)

The byte-verbatim v1 compute core (7 files, untouched) on the v3.1 scaffold, driven by a
calibrated judge. Three-part separation holds: `compute/` = math only, `spec.ts` = values only
(guarded writers), `logic/judgement.ts` = ranking only. 35/35 tests. All work snapshot-committed.

**The judgement law set** (each rule carries Dan's ruling in a comment, no shape names anywhere):
gravity guard (top ≤28) → vertical hold (bottom ≤40 limb) → strip law (spacing >96 = disconnected
islands) → band count law (B1=1, B2=2, B3/B4 free) → symmetry law (mirror shape → mirror
arrangement; mirror axis = mass axis) → structure law (tapered→triangle · waisted→waist-spanning
corners · narrow standing mass→filled column · full blob→corner square · diagonal→diagonal;
thresholds measured from the 7-shape corpus) → corners class → spread capped at 96 → mass-axis
alignment (3mm steps on symmetric shapes) → tight-before-even (coarse buckets) → fewer → size.
Offer: one optimal per band, bands ≥24mm apart, every band answers, band 4 steps the ladder.

**Canon scorecard (all frames verified on canvas at the serving commit):**

| Shape | B1 | B2 | B3 | B4 |
|---|---|---|---|---|
| bat | single·60 face ✓ | pair-v·88 ✓ | **tri utmost-corners·146 ✓ (ruled canon)** | pair-96·172 |
| duck | head·60 ✓ | pair-v·84 ✓ | rect skip-mid·154 ✓ | pair-96·178 (stepped) |
| butterfly | single·68 dead-centre ✓ | wings pair·92 ✓ | 4-in-wings·126 ✓ | square-96·204 ✓ |
| bot | 44 | pair-v·98 ✓ | narrow rect·144 ✓ exact | pair-96·168 (stepped) |
| pill | 54 | antidiag·82 ✓ | antidiag·120 ✓ | diag chain·172 ✓ |
| poke1/2 | singles ✓ | vertical pairs·76 ✓ | sq-48·126 / pair-96·136 | rect·172 / auto·190 |

Two canon rows diverge on physics, both probed: the 96-square cannot seat below ~214mm (poke)
/ ~170mm (butterfly) under the locked 12mm padding (canon frames were captured at 10mm padding).

## 2. Honest gap spectrum vs the v3.3 blueprint

**Substrate (cannot be tuned in, needs the new kernel):**
- G1 · Continuous feasible-region registration — placement solved exactly as
  `A ∩ ⋂(safeCore − offset_k)` instead of a 2mm sweep. Correctness (provably no missed seat)
  AND the entire performance story: v3.2 solves in 2–20 s; product target is ≤16 ms all-band.
- G2 · Certified conservative approximation — error envelope so a narrow valid seat can never
  silently vanish (we shipped this bug class twice: 5mm outline, 8mm sweep).
- G3 · Tolerance/quantum discipline in the judge — float compares + coarse buckets vs certified
  intervals; knife-edge flips (maxSide 28 vs 29) currently decide silently.

**Judgement quality (retrofittable into v3.2 incrementally):**
- G4 · Region hierarchy + persistence (multi-clearance components) — Dan's "major mass /
  marginal / connector" made computable; replaces template heuristics for multi-lobe shapes.
- G5 · Mass-aware cap moments — integrate the material hanging beyond the anchor hull instead
  of bbox distances (the butterfly-86mm-bottom bug class, solved principled).
- G6 · Constructive canonical parity seat (odd→node-on-centre, even→spacer-on-centre) proposed
  explicitly, not just preferred by buckets.
- G7 · Machine-readable rejection reasons per size/band (bench self-explains escalations).
- G8 · Counterexample + boundary-fixture suite (R3 §13.3: 11 named cases, ±1-quantum fixtures).

**Product plumbing (v3.3 scope):** versioned hashed profiles with provenance · two-stage
ManufacturingSpec (engine spec + fulfilment completion) with artifact hashes · byte-determinism
identity · size/payload budget (engine ≤250KB, ≤16ms).

**What v3.2 has that the blueprint lacks:** the hold-physics coverage oracle (48mm reach,
exact edge intervals — nothing equivalent in R3), strip law, symmetry law, structure classifier,
band separation/stepping — and above all a **working, canon-validated judgement**; R3's whole
mechanics registry is proposed and untested, frozen on a 27-decision ballot.

## 3. /o-necessity — the MVP change (directive: complete v3.2 into a mobile-first, fast,
super-precise, portable engine for the Next.js effects-engine repo, powering studio +
manufacturing spec)

**Directive deliverables enumerated:** (a) mobile-first fast — ms-class solve in Safari/Chrome;
(b) super-precise — no missed/false seats; (c) portable — a package the effects-engine repo can
import; (d) powers the studio — sizes+layouts per band to the UI; (e) powers manufacturing
spec — exact reproducible output object.

**Probe of current state (not documents):** the engine is already dependency-light TypeScript —
`compute/` + `spec.ts` + `logic/` import nothing from React/Next; the only native dep is
Clipper2 (already vendored via v1's `offset.ts`). The judge's cost is 100% the size×template×
2mm-sweep loop (~10⁴–10⁵ engine validations per solve). The bench page is the only Next.js
coupling. Meaning: **portability is nearly free; speed and precision are the same single fix.**

**MINIMAL DIFF — three elements, everything else is drift:**
1. **Replace the sweep with continuous placement (G1, subsumes G2/G3 where it matters).**
   Compute each size's safe-core once (Clipper2 inward offset — already in-repo), intersect
   shifted copies per template = the exact feasible region. Then — per the Meta finding
   2026-08-15, adopting v3.3's certified-restriction lesson at v3.2 cost — the region is NOT
   collapsed to one representative: the judge evaluates a **law-aware bounded deterministic
   candidate set drawn from the feasible region** (each region component's extremal points +
   the mass-axis projection + the canonical projection, in deterministic order), because the
   laws are translation-dependent (gravity, vertical hold, mass-axis) and a region can hold
   both a law-passing seat and a law-failing centre point. Every candidate is exact-revalidated
   via v1's own oracle (`distanceToPreparedContour`) — the sole legality proof, per our law and
   R3 §6.2 — and the judge ranks them exactly as today. Same laws, same canon gate, ~10² fewer
   engine calls → the ms-class, mobile-first target. This is the ONE substrate element of v3.3
   worth pulling forward, and it composes with the byte-verbatim core instead of replacing it.
2. **Package boundary:** lift `compute/ + spec.ts + logic/ + bridge.ts` as `@onemo/grid-engine`
   (pure TS + clipper2 wasm), consumed by the effects-engine repo; the bench page becomes its
   first consumer. No rewrite — the move/clone rule.
3. **ManufacturingSpec output:** serialize what the judge already knows per accepted solution
   (size, anchors mm, layout id, wraps, engine policy signature — v1 already hashes its law set
   as `GRID_ENGINE_POLICY_SIGNATURE`) into one canonical JSON object. The two-stage fulfilment
   completion stays v3.3.

**Justified beyond the baseline:** G7 rejection reasons (tiny; Dan asks "why" on every bench
review — pays for itself immediately). **Deliberately excluded from MVP:** G4/G5/G6 (quality
iterations on the calibration loop, not blockers), profiles/hashing/two-stage spec (production
hardening), backend probe TS-vs-WASM (Clipper2 is already the incumbent; probe only if the
budget fails), any new architecture, any rewrite of the v1 core.

**Necessity verdict:** no unnecessary elements — three changes + one justified extra.
**Sufficiency verdict:** delivers the directive in full — (a) via change 1, (b) via change 1's
exact feasibility + exact revalidation, (c) via change 2, (d) already delivered by the judge +
change 2, (e) via change 3. Not sliced: everything the directive names is covered; v3.3's
remaining scope (regions, moments, profiles, fulfilment chain) is explicitly deferred work for
Dan to schedule, not silently dropped.

## 4. Recommendation — the answer to Dan's "maybe we don't waste time"

**Yes: skip the v3.3 build entirely. v3.2 IS the engine; the v3.3 blueprint is a parts shop,
not a destination.** Building v3.3 from its documents is the exact pattern that failed twice
(both GPT tracks), and its own process is frozen on a 27-decision ballot — while v3.2 runs
today with Dan's laws calibrated and canon-verified. The only thing v3.2 lacks to be the
product engine is the three-element MVP in §3, of which the v3.3 blueprint contributes exactly
one idea (continuous placement). Everything else in the blueprint is either already present in
v3.2 in working form, a quality iteration for the existing calibration loop, or production
plumbing that hardens a shipping engine later. Sequence:
MVP changes 1–3 in this worktree behind the existing canon gate (all 7 shapes must reproduce
before merge) → package into the effects-engine repo → then iterate G4/G5 quality on the same
bench → v3.3's remaining scope becomes incremental hardening of a shipping engine, and its
27-decision ballot shrinks to the handful Dan actually needs to rule (offer rungs PD-08/09,
tolerance PD-38). Stated openly (Meta note): the MVP implicitly decides PD-25/26 — the
coordinate quantum and approximation tolerance land as the implementation's working values
(v1's existing quanta) and go to Dan for ratification with the package, not silently.

## 5. Review asks — THE BUILD, not this document

Dan's question to both reviewers: **is the v3.2 prototype solid enough to be COMPLETED into
the product engine rather than replaced?** Review what was built, in your own checkout:

- **s62-grid-qa — the build, adversarially:** the code (`src/lib/grid-engine/`: verbatim
  `compute/` vs v1 diff-zero claim, `spec.ts` guarded-values discipline, `logic/judgement.ts`
  law implementations vs Dan's rulings, `bridge.ts`, the bench page), the tests (35 claimed
  green — run them), and the results: solve the seven shapes on the running bench (port 3063,
  prove provenance first) and verify the §1 scorecard frame-by-frame against
  `selection-examples/`; attack the two physics-divergence claims (96-square seatability)
  with your own probes.
- **s62-kai-meta — the build, strategically:** does the code's actual structure support the
  "complete, don't replace" verdict — separation quality, whether the judge's laws faithfully
  encode Dan's rulings (each cites its ruling — spot-check against the transcripts/canon),
  what a senior engineer would delete, and whether §3's minimal diff (as amended per your
  finding) is buildable on this codebase without rewrite. Both verdict lines on the BUILD:
  necessity (what should not exist) and sufficiency (what the engine still lacks for Dan's
  directive).
