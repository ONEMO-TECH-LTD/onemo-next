{
  "name": "onemo-magnetic-engine-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs",
    "benchmark": "node scripts/benchmark.mjs",
    "verify": "npm run clean && npm run build && npm run test && npm run benchmark",
    "manifest": "node scripts/manifest.mjs",
    "browser-smoke": "python3 scripts/browser-smoke.py",
    "size-report": "node scripts/size-report.mjs",
    "package": "python3 scripts/package-release.py",
    "release": "npm run clean && npm run build && npm run test && npm run benchmark && npm run size-report && npm run manifest && npm run package",
    "demo": "node scripts/run-demo.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "5.9.3"
  }
}
