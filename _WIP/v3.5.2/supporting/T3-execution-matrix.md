# v3.5.2 T3 execution matrix — reconciled Grid-Meta + Grid-QA

Authority: v3.5.2 master, 948 lines; v3.5.2 T3 packet, 756 lines. Both preserve the operative content approved at `a65added` under the former v3.5.1 names. The rejected 611-line T3 expansion and interrupted finite-mesh proposal are excluded.

Current product: `1ccba648fd66143154d29b4cf6602ff166b467d2`; untracked `safe-topology.test.ts`, `scaling.test.ts`. Read end-to-end: all 10 current `src/lib/magnetic-grid` runtime files / 3,907 lines, plus `magnetic-grid-bridge.ts` / 113, `law.worker.ts` / 148 and `LawPanel.tsx` / 872. No tests or product edits.

| Surface | Exact approved clause | Required product behavior | Current code already delivering it | Smallest missing code | Forbidden changes | Live proof required |
|---|---|---|---|---|---|---|
| Centre | T3:14 freezes the Meta-cleared rule, governor, parity, four placements and 2mm `safeSegments`; T3:24 makes repair last and conditional. T3:49 derives the governed centre from the scaled shape before magnets. T3:108 preserves governor branches and parity canon. | During Wrap/scaling, reproduce the accepted Centre target and four-placement semantics at every evaluated scale. Do not repair the ruler until the completed three-law path proves a material failure. | Live `computeGrid` calls frozen `safeSegments`, `measureCentreBranches`, `centeringAnchors`, unchanged `governMass`, `centrePhaseCandidates`, `measureCentrePlacements` and `chooseCentrePlacement`. It returns centre target/evidence and remains the live Law-tab path. | For scaling only, expose/reuse this complete frozen Centre evaluation at every exact candidate site and carry its result into law evaluation. Do not create a new Centre algorithm. The heart jump proves a scale-evaluation gap exists, but approved T3 does not specify its mechanism; install one bounded contract block before coding it. Finite-mesh event enumeration is the strongest current proposal, not authority. | No edits to `safeSegments`, governor branches, parity/four placements or Centre outputs before the full-system gate; no seat-derived centre; no continuous offset/resultant/RUR/chart stack; no new tie/refusal semantics; no Voting/scoring donor. | After every semantic increment: squircle/72 flap-0 hashes identical; positive-flap governed centre/targets/evidence identical; comparison against frozen Centre-rules across all exposed Centre modes/governors at evaluated sites. Final conditional ruler probe only after Centre+Wrap+scaling runs. |
| Wrap | T3:15 requires the smallest exact belt law; T3:50 requires every perimeter-belt disc within allowance, flap 0 exact tangency and Auto minimum. T3:122 deletes the allowance scan; T3:126 adapts the belt only for live Wrap. | Measure exact worst-belt required allowance over the complete supplied outer+holes boundary; fixed/Auto decisions are typed; truth dots come only from witnesses; refusal shows no layout. | At a fixed numeric size, `contact-root.ts` prepares the complete segment boundary and computes exact per-anchor nearest contact/allowance from the supplied IEEE values; `seat.ts` owns material legality; `identity.ts` certifies witnesses; `logic.evaluateWrap` applies fixed/Auto cap; `engine.computeGrid` invokes it and hides anchors/dots on refusal; worker/UI expose truthful status. This fixed-size path is live and previously QA/Meta-cleared. | Preserve the existing law and fixed-size implementation. Add only the exact scale-parametric adapter needed to construct the same belt measurement/witnesses directly at rational/algebraic rung scales without converting the scale, contour or anchors through `number`; then carry that result into candidates, rungs, inspection and stored worker output. | Do not copy old `maxPressMM`/`pressExcessMM`/tolerance/scan bodies into law; do not alter Centre; no guard/epsilon, approximate verdict, allowance scan, missing holes, number round-trip at an exact rung, second Wrap policy, or recomputed UI dots. | Re-observe current exact flap-0 contact and loose near-miss refusal after scaling integration; fixed allowance and Auto cap/minimum; irrational-rung witness survives engine→worker→cache→UI; holes and every belt anchor; witness-only dots; Centre and fixed-size Wrap hashes unchanged. |
| Scaling | T3:19 requires every new count at its first lawful B1-B4 size without repeats; T3:51 requires exact contact equations, unique/increasing counts and no lower-band repeat; T3:121 deletes/replaces sampled `bandWalk`; T3:681–721 supplies the `solveBands` sequence and reducer ownership; T3:643 makes §7 conditional, not automatic. | Enumerate every verdict-changing site required by the live frozen Centre+Wrap path, evaluate all four parity candidates, accept only simultaneously centred+wrapped candidates, reduce complete B1-B4 results to first-lawful unique increasing counts, preserve ties and predecessor evidence, and return typed refusals. | Dormant primitives only: exact rational/quadratic scale arithmetic; `regimes.ts` has B1-B4 continuous domains, affine Box contact events and parity-boundary events. None are exported by `compute.ts` or called by `engine.ts`, worker or UI, so scaling is not delivered. The live path still uses sampled `bandWalk`/`fitSizeInBand`; no `solveBands`, `inspectFixedSize`, regime partition, candidate reducer, `FirstLawfulCertificate` or all-band result exists. Post-`3849ed1f` offset/resultant/RUR machinery is also dormant and receives no delivery credit. | Implement the narrow live vertical slice: complete only event families proven necessary for the frozen Centre outputs; partition exact sites; evaluate frozen Centre → four placements → seat/belt → existing Wrap; add Logic’s complete B1-B4 reduction/first-lawful ownership; add `solveBands`/typed results; replace the sampled band boundary only when the exact result reaches the worker. | No millimetre walk, bisection, tolerance or monotonicity proof; no fallback “best” rung; no partial Box-only policy while other controls remain exposed; no automatic Support B; no dormant foundation/public types; no engine-owned law predicates/reducer; no editing Centre/Wrap to make scaling easier. | Live v3.5.2 tab exercises B1-B4: every count first-lawful, unique and strictly increasing, no boundary double-owner/cross-band repeat; irrational contact root; all exposed Centre modes/governors, Coverage and MagnetPlan invariants; co-lawful ties; fixed/Auto; predecessor certificate; no sampled production call. |
| Worker | T3:622 keeps the isolated cloned worker through T3; final transport-only worker belongs to T4. T3:639 says selected rung is stored lookup, never re-solve. | During T3 retain the isolated newest-only queue/cache, but band requests return the exact all-band result and clicked stored qualifying layout; free/manual uses typed inspection. Legacy worker/cache remains untouched. | Isolated `law.worker.ts` has correct engine identity, contour identity, newest-only execution, independent caches and live exact Wrap for free/fixed/Auto. It still calls sampled `fitSizeInBand`, prefetches integer sizes and re-runs `computeGrid` for selected diagnostic rungs. | Replace only its band execution/replay/cache payload with live `solveBands` results and pure selected-result lookup; use `inspectFixedSize` for the current manual/free consumer. Retain cloned orchestration until T4. | No T4 bridge-service/transport-only refactor now; no legacy-worker edits; no shared cache; no integer prefetch/diagnostic ladder in production results; no selected-rung re-solve or altered config. | Worker result equals direct engine result; rapid newest-only replacement; cache invalidation by complete contour/config/policy; Law↔Centre↔Voting isolation; selected-rung bytes equal stored qualifying result; current-worker hashes unchanged. |
| UI | T3:16–22 requires truthful progressive copy and live B1-B4 demonstration; T3:727–738 makes manual fixed-size inspection explicit; T3:742–754 defines control truth. | Keep the `v3.5.2` comparison tab. Show frozen Centre, exact Wrap, exact B1-B4 rungs/layouts/refusals and manual concessions truthfully; controls map to the live engine. | `LawPanel` is isolated and live; it shows exact Wrap status/witness dots, Centre controls/evidence, three selectors and an honesty note saying scaling is absent. It labels B1-B4 output diagnostic and hides anchors on Wrap refusal. | Replace diagnostic ladder/range copy and data shape with stored exact rungs/layouts/refusals once `solveBands` is live; wire current manual/free interaction to typed inspection with Centre/Wrap concessions; change the honesty note to all three laws only after live proof. | No law arithmetic/policy in React; no premature “scaling implemented” label; no fourth tab/restyle; no hidden refusal or manufactured winner; no T4 state/view-model rewrite during T3; no phone/meta/audit work. | Provenance-bound real route: all three tabs work; v3.5.2 B1-B4 exact rungs selectable; clicked layout matches stored result; fixed/Auto and manual concessions visible; no clipped controls/errors; screenshots and real interactions at current HEAD. |

Necessity — shrink current delivery by excluding/reverting dormant post-`3849ed1f` continuous-offset, resultant, multivariate/RUR/chart/proof-request stack unless a later completed-path measurement independently invokes its minimum subset.

Sufficiency — partial: Centre and fixed-size Wrap are live; Centre-site mechanism approval, exact-scale Wrap adapter, exact scaling, typed all-band/fixed-inspection results, worker consumption and final T3 UI remain missing. The matrix covers all five required T3 surfaces without treating optional Support B as a phase.

Grid-QA cross-review: accepted this reconciliation without correction. It withdrew its earlier assumption that finite-mesh enumeration was already authorized and confirmed the missing exact-scale Wrap adapter.
