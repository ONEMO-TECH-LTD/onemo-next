# v3.5.2-3 corrections reconciliation ledger — every lane's items, every disposition

Author: s62-kai-lead · 2026-08-23 · master at plan commit `3fdce16b` (197 lines). Dan: "all corrections and versions proposed by each and one of you mutually reviewed by each and considered properly, no consolidation with gaps."

Rule for this ledger: every correction item from every lane since the ruler ruling (`cb1a2704` onward) is listed with its disposition in the master, by section. **Applied** = text or meaning present; **Adapted** = present in different words/shape (difference named); **Rejected** = not in master, with the reason; **Superseded** = withdrawn by its own author or by a later Dan ruling. Items marked **GAP** are open and need the named lane's answer (accept or paste-ready text).

Cross-review required: QA reviews every Meta row and every Lead row; Meta reviews every QA row and every Lead row; each lane confirms or corrects its own rows. A row is closed only when the other lane has signed it.

## A. grid-qa — team corrections for `cb1a2704` (file `/tmp/s62-grid-qa-cb1a2704-team-corrections.md`)

| # | Item | Disposition | Where |
|---|---|---|---|
| A1 | Flap 0 must mean contact; positive air never rounds to 0; delete `roundToRulerMM(nearest)` | **Superseded** — QA withdrew (last corrections: "I accept nearest rounding"); Dan's lock still pending | §1 parenthetical |
| A1b | Squared-distance gate `d² ≥ r²`, `d² ≤ (r+A)²`, integer A 0..cap | **Rejected** — needs no sqrt only if air is never reported; master publishes whole-mm air as the Auto value and witness `airMM`, which needs the distance once. One conversion instead of a 0..cap loop. Same verdicts. | §5.2 |
| A2 | Even size does not make internals integral; replace scaling paragraph | **Applied** | §1 bullet 2, §2.3 |
| A2b | Rename §7.1b to even-size candidate domain; no exact Centre reconstruction | **Applied** (section structure collapsed; wording in §1/§2.1) | §2.1 |
| A3 | Seat and Wrap share a measurement, not a verdict; fixture 12 replacement; diamond 34 refuses flap 0 | **Adapted** — the shared-measurement/opposite-bounds text is applied verbatim; the diamond-34 outcome follows the rounding lock (nearest → lawful at 0, as written; up → refuses) | §2.2, fixture 2 |
| A4 | `wrapIsLawful(number, number)`; no `ExactReal` in the Logic API; worker/UI use only whole-mm fields | **Applied** as `evaluateWrap(measured, policy)` on integers | §5.3 |
| A5 | Wrap may be skipped only for a count already accepted; all placements at first accepted size | **Applied** verbatim | §5.4 cost paragraph |
| A6 | Stale exact-root language removals across packets | **Applied** — packets archived under `canon/_completed/`; master carries no exact-scale language | §10 |
| A6b | "Fixed-size Wrap retains its existing exact measurement evidence" | **Superseded** by Dan's final-state directive (Wrap deslop in this contract) and QA's own v3523 corrections ("Cut AlgebraicReal…") | §3 table |

## B. grid-qa — v3.5.2-3 final contract corrections (file `/tmp/s62-grid-qa-v3523-final-contract-corrections.md`)

| # | Item | Disposition | Where |
|---|---|---|---|
| B1 | Final law block: Centre preserved; all-seated legality may refuse (seat-law correction, not Centre policy); belt-only air; `floor(air+0.5)`; flap 0 = measured air 0, not bit-exact; truth dot = nearest outline point | **Applied** | §2, §3 kept-list, §5.1 `ContactWitness` comment |
| B2 | Scaling: even 24…214, bands, every placement, first lawful size, ties, gravity after equality, no repeat | **Applied** | §2.3, §5.3 |
| B3 | Internals may be fractional; only size, flap, ruler value whole | **Applied** | §1 bullet 2 |
| B4 | Runtime ownership: Compute list (scale once, four placements, numeric seat as prescreen only, one final all-seated measurement, reuse for belt witnesses, convert once, Coverage population-only, MagnetPlan diameters-only) | **Applied** — §5.2 + §6 rows Coverage/Magnet plan | §5.2, §6 |
| B5 | Logic owns only policy; Engine no geometry/rounding/scoring; Worker/UI cache ladder | **Applied** | §5.3, §5.4, §5.5 |
| B6 | "`v3.5.1` tab renamed only as Dan directs" | **GAP (QA/Meta to confirm)** — master names the tab `v3.5.2` (§3, §7.6). Dan has not ruled on the tab name. Proposed fix: §3 kept-list and §7.6 read "the isolated Law tab (currently `v3.5.1`; renamed only as Dan directs)". | §3, §7.6 |
| B7 | Wrap surface: `NearestOutlineWitness { beltAnchorId, outlineElementId, pointMM }`; `WrapMeasurement` with `status` | **Adapted** — `status` union applied (C2). Witness carries `beltAnchorMM`, `outlinePointMM`, `airMM` (coordinates, not ids) because the tab draws the dot from coordinates and `airMM` is what the dot condition reads; no id registry exists to resolve. **QA to confirm the coordinate shape or supply the id consumer.** | §5.1 |
| B8 | `PlacementCandidate` with `wrap` field | **Adapted** — field named `wrapMeasurement` (Meta's patch §2 name); same content | §5.1 |
| B9 | No algebraic/polynomial/root/hash/event-scale in `WrapMeasurement`; witness identity not cryptographic | **Applied** | §3 table, §5.2 `identity.ts` |
| B10 | Implementation sequence 1–5: characterize → one cut-over (seat/Wrap + delete exact) → graduate walk → wire worker/tab → live gate; "Stop. No conditional Centre-repair" | **Adapted** — sequence is W1 W2 S1–S5 F1 F2 (finer rollback commits, same order). **Conflict with Meta:** QA cuts conditional Centre repair; Meta's audit and patch keep it as conditional F2. Master keeps F2 as *check only* (repair = separate amendment). **GAP — QA and Meta to settle in one sentence.** | §8 F2 |
| B11 | Mandatory biting fixtures (10 items) | **Adapted** — covered by §7.1–7.6 except: (a) "Coverage changes output count only; seated population and Wrap belt unchanged" and (b) "MagnetPlan changes diameters only" are control-truth rows (§6), not mandatory fixtures; (c) "a second walk fails the guard" not in §7.4. **GAP — proposed fix:** add to §7.2 "Coverage full/perimeter and MagnetPlan 6/8 leave `seated`, `belt` and every `airMM` unchanged" and to §7.4 "and that `bandWalk` is the only size loop". | §7 |
| B12 | Required cuts: integral-coordinate claim; "contact" → "ruler-zero"; exact APIs; `contact-root.ts` name; conditional Centre repair / visualization deletion / edge-index replacement; stale packets | **Applied** except the Centre-repair cut (B10 GAP); visualization deletion and edge-index replacement are not in the master (Centre unchanged) | §3, §5.2, §10 |

## C. grid-qa — last corrections (file `/tmp/s62-grid-qa-v3523-last-corrections.md`)

| # | Item | Disposition | Where |
|---|---|---|---|
| C1 | Remove the alternative-reading sentence after Dan locks nearest; until then label "freeze candidate" | **Applied** (label); sentence removal staged on Dan's lock | Status line, §1 |
| C2 | `WrapMeasurement`/`WrapEvaluation` unions with null allowance on geometry refusal; §5.3 pass-through; fixture 2 null-evidence clause | **Applied** verbatim | §5.1, §5.3, §7.2 |

## D. grid-qa — deferred post-delivery deslop (`_audits/deferred-post-delivery-engine-deslop-grid-qa.md`)

| # | Item | Disposition | Where |
|---|---|---|---|
| D1 | "Do not simplify Wrap while the engine is being completed; defer" | **Superseded** by Dan (10:31 "final contract must cover final state … deslop of the wrap") and by QA's own B10 sequence | §3 |
| D2 | Keep exact squared comparison at the boundary decision | **Rejected** — the ruler makes a 1e-16 boundary decision invisible; float `d ≥ r` + `pointInMaterial` is the admission (Meta's preferred form, QA's B4 "numeric seat as prescreen only" + "one final measurement"). **QA to confirm it does not stand on D2.** | §5.2 |
| D3 | Dead residue: `parityHolds`, `TANGENT_GUARD_MM`, `maxPressMM`, `contactPointsMM`, `impliedFlapMM` | **Adapted** — all in §3 table except `parityHolds`. **GAP — proposed fix:** add `parityHolds` to the dead-helper row and to the §7.4 identifier list. | §3, §7.4 |
| D4 | `pressExcessMM` live as Centre tie-breaker; do not delete without disposition | **Applied** (not deleted; Centre unchanged) | §3 |
| D5 | Replace 0.001 mm integer seating with shared predicate | **Rejected** for this contract — Meta's item-4 correction: deleting it changes Centre placements (freeze). Stays as prescreen. | §1 bullet 3, §3 net |
| D6 | Rings/Newton only if UI needs them; edge index only if budget | **Rejected** — Centre evidence unchanged in final state; F2 is the only Centre door | §3 |

## E. grid-meta — validation patch (`_proposals/…/s62-grid-meta-v3522-validation-patch.md`)

| # | Item | Disposition | Where |
|---|---|---|---|
| E1 | Scaling paragraph (even sizes, every size, once per count, jumps valid, no repeat, internals fractional) | **Applied** | §2.3, §1 |
| E2 | `PlacementCandidate` with `requiredFlapMM` + `wrapMeasurement` | **Adapted** — `requiredFlapMM` lives inside `wrapMeasurement` (C2 union), not duplicated on the candidate. **Meta to confirm.** | §5.1 |
| E3 | One geometry block; `scaleContour`, `measureCentrePlacements` all four, `measureWrap(prepared, seated, belt, r)`, `roundToRulerMM` single conversion, witness = nearest point not literal contact | **Adapted** — `roundToRulerMM` is not a named export; the conversion is one line inside `measureWrap` (§5.2). Same single site. **Meta to confirm.** | §5.2 |
| E4 | Logic opening rule + reducer steps 3–4 (whole-mm fixed/Auto) | **Applied** | §5.3 |
| E5 | §7.1 boundary primitive = supplied segments; no event scale | **Applied** | §2.2, §5.2 |
| E6 | §7.1b even-millimetre candidate sizes | **Applied** (folded into §1/§2.1) | §1 |
| E7 | §8 control rows (bands, fixed, Auto) | **Applied** | §6 |
| E8 | T3 build wording: Wrap first; live demo of ruler-zero and ≥0.5 refusal before scaling | **Applied** — W before S (§8); the live demo is W1's gate ("live tab observed after each"). | §8 |
| E9 | Sub-plan stale lines (status, performance scope, G1, B2, F1, F2) | **Applied** — sub-plan rewritten to 32 lines | sub-plan |

## F. grid-meta — Centre/Wrap necessity & deslop audit (`_audits/Centre-Wrap-necessity-and-deslop-audit.md`)

| # | Item | Disposition | Where |
|---|---|---|---|
| F1 | Centre keep list; rings/Newton keep while visibly consumed; edge index keep if budget; `TANGENT_GUARD_MM` and float Wrap helpers remove; circle branch remove from Law | **Applied** — circle path and dead helpers deleted; Centre evidence kept unchanged (rings, index untouched) | §3 |
| F2 | Do not rebuild Centre; retain byte-for-byte unless the complete engine shows a material change | **Applied** as F2 | §8 F2 |
| F3 | Wrap keep list (outer+holes, all-seated legality, belt-only, witnesses incl. co-nearest, typed refusals, one shared result) | **Applied** — **except "co-nearest boundary elements where the UI truth dots require them"**: master keeps one nearest point per belt disc. **GAP — Meta to confirm one point per disc suffices for the dots, or supply the co-nearest clause.** | §5.2 |
| F4 | Remove exact layer, certificates, SHA-256, event-scale language, sub-mm in Logic/UI; rename to `wrap-measurement.ts` | **Applied** | §3, §5.2 |
| F5 | Collapse duplicate legality: numeric prescreen stays; one final all-seated measurement; refuse on any illegal seat; convert once; Logic compares integers; final seat uses raw float sign, no quantum | **Applied** | §5.2 |
| F6 | Scaling keep/forbid lists | **Applied** | §2.3, §9 |
| F7 | Minimal sequence 1–6 | **Applied** as W1 W2 S1–S5 F1 F2 | §8 |

## G. grid-meta — messages (second full-read patch, item-4 correction, final four @d2f7faff)

| # | Item | Disposition | Where |
|---|---|---|---|
| G1 | §5.5 T4 must be final, not optional | **Applied** — worker/tab ownership declared final; T4 withdrawn | §5.5 |
| G2 | Remove `circle?` and `makeCircleSeatPredicate` | **Applied** | §3, S1 |
| G3 | Rename `contact-root.ts` → `wrap-measurement.ts`; import law/W1 updated | **Applied** | §4, §8 W2 |
| G4 | Remove "nanometre-accurate" and "no quanta anywhere"; then (correction) keep the 0.001 kernel; "no sub-mm value or NEW quantum enters Law policy" | **Applied** | §1 bullet 3, §9 |
| G5 | Duplicate ```ts fence | **Applied** | §5.4 |
| G6 | Truth dots = nearest-outline ruler-zero witnesses | **Applied** | §3, §5.1 |
| G7 | Final four: Centre honesty (circle preset only, disposition fixture); quantum wording; `seat.ts` in W1; necessity line | **Applied** verbatim | §7.1, §1, §8 W1, §11 |

## H. s62-kai-lead — own items (for the peers to review)

| # | Item | Disposition | Where |
|---|---|---|---|
| H1 | Nearest rounding as the reading of "no measurement is less than 1 mm"; alternative "up" held for Dan | Applied; Dan's lock pending | §1 |
| H2 | Keep the 0.001 integer kernel (Centre freeze) after initially deleting it | Applied (Meta G4 correction) | §1, §3 |
| H3 | Conversion inside `measureWrap`, not a separate `roundToRulerMM` export | Applied (E3 adapted) | §5.2 |
| H4 | `Rung { band, sizeMM, magnetCount, layouts[] }`, `BandLadder`, `RefusalCode` | Applied; not reviewed line-by-line by either peer — **peers to review** | §5.1 |
| H5 | Fixture 5 performance "< 2 s per band on the squircle and one real cutout" | Applied; number is my estimate from the float-walk measurement, not a Dan requirement — **peers to confirm or cut** | §7.5 |
| H6 | Net line counts (−650 / +130) and per-step sizes | Estimates, not authority — **peers to confirm or delete the column** | §3, §8 |

## Open GAPs (need a lane's word — accept, or paste-ready text)

- **B6** tab name (`v3.5.2` vs "renamed only as Dan directs") — QA and Meta.
- **B7** witness shape: coordinates+airMM (master) vs ids (QA) — QA.
- **B10** F2 conditional Centre check: keep (Meta) vs cut (QA) — both, one sentence.
- **B11** Coverage/MagnetPlan invariance as a mandatory fixture; "only one size loop" in the guard — QA and Meta.
- **D2** exact squared comparison at the boundary: confirm withdrawn — QA.
- **D3** `parityHolds` added to the dead-helper row — QA and Meta.
- **E2/E3** `requiredFlapMM` inside the union; conversion inline in `measureWrap` — Meta.
- **F3** co-nearest witnesses for the dots — Meta.
- **H4–H6** Lead's own unreviewed items — both.
