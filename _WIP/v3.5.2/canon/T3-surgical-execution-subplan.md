# v3.5.2 T3 surgical execution sub-plan

Status: final execution authority — R0 + G1 QA/Meta CLEAR. This revision releases B1 only; B2 remains gated on B1 CLEAR.

## Authority

Execute only from:

1. `v3.5.2-master-contract.md` — 948 lines — SHA-256 `73d4e29e87e385ca5ff35b1ba635b4b1f38f8abdc4a3d2e6ae9bac8601dc611e`; operative content approved at `a65added` under the former v3.5.1 name.
2. `T3-build-three-laws.md` — 756 lines — SHA-256 `2622b9f932a84d5c7b986c2430afd12d1385a3327117d4ae89681e782fd26db5`; operative content approved at `a65added` under the former v3.5.1 name.
3. `../supporting/T3-execution-matrix.md` — SHA-256 `f3bde325b18f1c2957d2b211b3bdc25222a7309d0aa71cc6b197f329b11a06f7`.
4. `../supporting/T3-post-wrap-commit-audit.md` — 50 lines — SHA-256 `45c0d3f59c1ae8faafd060492c1cdd53f3798ec42d8cf0ab0c3716d2cbb6acab` — joint 34-commit recovery disposition.

Voting, Centre-rules, their worker/modules and the Meta-cleared fixed-size Wrap behavior remain frozen comparators.

## Operating contract

- One builder owns product edits. Grid-Meta continuously watches the builder pane and actual diff; Grid-QA independently reviews each named gate.
- Before each edit block, the builder states only: sub-plan step, controlling contract lines, allowed files and intended semantic result. It does not design in code.
- Every semantic result is a local rollback commit. No amend, rebase, reset, push or product deletion outside the named recovery disposition.
- An off-step file, unapproved mechanism, public stub, fallback, number-based law decision or dormant platform is an immediate hard stop.
- A genuine contract silence produces one bounded proposed block. Grid-Meta and Grid-QA must both clear necessity and sufficiency before that block enters the contract or code.
- Compile/focused tests and the frozen Centre/Wrap replay run after every semantic commit. The live Law tab is observed after every user-visible or worker/engine integration commit.
- No T4 bridge/transport/UI ownership migration, phone work, performance programme, broad audit or Centre repair occurs in this sub-plan before its named gate.

## R0 — Recover the branch surgically

Goal: remove unauthorized machinery while preserving every independently necessary exact-scaling input and all history.

### R0.1 — Remove continuous Centre-repair machinery

Allowed files:

- `src/lib/magnetic-grid/compute/centre-evidence.ts`
- `src/lib/magnetic-grid/__tests__/offset-features.test.ts`
- `src/lib/magnetic-grid/__tests__/exact-centre-scaling.test.ts`
- `src/lib/magnetic-grid/__tests__/separation.test.ts`

Action:

- Remove every body/type/test/allowlist entry introduced by `862b71e4..fac33358`: offset features, line/curve intersections, expression graph, arrangements, legal-edge filtering and canonical offset-ring identity.
- Preserve the frozen `safeSegments`, `centroidOf`, `measureCentreBranches` and other pre-offset live Centre code.
- Remove the consumerless hole-aware exact Weight coefficient body and the focused exact-centre-scaling tests. Retain exact Box through R0.1 only because the still-present `c1132856` contact-event enumerator consumes it; R0.2 deletes exact Box atomically with that consumer. B1 reintroduces exact Box plus frozen full-outer/no-holes Weight with their first live evaluator.
- Delete `offset-features.test.ts` only after the tracked import/reference trace proves it belongs entirely to the reverted stack.

Proof: frozen fixed-size Centre/Wrap hashes equal; no offset symbol remains; package separation still passes.

Commit: `revert(grid): remove unauthorized continuous centre stack`.

### R0.2 — Remove the generic proof platform

Allowed files:

- `src/lib/magnetic-grid/spec.ts`
- `src/lib/magnetic-grid/compute/centre-evidence.ts`
- `src/lib/magnetic-grid/compute/exact-real.ts`
- `src/lib/magnetic-grid/compute/identity.ts`
- `src/lib/magnetic-grid/compute/regimes.ts`
- `src/lib/magnetic-grid/__tests__/exact-real.test.ts`
- `src/lib/magnetic-grid/__tests__/identity.test.ts`
- `src/lib/magnetic-grid/__tests__/regimes.test.ts`
- `src/lib/magnetic-grid/__tests__/separation.test.ts`

Action:

- Remove all general predicate, multivariate token, resultant, generator factorization, RUR/tuple, back-substitution and predicate-request bodies/types/tests introduced by `e77bfcdf..5c9a6f40` and `1ccba648`.
- Remove the consumerless `c1132856` contact-event enumerator, its exact Box coefficient/re-export, focused tests and allowlist entries. B1 recovers its audited segment equations/lattice enumeration and exact Box only with exact projection proof and the first live consumer.
- Remove all `40d90c53` changes, including its consumerless runtime typing and offset-test coupling. B1 recovers only the audited `exact-real.ts`/`regimes.ts` contact-root typing and tuple fixes with the live event path.
- Preserve the audited KEEP kernel: exact rational/quadratic roots, affine transform, uncapped comparison, parity events, half-open band domains and B1-B4 horizon.
- Remove the consumerless `CertifiedExpressionReal` scale-expression functions/types/tests from `3849ed1f+c1911cdf+a98bb671`. B1 may recover only their audited minimum in the same commit that the live exact-scale Wrap adapter consumes it.

Proof: no removed symbol has a runtime reference; fixed Centre/Wrap hashes equal; exact root/domain/parity focused oracles remain green.

Commit: `revert(grid): remove dormant symbolic proof platform`.

R0 gate: Grid-Meta and Grid-QA independently verify the resulting tree against the joint audit. No body classified REWORK remains as a standalone foundation; correct portions enter only with their first B1 live consumer. The two untracked RED scaling/topology fixtures remain preserved and unadopted until their owning step.

## G1 — Close the one Centre-site contract gap

No product code.

Required contract block:

- Define the minimum exact event/evaluation mechanism that makes the frozen 2mm/800-point Centre behavior complete over scale without changing its outputs.
- Preserve full-outer Box and Weight; preserve donor decimation, bbox anchoring, row-major sample/component order, strict equality winner, governor branches, parity and four placements for Core/Deep/Masses/Top.
- Enumerate only verdict-changing transitions necessary to prove first-lawful scaling.
- Forbid continuous offsets, new Centre ties/refusals, hole-aware Weight, generic resultants/RUR/chart machinery, mesh-constant changes and seated-magnet-derived centres.
- Return typed unresolved if the admitted exact mechanism cannot certify a site; never approximate.
- Name falsifiers for mesh-dimension rounding, sample sign/clearance, component/mass transition, deepest/governor identity and the measured heart jump.

Gate: Grid-Meta necessity+sufficiency CLEAR, then Grid-QA independent CLEAR, on one immutable contract revision. Any rejection names one contradiction and the smallest complete replacement. Only the cleared block becomes authority.

### G1 candidate contract block — frozen Centre sites over scale

This block is review material until both G1 gates are CLEAR. It closes only the scale-site gap for the frozen Centre implementation; it does not repair Centre or authorize product code before those gates.

When this block clears both G1 gates, it narrowly supersedes through F2 only the master/original T3 clauses that forbid mesh/decimation or require exact continuous-offset Centre evidence for Core, Deep, Masses and Top. Box and Weight remain full-outer. The complete supplied boundary remains mandatory for seat, parity, Wrap and contact. Every continuous offset, arrangement, integral, medial-axis, generic resultant, RUR/tuple or chart-based Centre mechanism remains forbidden. No other master/original T3 clause is superseded.

#### Frozen source model

- Box is the midpoint of the scaled full-outer bbox. Weight is the frozen `centroidOf(outer)` result over the full outer ring; holes remain ignored. Neither uses `safeSegments`.
- Core, Deep, Masses and Top use only the frozen `safeSegments` model. Let `k = max(1, ceil(outer.length / 800))`; the donor ring is `outer[0], outer[k], ...` in that order. Scaling never reorders or resamples it.
- At exact scale `s`, construct the donor bbox, `x0 = minX(s) - 2`, `y0 = minY(s) - 2`, `nx = max(2, round((maxX(s)-minX(s))/2)+3)` and the equivalent `ny`, with JavaScript `Math.round` boundary ownership reproduced exactly. Mesh sample `(ix,iy)` is `[x0+2ix,y0+2iy]` and its identity is the donor row-major index `iy*nx+ix` at that mesh dimension.
- For each sample, reproduce the donor signed clearance against the decimated scaled ring: exact inside/outside winding; exact nearest supplied-segment projection class and distance; inside gives `distance-SPOT_RADIUS_MM`, outside gives `-(distance+SPOT_RADIUS_MM)`. Legal activity is `signed >= 0`; depth-mass activity is `signed >= max(0, MASS_DEPTH_MM-SPOT_RADIUS_MM)`.
- Reproduce donor traversal, not a cleaned-up equivalent: row-major component seeds; the neighbour list `[i-1,i+1,i-nx,i+nx]` with the donor row-wrap rejection; LIFO stack visitation; deepest sample replaced only by strict `>`; component and mass arrays in discovery order; masses sorted by strict ascending area with stable equality order; segment output sorted the same way.
- Attach each depth mass to the legal component containing that mass's deepest row-major sample, exactly as `iso0.comp[m.deepIdx]`. When a legal component has no depth mass, expose that component itself as its fallback mass. With no legal component, reproduce the frozen Box fallbacks for Core, Deep, Masses and Top.
- On a fixed site interval, active samples, component membership, mass membership, nearest projection class and every strict winner identity are constant. Compute exact scale-parametric branch values for donor sample coordinates, component counts/means/bboxes, Core, Deep, mass centres/areas/peak clearance and Top. Logic then applies the unchanged governor branches—including bbox-midline `top-small` and its topmost fallback—plus the unchanged parity canon and four placements. Equal comparisons retain the donor incumbent; they do not create a new tie or refusal.
- Because the contour, bbox-anchored samples and segment endpoints are affine in positive scale, every admitted winding/projection boundary is linear after cancelling positive scale factors, and every squared clearance/nearest equality is at most quadratic. A deepest/governor equality is admitted only when its exact comparison reduces to the same retained linear/quadratic domain, including equal-offset peak comparisons; a mixed fallback/depth offset or any other comparison outside that domain is unresolved. Roots outside their exact projection/winding branch are rejected. No broader solver is invoked.
- A final frozen-mesh `peakClear` value at an irrational site may require the audited scale-coupled `CertifiedExpressionReal` subset solely to publish the scalar form `sqrt(q(s)) + c`. Its canonical identity includes the normalized quadratic radicand branch, ruled constant, exact scale identity and selected nonnegative square-root branch; directed rational bounds and replay validation travel with every stored/transported value. This representation never discovers an event or decides a Centre policy comparison: root discovery and strict governor ordering use the exact squared linear/quadratic predicates above. If the bounded publisher or replay cannot certify the scalar, the site is `CENTRE_EVIDENCE_UNRESOLVED`.

#### Complete Centre transition set

For each B1-B4 exact band domain, Compute enumerates only roots that can change the frozen Centre outputs or the branch formula consumed by scaling:

1. `MESH_DIMENSION` (`SAFE_TOPOLOGY` evidence subkind): scaled bbox spans cross a donor `Math.round` half-step, changing `nx` or `ny`.
2. `SAMPLE_CLASS` (`SAFE_TOPOLOGY` evidence subkind): for every live row-major sample and decimated ring segment, point-in-ring winding crossings, projection-class changes, nearest-segment identity equalities and signed-clearance crossings at the legal and depth thresholds.
3. `COMPONENT_MASS` (`SAFE_TOPOLOGY` evidence subkind): at a `SAMPLE_CLASS` root, a sample activation/deactivation changes four-neighbour legal/depth connectivity, component discovery, depth-mass ownership or the ordered component/mass record. This is a derived transition disposition, not a second root family.
4. `DEEPEST_GOVERNOR` (`CENTRE_IDENTITY` evidence subkind): exact equalities that can change a component deepest sample, peak-clear winner, top mass, smallest-area mass, upper-half membership or `top-small` fallback under the donor's strict comparisons and stable order.

The policy-independent ordered union of these roots and the existing exact seat/parity/binding/contact roots defines boundary sites plus open intervals. Equal exact scales are merged and every coincident transition is applied atomically before the boundary is evaluated. At a boundary, reproduce the donor rules exactly: threshold membership uses `>=`; upper-half membership uses `>=`; deepest, smallest and top replacement use strict `>`, `<` and `>` respectively, so equality keeps the incumbent in canonical donor order. On an open interval, emit one exact branch record only after proving every listed predicate and winner identity constant there. Each event proof carries its exact scale, transition kind, donor sample/segment/component/mass identities, the exact equality that creates the root and the exact sign/order disposition on both adjacent intervals. The branch record carries all six frozen mode evidence branches and the neutral exact comparison/order certificates needed by all four Masses governors; Compute never receives or applies the selected Centre policy. Logic alone applies the unchanged governor branches. The record also carries the exact branch expressions needed by contact equations and the predecessor evidence. It never substitutes a sampled millimetre size for the interval.

The implementation may use the retained rational/quadratic root, affine-transform and uncapped-comparison kernel. If any required predicate cannot be reduced, isolated, ordered or replayed in that admitted mechanism, the whole affected Centre site returns `CENTRE_EVIDENCE_UNRESOLVED`; no partial site, approximate centre or fallback winner is emitted.

#### Explicit exclusions

This mechanism does not authorize continuous inward offsets, offset arrangements, region integrals, medial axes, a general expression graph, generic multivariate resultants, RUR/tuple or chart machinery, a general predicate-proof platform, mesh grain/800-point changes, hole-aware Weight, seated-magnet-derived centres, new Centre ties/refusals, altered governor comparisons, altered parity or altered four-placement ordering. The bounded `sqrt(q(s))+c` evidence publisher above is the only Centre-expression exception. The mechanism exists only to evaluate the frozen donor Centre model completely at the exact sites required by first-lawful scaling.

#### Required falsifiers

1. **Mesh rounding:** one bbox span crosses an exact half-step; the boundary owns the same `nx/ny` as donor `Math.round`, and the adjacent intervals expose the correct row-major sample identities.
2. **Sample sign/clearance:** a non-owning segment changes projection class and a sample crosses each legal/depth threshold; deleting either root changes the active-set certificate and fails.
3. **Component/mass transition:** one activation joins/splits four-neighbour legal components and separately changes depth-mass ownership; discovery order, means, areas and stable mass order match direct frozen evaluation on both sides and at the boundary.
4. **Deepest/governor identity:** two samples/masses cross for deepest, top, smallest and upper-half `top-small`; strict equality retains the earlier donor incumbent, and mutating event enumeration away from the canonical donor identities cannot manufacture a different winner.
5. **Measured heart jump:** the known heart contour contains the exact transition responsible for the frozen mesh centre jump; sites immediately before, at and after reproduce direct frozen Core/Deep/Masses/Top evidence and the same governor/parity/four-placement outcome. Removing that transition must make the first-lawful predecessor proof fail.

Necessity — no unnecessary elements: the block models only the already-shipping frozen 2mm/800-point Centre computations and only transitions that can change their scaling verdicts.

Sufficiency — delivers the G1 gap in full: every frozen Centre mode has an exact branch at every scaling site, donor ordering and equality behavior are preserved, all uncertifiable cases fail typed, and no continuous Centre-repair platform is introduced.

## B1 — Build the complete exact engine vertical slice

This is one semantic delivery even if implemented through multiple rollback commits. No public partial engine, stub or UI claim exists between commits; each intermediate private body must be consumed by the immediately following commit inside B1.

Allowed runtime files:

- `src/lib/magnetic-grid/spec.ts`
- `src/lib/magnetic-grid/compute.ts`
- `src/lib/magnetic-grid/compute/{exact-real,seat,centre-evidence,regimes,contact-root,identity}.ts`
- `src/lib/magnetic-grid/logic.ts`
- `src/lib/magnetic-grid/engine.ts`
- focused magnetic-grid tests and existing separation guard

Required result:

1. `spec.ts` contains only the final live T3 records required by `solveBands` and `inspectFixedSize`; no dormant proof platform or duplicate contract owner.
2. Compute prepares the supplied segment boundary once and enumerates the complete exact B1-B4 sites required by the approved G1 mechanism plus seat-count, parity, binding and contact multiplicity. Recover the audited `c1132856` segment equations/lattice enumeration and the runtime-only `40d90c53` contact-root typing here with their first live consumer. Reintroduce exact Box and frozen Weight here with their first live evaluator: Box reproduces full-outer bbox centre; Weight reproduces `centroidOf(outer)` with holes ignored. Every endpoint/interior contact-event root proves its labelled projection class before admission.
3. At each interval/boundary site, Compute evaluates the frozen Centre evidence, all four parity placements, exact seat/belt population and complete-boundary Wrap measurements neutrally.
4. Recover the audited `CertifiedExpressionReal` subset from `3849ed1f+c1911cdf+a98bb671` only with its first live consumer: G1's bounded frozen-mesh `sqrt(q(s))+c` scalar evidence and/or the exact-scale Wrap adapter. Wire creation plus every replay validation in that same semantic commit; otherwise it remains deleted. Exact-scale Wrap uses the existing fixed-size law but constructs boundary points, anchors, allowances, tangencies and witnesses directly from Rational/Algebraic scale values. No scale/point/contour round-trips through `number`.
5. Logic alone applies Centre policy, parity, Coverage, fixed/Auto Wrap, vertical elimination, co-lawful ties, first-lawful count ownership, cross-band no-repeat and `RUNG_CONFLICT`.
6. `engine.solveBands` executes the approved §7.4 sequence and assembles results without inspecting law flags/refusal codes. `inspectFixedSize` returns measured concessions and never inserts a candidate into a ladder.
7. Sampled `bandWalk`/bisection remains only until the new result is ready for worker cut-over; it is deleted from the production path in B2, not used as proof here.

Required proof:

- frozen Centre/Wrap zero- and positive-flap replay;
- exact irrational diamond rung and witness without number conversion;
- B1-B4 complete first-lawful predecessor evidence;
- every exposed Centre mode and four Masses governors;
- fixed/Auto, Full/Perimeter and MagnetPlan invariants;
- all co-lawful layouts retained; no stable-order winner;
- direct `solveBands` and `inspectFixedSize` typed results;
- mutation of any verdict-changing event family fails.

B1 gate: builder self-audit, Grid-QA independent source/runtime QA, then Grid-Meta necessity/sufficiency/deslop/live-product Meta audit. Continue only on both independent verdicts CLEAR.

## B2 — Atomic exact engine → worker → UI cut-over

Allowed files:

- `src/app/(dev)/effect-creator/grid-origin/law.worker.ts`
- `src/app/(dev)/effect-creator/grid-origin/LawPanel.tsx`
- `src/lib/effect/magnetic-grid-bridge.ts`
- `src/lib/magnetic-grid/engine.ts`
- `src/lib/magnetic-grid/spec.ts`
- worker/UI/bridge-focused tests and the existing separation guard only

Action:

- Preserve the isolated newest-only queue/lifecycle and engine/contour identity checks.
- Add only the bridge-owned, lossless current `Contour` → approved exact `NormalizedBoundary`/`BoundaryTruth` adapter required by `solveBands` and `inspectFixedSize`. The worker calls this adapter; exact scaling remains in Compute. Do not add the T4 solve service or view-model architecture.
- Replace sampled `bandFit`, per-mm caches/prefetch and clicked-rung recompute with one complete-config `solveBands` cache and pure stored band/rung/layout lookup.
- Route free/manual interaction through `inspectFixedSize`.
- Transport exact scales, witnesses, refusals and centre evidence unchanged.
- Bind existing B1-B4 controls/rung chips to stored `solveBands` results.
- Render every co-lawful layout and typed refusal without choosing a hidden winner.
- Display rounded exact scale only as report text; retain the exact value in state/payload.
- Render truth dots only from stored witnesses. Show manual Centre/Wrap concessions from `inspectFixedSize`; manual results never enter ladders.
- In this same semantic cut-over, delete `snapRange`, `bandSnapPoints`, `bandWalk`, `fitSizeInBand`, `autoFlapInBand`, their exports, orphan imports and law-only sampled constants from engine/spec; delete the matching worker prefetch/cache and LawPanel snap-step state/control/message/copy.
- Change the honesty note to all three laws only after the committed live result renders.
- Keep exactly three tabs and the existing surface. Do not convert the worker/UI to T4 ownership or restyle the panel.

Proof: worker result byte-equals direct engine; changing shape/config invalidates once; band/rung selection causes no solve; stale requests never render; legacy worker hashes unchanged; Law→Centre-rules→Voting→Law remains isolated. On the provenance-bound real route, all three labels remain unclipped; every B1-B4 band renders stored exact rungs/refusals; irrational rung and ties are visible; fixed/Auto/manual concessions work; no sampled Law symbol/call remains; frozen Centre/Wrap visuals are unchanged.

Commit: `feat(grid): cut v3.5.2 onto exact three-law results`.

## F1 — Completed-system gate

Run Centre + Wrap + scaling together on the real Law tab across:

- B1-B4;
- all six Centre modes and four Masses governors;
- fixed flap 0, fixed positive and Auto cap pass/refusal;
- Full/Perimeter and all MagnetPlans;
- square, diamond, heart and real supplied concave/holed cutouts;
- manual fixed inspection;
- direct engine, worker/cache replay and selected-layout lookup.

Required verdict: unique increasing first-lawful counts, no cross-band repeat/double owner, exact witnesses, explicit refusals/ties, no sampled production path, comparator hashes unchanged, real UI truthful.

## F2 — Centre repair remains last and conditional

Only after F1 passes, run the named 2mm-ruler residue/sliver cases against actual law results.

- If no required rung/verdict changes materially, record evidence and leave Centre untouched.
- If a material product result changes, open one bounded Centre-repair increment under the master §7.1b reference. It is not part of B1-B2 and cannot retroactively justify the reverted platform.

## Final T3 closure

Builder runs a complete self-audit. Grid-QA performs independent source/runtime QA. Grid-Meta performs independent necessity, sufficiency, deslop and live-product Meta review. T3 closes only when both verdicts are CLEAR on one clean immutable HEAD. No T4 work begins before that closure.

## Necessity and sufficiency

Necessity — the plan removes the unauthorized platform first, retains only audited exact inputs, closes one proven contract gap, and builds one live vertical engine→worker→UI path. No parallel engine, speculative Support B, duplicate Wrap or premature Centre repair exists.

Sufficiency — the plan delivers frozen Centre, exact fixed/Auto Wrap at algebraic rung scales, exact first-lawful B1-B4 scaling, typed direct/worker/manual results, stored lookup, truthful UI, completed-system proof and conditional Centre repair.
