# v3.5.2-1 — simplify and complete

Author and proposal owner: **s62-grid-qa (Codex)**

Status: proposal only. It does not amend canon or authorize product edits.

## Audit base

Verified sources:

- Dan's flap/contact ruling: Lead transcript, 19 August, 18:32 onward.
- Dan's three-law directive: Lead transcript, 20 August, 10:31 onward.
- Exact solve-not-search correction: contract commit `3c18f6ee` and Grid-QA transcript, 20 August, 14:39 onward.
- Exact Wrap implementation: QA+Meta CLEAR at product commit `2c043257`.
- Drift record: `3c18f6ee..8d17780c`, where exact accepted contact became exhaustive continuous-domain proof.
- Current canon and surgical sub-plan, plus `_context/briefs&decisions.md`.

Audit conclusion:

- Centre is built and must stay frozen.
- Wrap is built, exact and cleared; do not rebuild it.
- Scaling is missing.
- Scaling became blocked because the contract demands proof of every possible continuous Centre/topology regime. Dan required exact accepted rungs, not a universal geometry-proof platform.

## Minimal contract correction

Keep unchanged:

- the three-law goal;
- exact supplied-boundary Wrap;
- Rational/Algebraic contact scales and `ContactWitness`;
- Fixed/Auto worst-belt law;
- one rung per magnet count, B1-B4 ownership and no cross-band repeats;
- frozen Centre/governor/parity/four placements;
- worker/UI isolation and the `v3.5.2` tab.

Remove from required scaling authority:

- exact inward-offset Centre reconstruction;
- universal `SAFE_TOPOLOGY`/`CENTRE_IDENTITY` regime enumeration;
- medial-axis, integral, resultant, RUR and general certificate platforms;
- exact reproduction of every 2mm-mesh transition;
- proof that every earlier real-valued candidate in the continuum was absent;
- `FirstLawfulCertificate` fields that exist only to carry that universal proof.

Replace those clauses with:

> The existing band walk discovers candidate counts and layouts in increasing size order. It never certifies contact. For each newly discovered count/layout, Compute solves the exact supplied-segment contact equation, reruns the frozen Centre/parity and cleared Wrap laws at that exact scale, and accepts only a stable centred-and-wrapped result carrying its exact witnesses. One count is emitted once across B1-B4. Approximate size, scan quantum and report decimals never decide acceptance.

## Smallest complete build

1. **Candidate discovery** — retain the existing band walk privately. Remove only its authority to decide Wrap or publish a rung.
2. **Exact scale adapter** — use the audited rational/quadratic root kernel to scale supplied segment coordinates and anchors without a `number` round-trip.
3. **Exact validation** — run the cleared Wrap measurement at the solved scale. Preserve all co-binding witnesses and every co-lawful placement.
4. **Stability check** — rerun frozen Centre/parity at the root. If the candidate changes, solve the resulting candidate; accept only when the same count/layout remains lawful. Reject a detected cycle instead of inventing a winner.
5. **Ladder reduction** — first accepted exact rung per count; strictly increasing unique counts; suppress any count already owned by a lower band.
6. **Auto** — reuse the cleared exact worst-belt minimum and cap refusal.
7. **Cut-over** — worker caches one complete solve; UI selects stored results and displays rounded scale while retaining exact scale/witness data.

No public stub, new solver platform, Centre repair, T4 rearchitecture or parallel result schema is permitted.

## Biting proof

- square 24mm: flap-0 exact witness; loose 24.1mm near-miss refuses;
- irrational diamond contact scale: exact witness survives engine → worker → UI without number conversion;
- B1-B4: counts unique and increasing; no lower-band repeat;
- every accepted rung reruns frozen Centre and exact Wrap at its stored scale;
- higher-count Wrap failure does not delete a lawful lower count;
- two co-lawful placements remain two results;
- fixed and Auto use the same exact required allowance;
- real square, circle and supplied cutout comparison against a denser discovery probe finds no missing count.

The denser probe is validation only. It does not become production architecture. If it exposes a real missed count, fix that measured discovery seam only and add its fixture.

## Execution order

1. Amend master/T3/sub-plan to the bounded mechanism above and delete the superseded proof programme.
2. Independent QA + Meta verify necessity, sufficiency and absence of contradictory old authority.
3. Recover only the retained exact affine/root/order bodies with their first live scaling consumer.
4. Complete engine scaling and direct fixtures.
5. QA + Meta clear scaling.
6. Perform the existing worker/UI cut-over and live three-law gate.

## Stop conditions

Stop before adding code when:

- a helper does not serve the current live rung path;
- a test asks for universal proof rather than a rendered product result;
- Centre or Wrap bytes/behavior move;
- a new certificate/type exists only to satisfy another certificate/type;
- a hypothetical shape, root or regime is used to justify architecture without a failing product fixture.

Necessity: shrink the continuous proof programme; retain only discovery, exact contact construction, exact validation and count ownership.

Sufficiency: completes Centre + Wrap + Magnet-quantity scaling, worker delivery and the live v3.5.2 surface without weakening flap exactness.
