# v3.5.2 simplest complete way forward — Grid-Meta final joint recommendation

## Decision

Do not restore the original 99-line proposal and do not preserve the current scaling contract. Surgically simplify the current master/T3 authority around the last cleared Centre + Wrap product state.

Keep the existing Centre choices and exact Wrap law. Add only a local exact coordinate adapter for the layout Centre already selected, then use the existing walk to discover candidate states and solve their finite contact roots. No exact Centre rebuild, global event system, recursive stability, algebraic contour, or T4 migration.

## Why the fourth pass changed the representation decision

The proposed post-scale 0.001 mm geometry is internally inconsistent with continuous scaling:

- it removes the two measured seat-versus-Wrap disagreements by changing the judged geometry;
- but it turns geometry into step functions of scale;
- and it leaves the analytic diamond contact about 0.000309 mm loose at its true irrational root.

Quantizing the normalized source also failed: the measured squircle changed from four anchors to one and still refused. The supplied IEEE coordinates are the required truth; the current defect is only that approximate seat and exact Wrap judge them differently.

The governing boundary rule already settles the alternative: the supplied final contour bytes are the truth, converted to exact IEEE rationals without rounding or reinterpretation. Therefore no global geometry quantum is adopted and no Dan product ruling is required. A nominally symmetric preset whose supplied flattened bytes are microscopically asymmetric may correctly refuse at one requested fixed size; scaling finds the actual contact size for those bytes.

## Minimal diff

1. Delete from authority: exact Centre reconstruction, Support B/G1, global regimes/events, resultants/RUR/certificate platforms, recursive stability, `FirstLawfulCertificate`, algebraic contour materialization, the ban on adapting `bandWalk`, T4 migration, B5, and seat-based `below` ownership.
2. Preserve unchanged: all six Centre modes, every Mass governor, the 2 mm mesh and its topology/branch selection, parity, four placements, exact fixed/Auto Wrap, supplied-segment witnesses, isolated worker/tab, B1-B4.
3. Add one private exact coordinate adapter consumed directly by fixed inspection, near-boundary seat legality, Wrap, and scaling contact construction.
4. Adapt the existing walk into cheap state discovery; solve and validate only candidate-local roots.
5. Extend the existing result/worker/tab only with the final B1-B4 ladder, exact stored scale/witness, gravity, and truthful display.

## Exact coordinate adapter — bounded, not an exact Centre engine

Numeric Centre remains authoritative for choosing the branch, component, governor, parity, placement, lattice indices, and belt membership.

After that choice, reconstruct only the chosen coordinates from their existing source identities:

- Box: rational bounding-box midpoint;
- Weight: rational shoelace centroid of the supplied bytes, without forcing ideal symmetry;
- Core: rational area-weighted mean of the already-selected 2 mm mesh samples;
- Deep, Top, and each governed Mass: the already-selected mesh sample coordinate;
- phase and anchors: selected centre form plus exact pitch multiples;
- contour segments: normalized base endpoint times scale.

Within one observed stable state, every anchor has the direct form `p(s) = a*s + b`. The discrete Centre decision does not change; only the chosen coordinates stop losing truth through float scaling, renormalization, and lattice stepping.

Both fixed inspection and rung validation use this same construction. There is one law geometry and no tolerance. The current micron seat predicate may remain only as a cheap conservative prescreen; its near-boundary answer is replaced by the same exact supplied-coordinate predicate Wrap uses.

## Scaling algorithm

1. Walk each B1-B4 band upward as a cheap observer through the frozen Centre path.
2. Record every new state: count, selected Centre/layout, parity/phase, lattice indices, and belt membership.
3. For that state and discovery bracket, enumerate its finite belt-disc x supplied-segment endpoint/interior-projection contact equations.
4. Solve their rational/quadratic roots using the retained narrow kernel; keep roots inside the bracket and band.
5. At each root, use the exact coordinate adapter to check complete seat legality and worst-belt Wrap. Re-run numeric Centre once only to confirm the same discrete state. A changed state is discarded; discovery continues. No recursion.
6. Publish the first accepted rung for each magnet count strictly greater than the last published count. Jumps larger than one are valid. Keep co-lawful placements; suppress cross-band duplicates.
7. Apply gravity only after Centre, Wrap, count, and allowance tie. Fixed and Auto reuse the same Wrap measurement.

The promise is exact truth for every published rung plus measured operational discovery. It is not a proof over every real scale.

## Mandatory pre-canon proof

Before this mechanism enters the contract, a disposable bounded probe must demonstrate the exact adapter without a generic expression layer:

- Box, Weight, Core, Deep, Top, and every Mass governor preserve the current discrete branch/layout/count;
- square 25 mm at pitch 24 has one exact verdict in fixed and rung paths, without a binary-computation disagreement;
- Weight squircle 72 mm has one exact verdict in fixed and rung paths; the test must not force lawfulness when the supplied flattened bytes are not exactly symmetric;
- analytic diamond carries its exact irrational contact witness;
- one real Masses mesh branch reconstructs directly from its existing sample/component identity.
- restoring approximate seat admission makes the fixed/rung or seat/Wrap identity fixture fail.

If this requires global regimes, expression machinery, Centre topology replacement, or recursive stabilization, reject it and shrink again. Do not amend canon around an unproved mechanism.

## Completion boundary

In scope: scaling, shared exact fixed/rung construction, performance through cheap discovery and candidate-only exact work, B1-B4, gravity, result transport, worker/cache, truthful tab, and the minimum truthful behavior for controls already visible.

Out: T4 migration, general exact geometry, continuous-domain completeness, new public proof frameworks, speculative optimization, and unrelated manual tooling.

## Gates

1. Bounded exact-adapter probe.
2. One surgical master/T3 amendment that deletes contradictory authority and copies the proved mechanism precisely.
3. Independent QA + Meta on that one canon revision.
4. Build from the cleared Centre + Wrap base in semantic rollback commits, monitored for drift but without per-commit review ceremonies.
5. One full QA + Meta on engine, worker, and live tab; Dan then reviews the product-visible result.

## Verdicts

Necessity — no unnecessary elements: the mechanism reconstructs only the already-selected coordinates directly consumed by seat, Wrap, and scaling; every global proof or Centre-rebuild element is deleted.

Sufficiency — complete conditional on the bounded adapter proof: it covers Centre preservation, exact Wrap, next-available magnet-count scaling, B1-B4 ownership, gravity, performance, transport, and truthful live delivery.
