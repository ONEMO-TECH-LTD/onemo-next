# v3.5.2-3 three-way reconciliation

Target: plan commit `3fdce16b`; master 197 lines, SHA-256 `0f49b077a455f244ec0033c0d6783242be1b0f6e3443d0b2e1122791fcade2c1`.

This is the shared correction ledger for Grid-Meta, Grid-QA and Lead. The final master is not consolidated until every row is marked `MERGED`, `SUPERSEDED` with the replacing clause, or `REJECTED` with a source-backed replacement accepted by the other two reviewers.

## Proposal register

| Input | SHA-256 | Final disposition |
|---|---|---|
| `s62-kai-lead-simplify-and-complete-proposal.md` | `588633c2d703d49c6d7f76e09db99bd55538290649b898076a17ab049918165e` | Superseded: exact roots and 0.001 touch quantum removed by Dan's whole-mm/even-size ruling. Its gravity, B1-B4, manual concessions and honesty-note gaps remain merged. |
| `s62-grid-qa-proposal.md` | `cd161b23137bce392f85439fd9100e48e1fc616862387d0f17bb103d3a774b9c` | Superseded: exact event scale, root adapter and continuous discovery removed. Its first-acceptance ownership, co-lawful layouts, B1-B4 and stored-result delivery remain merged. |
| `s62-grid-meta-proposal.md` | `e0956959dd83364fc8c34dba6f06f5aaa94a41dd1b6c0eafbb71f34da208418c` | Superseded on exact roots; merged on preserving Centre, deleting global proof machinery, next-available counts, first-band ownership and worker/UI completion. |
| `s62-synthesis-way-forward.md` | `73bea6800fcd0ef237836bd381a82a89ca9e6fbbbda1aa33fbe7c9aa705f8659` | Superseded on exact roots/0.001 input quantum; merged on measured defects, acceptance ownership, gravity, B1-B4 and deletion of the proof platform. |
| `s62-grid-meta-final-joint-recommendation.md` | `74f03edba7b011572fdd40996212953268036fda7306626b439c377cd7ac6042` | Superseded by Dan's whole-mm/even-size ruling. Its anti-overbuild boundary remains in the master stop rules. |
| `s62-team-final-necessary-sufficient-proposal.md` | `c3632262345d6249da4a6c1fda4bc5d77eff2a6ce4e8b59066794bdb9f15136d` | Superseded on exact adapter/root/FieldSqrtReal. Its distinct seated/belt/output populations, all four placements, accepted-count reducer, gravity and stored cut-over remain merged. |
| `final-minimal-delivery-proposal.md` | `696323c56a604b9c30cf4129ba18a8b67e7ac5f8a4c113b9d27272bcc2e5079a` | Superseded on exact machinery; its complete product boundary and stop conditions remain merged. |
| `s62-kai-lead-replacement-graduated-walk.md` | `6c26a275b42294b779b641614845aa07acef2c5f463794449f69aa7fa854612d` | Superseded: 1 µm refinement conflicts with Dan's whole-mm/even-size ruling. Its measured current-walk defects and direct walk adaptation remain merged. |
| `s62-grid-meta-v3522-validation-patch.md` | `bc412df8ede8a1959a868fa506d41374e60a4f32286c3f5d6d645020b11ecff8` | Merged: even sizes, distinct populations, one ruler conversion, integer Logic, no exact-event language and final execution wording. |
| `s62-kai-lead-FINAL-proposal.md` | `f83f3d140487d333f0f123d55b508e1708b007bf4059abf585bb788a8c4dc218` | Superseded by Dan's whole-mm/even-size ruling; retained only as failure analysis and proof inventory. |
| Grid-QA last corrections | `3c7f5a1090e11e62015e04c761042eae0ab7aec222725bcc6a1638913052109f` | Geometry-refusal union merged at `3fdce16b`; rounding alternative remains open until Dan locks the product rule. |
| `Centre-Wrap-necessity-and-deslop-audit.md` | `7998e50b09911805e03c2fe42c98981fe46abdd46cb27aafeacfaefedf030023` | Must be committed as the durable deslop record; its raw-sign seat sentence is replaced by correction 1 below. |

## Remaining executable corrections

### 1. One signed ruler measurement must govern final Law seat and Wrap

Measured source contradiction: at product `2c043257`, squircle 72 is lawful in Box/Core/Masses/Deep/Top but `invalid-seat` in Weight, although its clearance differs only by floating residue and mandatory fixture 2 requires all six modes lawful. Master §1 says nothing below the ruler decides law, while §5.2 currently makes raw `d >= r` decide final seat legality.

Replace the ruler and `measureWrap` rules with:

```md
Compute measures signed material clearance for every seated disc: `rawClearanceMM = pointInMaterial(anchor) ? nearestOutlineDistance - spotRadius : -(nearestOutlineDistance + spotRadius)`. It converts that value once to the whole-mm ruler: `clearanceMM = Math.floor(rawClearanceMM + 0.5)`. `[-0.5, 0.5)` reads 0, `[0.5, 1.5)` reads 1 and `[-1.5, -0.5)` reads -1. Final Law seat legality requires `clearanceMM >= 0`. Wrap measures only belt discs and requires `requiredFlapMM = max(0, ...beltClearanceMM) <= allowanceMM`. The frozen 0.001 mm Centre prescreen remains only the accepted Centre placement measurement; its sub-mm answer is never reused as the final Law seat verdict.
```

Update `measureWrap` to return `invalid-seat` only when a seated disc's ruled signed clearance is negative. Keep the existing refused union with null allowance evidence. Update fixture 2 so the Weight squircle mutation to raw `d >= r` fails.

This is the smallest mechanism consistent with Dan's 1 mm law and the contract's own squircle fixture. It adds no tolerance, quantum, exact type or second geometry.

### 2. A stored lawful layout must contain enough state to render without a solve

Current contradiction: `Rung.layouts` contains `PlacementCandidate`, but that type lacks `phaseMM`, `lattice` and the evaluated Wrap verdict. `fitSizeInBand` promises a `GridResult` from the stored ladder with no re-solve. The worker cannot reconstruct the selected placement without either recomputing geometry or inventing state.

Replace the relevant declarations with:

```ts
export interface PlacementCandidate {
  sizeMM: number
  placement: { xHalf: boolean; yHalf: boolean }
  phaseMM: Pt
  lattice: Pt[]
  canon: number
  seated: Pt[]
  belt: Pt[]
  anchors: Anchor[]
  magnetCount: number
  parityTrue: boolean
  centreErrorMM: number
  wrapMeasurement: WrapMeasurement
}

export interface LawfulLayout {
  candidate: PlacementCandidate
  wrap: Extract<WrapEvaluation, { status: 'lawful' }>
}

export interface Rung {
  band: 1 | 2 | 3 | 4
  sizeMM: number
  magnetCount: number
  layouts: LawfulLayout[]
}

export interface BandSolveResult {
  bands: BandLadder[]
  gridsBySize: ReadonlyMap<number, GridResult>
}

export interface GridResult {
  anchors: Anchor[]
  pitchCentreMM: number
  lattice: Pt[]
  phaseMM: Pt
  panMM: Pt
  spotRadiusMM: number
  contactsMM: Pt[]
  segments: SafeSegment[]
  centresMM: Pt[]
  centreMainMM: Pt
  wrap: WrapEvaluation
  parityTrue: boolean
  centreErrorMM: number
  concessions: ('CENTRE' | 'WRAP')[]
  candidates: PlacementCandidate[]
}
```

`solveBands(sized: (evenSizeMM: number) => Contour, cfg?)` receives the bridge's existing size function so outline offset remains part of the input contour at every size, then returns `BandSolveResult` after computing each even size once. `reduceBandLadders` attaches the one Logic-produced lawful `WrapEvaluation` to each retained layout. Rung selection reads `gridsBySize` and overlays `phaseMM`, `lattice`, `anchors`, `wrap`, `parityTrue`, `centreErrorMM` and `contactsMM = wrap.witnesses.map(w => w.outlinePointMM)` from the stored lawful layout. Common `pitchCentreMM`, `spotRadiusMM`, Centre evidence and candidates remain from the cached size result; `concessions: []` because a published rung passed all three laws. It calls no geometry, Centre, Wrap, Coverage, contour-scaling or Logic code. Delete `GridConfig.solveCache`: cache state belongs to the engine result/worker, not policy config.

Add one mutation gate: selecting every co-lawful layout changes the visible phase/anchors as stored while the compute-call counter remains unchanged.

### 2a. Co-nearest witnesses, conditional Centre stop and one-walk ownership

For each belt disc, emit every distinct outline point tied for the minimum distance under the same double computation. Exact native equality identifies a tie; no tolerance creates one. Preserve stable ring/segment traversal order and deduplicate identical projected coordinates. Duplicate binders never change `requiredFlapMM`.

Remove F2 from the build table. After F1, compare completed rungs against frozen Centre. If a material Centre-caused change is measured, stop and write a separate bounded amendment; this contract authorizes no Centre repair. With no measured Centre-caused failure, Centre remains unchanged and delivery closes at F1.

Mandatory fixtures prove Coverage changes only the ruled output population, MagnetPlan changes only assigned diameters, and `seated`/`belt`/Wrap evidence/rung ownership remain invariant. The owner guard proves `solveBands` is the only production loop over `BANDS`/`SIZE_STEP_MM`; worker, UI, fixed inspection and rung selection contain no second size walk. An instrumented solve visits each even size at most once per configuration.

### 3. Preserve the Dan-ruled visible tab identity

My initial proposal to rename the visible tab to `v3.5.2` is withdrawn. Dan previously ruled the tab label `v3.5.1`; the documentation version does not silently rename a product surface. Replace the master references to an isolated `v3.5.2` tab with:

```md
Preserve the isolated Law tab's Dan-ruled visible label `v3.5.1`, current worker request identity and local-storage namespace. Only the honesty note changes when all three laws render. A later visible rename requires Dan's explicit product ruling.
```

## Closure protocol

1. Grid-QA and Lead each full-read this ledger, every registered proposal and the current master; each row and correction receives an explicit accept or source-backed replacement.
2. Lead merges only the mutually resolved text into one new immutable master/sub-plan/README commit and commits the Centre/Wrap deslop audit.
3. Grid-QA and Grid-Meta independently full-read the final files and verify every ledger row against the final text. Any omission reopens the same shared ledger; no private temp-only correction closes it.

Necessity — the only additions are the two missing executable dataflows; the existing product identity is preserved and all superseded exact/root/proof machinery stays deleted.

Sufficiency — after these corrections and the outstanding Dan rounding lock, the master covers frozen Centre selection, one whole-mm seat/Wrap law, complete even-size next-count scaling, stored no-re-solve rendering, worker/UI delivery and durable deslop history.
