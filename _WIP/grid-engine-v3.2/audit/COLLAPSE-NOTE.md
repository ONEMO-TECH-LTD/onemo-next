# Collapse of plan-and-contract.md — reply to s62-meta deslop

Same file. Pointers, not deleted rules.

**Homes (one full statement each)**
- leftover-is-not-a-door → Always-true leftover row + Phase 2 delete (`spec.ts` line)
- ceiling (reduce, never grow) → pipeline step 3
- centering (parity, first-test, 48 mm, tie-break) → pipeline step 7
- 28 / 40 / 108 + gate≠exemption → Spec bullet
- mass map → pipeline step 6
- derived-from-square → class-table preamble

**Kept as you asked**
- commit + `git status` on every measurement — labelled process discipline, not engine spec
- branch/worktree path + door name — execution scaffolding

**Re-measure (distinct lines, after write)**
- centering: 1 full + 3 pointers
- leftover-cannot-refuse: 1 full + 1 pointer + 1 delete action (other leftover hits are pick-rank, exam hug ~0, door field, open numbers)
- ceiling: 1 full + pointers
- 28 / 40 / 108: 1 statement + 1 delete action (circle `@ ~40` is the class-table pad, not the limb gate)
- mass map: 1 full + pointers
- derived-from-square: 1 preamble + 1 Phase 2 action

167 lines / 2,300 words (from 190 / 3,294). Tables + exam rows are the floor; I did not cut those.

No build. Please re-index.

## 16:33+ folds (library + §21.5 + balance)

- Step 9: library grows; missing entry → Spec, not recognition.
- Open: PB §21.5 equal-support threshold.
- Step 10 names **balance** (LS §2 P8, three components). Not centroid distance, not displacement bound. Step 7 produces parity; step 10 judges it.
