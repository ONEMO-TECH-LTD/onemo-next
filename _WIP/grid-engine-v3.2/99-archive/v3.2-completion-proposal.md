# v3.2 — COMPLETION PROPOSAL

**What this is:** the zoomed-out proposal to finish v3.2 properly, produced by the read-only
audit of the code against `logic-spec-optimum.md` under /o-necessity. **Nothing here is built
until Dan says go.**

**State of play (verified, 2026-08-16):**
- **GPT Pro engine — dead.** Its "repaired" delivery is byte-identical to the failed original
  (three package archives, same SHA-256; master archive = 19 files, no source tree, reports
  claiming repairs its own bytes contradict). Moved on.
- **v3.3 — not ready.** No audited implementation exists.
- **v3.2 — the base.** Two independent audits (mine + s62-grid-qa) agree on the same shape:
  **preserve the exact-geometry substrate, replace the search/selection/offer path.** QA
  examines the repairs as we make them.
- **Current tree health, measured:** committed head 91/97, working state 92/97, five canon
  shapes failing; live bat solve **42.2s** with band 4's blessed 5pt·172 lost to a band-3
  ladder chip. The selector is both non-conformant and slow.

---

## 1. THE MINIMAL DIFF

**Preserve (untouched):** the byte-verbatim v1 compute core · guarded spec writers ·
`bridge.ts` · the bench page and its wiring · the fixture corpus and harness.

**Replace:** candidate generation → ranking → offer. Everything else stays.

That is the whole change. No new module beyond the two the spec names as missing (shape
classification, region graph). No migration ritual, no parallel engine, no rewrite of code
that works.

---

## 2. STEP 0 — RESTORE THE REVIEW SURFACE  *(no engineering)*

Roll the bench to `4bf5043a` — the last state Dan blessed (bat B3 tee·146, B4 with 5pt·172 +
six·206). The degraded working state stops being what he reviews.
**Acceptance:** port 3063 proven serving `4bf5043a`; bat chips match the blessed frames; suite
result recorded as-is.

---

## 3. STEP 1 — DELETE WHAT THE SPEC NEVER ASKED FOR

| # | Delete | Why |
|---|---|---|
| 1 | **`targetMagnets` band-count law** — ranking key + the 1/2/0/0 spec values | Dan 08-16: *"we do not have rule of band 1 = 1 magnet or band 2 having only pair"*. Count is the class×band frame. |
| 2 | **Sparse-spread standalone key** | Sparseness exists only inside "fewer at equivalent support" (§11.9). |
| 3 | **Fuller-at-stepped-band count flip** | A bigger grid may win only by better support, never by count. |
| 4 | **`maxTestedMM` 214** + its writer, limits, boundary row | Stale v1-era value; the band top 216 binds. Functionally dead. |
| 5 | **Fullest-per-footprint dedup** | Count-maximizing; its honest job transfers to pattern governance. |
| 6 | **`probe-winners.test.ts`** | A probe committed as a test. |

**Acceptance:** each deletion its own snapshot · no shape's answer changes for a reason other
than the deleted rule, and every change is named · suite no worse than before the step.

---

## 4. STEP 2 — THE SQUARE STANDARD ENTERS THE GATE

Add square / tall-rect / wide-rect / circle rows to the canon gate with their exact standards
(24·72·120·168·216 · per-axis combinations · 92/160/228).

**Why this comes before the build:** every shape in the current corpus is a free/narrow class,
which is exactly how the band-count mis-derivation survived undetected for two days. The square
is the calibration control; without it the harness cannot catch a class error.
**Acceptance:** the square returns 1 / 2×2 / 3×3 / 4×4 across the bands, or the failure is the
first defect the rebuild must fix.

---

## 5. STEP 3 — CLASSIFICATION AND FRAME HYPOTHESIS  *(funnel steps 2–3)*

Per-axis class (1–5) from the bounding box · shape class (square-like / tall / wide / circle /
free) · the frame inherited from the square standard at those axis classes — **capacity, never
compulsory**; material may reduce it, never inflate it.

**Acceptance:** a square bbox proposes 2×2 at B2 and a narrow bbox proposes the column — proven
on the Step-2 rows · no canon shape's ruled family changes.

---

## 6. STEP 4 — THE REGION GRAPH  *(funnel step 6 — the real build)*

Major masses / connectors / peripheral branches from the safe core, with local width and
**persistence across sizes** separating strong from marginal. Pure measurement in `compute/`;
all thresholds in the profile.

**Why it is the keystone:** coverage of major regions (P2) and distribution across masses (P7)
cannot exist without it, and the structure law is only standing in for it today.
**Acceptance:** the bat's map reads head-mass / narrow connector / broad lower mass without any
shape name in the code · ear tips and neck fall out by measurement, per size · the counterexample
suite still passes.

---

## 7. STEP 5 — RE-BASE THE COMPARATOR TO THE SPEC ORDER

legality → **coverage** → gravity (mass-aware) → **unsupported extent at the 12–24 switch, limb
exemption reported** → peel leverage *(OPEN: formula)* → coherent approved pattern → distribution
→ balance (evenness first-class) → **fewer at equivalent support**; then the snug seat selects
the size. Registration: canonical seat constructed first, mechanics choose, canonical breaks ties.

**Acceptance:** every ✅ canon cell holds · every moved cell is a Ⓓ shown to Dan with its frame ·
the square standard still exact.

---

## 8. STEP 6 — THE OFFER: ONE BULLS-EYE, EVERYTHING RETURNED

All distinct optimals returned; one **marked** as the guaranteed answer (L17); machine-readable
rejection reasons per rejected size/band.
**Acceptance:** the bench shows the marked answer and steps through the rest · a band that
refuses says why, in the UI, without anyone asking.

---

## 9. WHAT THIS PROPOSAL DOES **NOT** INCLUDE

Certified continuous placement (the performance fix — after correctness) · the package cut ·
ManufacturingSpec output · B5 · any adoption of external engine code. Named, not smuggled.

---

## 10. OWED CORRECTIONS TO THE FOCUS SPEC  *(provenance, caught by QA — two lines)*

1. **B4 upper allowance = 40** — RULED principle (*"more disks at the bottom and less in the
   top"*), **DERIVED value**. Reusing the limb bound was a calibration choice.
2. **"≥24mm above the previous band"** — **INFERRED**, not verifiably ruled: the vault holds only
   my restatement, never Dan's own words.
3. **Cross-band out-counting** — a **consequence** of the class×band frames, not a standalone law.

With these three marks corrected the focus spec is clean authority; QA's authority set (Dan's
rulings + the amended contracts) and this document then say the same thing.

---

## 11. /o-necessity VERDICT ON THIS PROPOSAL

**Necessity — no unnecessary elements.** Six deletions are genuine subtraction; two additions
(classification, regions) are the only elements the spec names and the code lacks; the
comparator re-base is a reordering, not new machinery; nothing is rewritten that works.

**Sufficiency — delivers the audited gap in full**, with the deferred items named in §9 rather
than silently dropped. The correctness path is complete; the performance path is explicitly a
later phase and is not presented as part of this one.

**Sequence rationale:** Steps 0–2 are hours and unblock everything (surface restored,
contradictions gone, control row in the gate). Steps 3–4 are the substance. Steps 5–6 land on
top of them and cannot be done first — which is precisely why tuning `judgement.ts` has failed
repeatedly for two days.
