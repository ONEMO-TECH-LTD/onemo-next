# v3.5.6 — task tracker

The one list. Every task cites the ruling that requires it; anything without one is not here.
Status is updated in place, in the same commit as the work it describes.

**The contract being delivered** (Dan, 2026-08-29): *"segmentation > classification > layout is the
filter already, we cannot apply anything here at all, wrap applies to that and this is MVP — no
other filters must be there at all."*

**Gate discipline** (Dan, 2026-08-29): propose → Dan and QA agree → build → lead self-gate →
adversarial gate. Nothing lands unconsumed; no additive old/new dual path.

---

## WHAT YOU GET, STEP BY STEP

Each step named by what it delivers, not by a label. The right-hand column is what is true for Dan
when it closes — what he can look at, or what stops being a risk.

| # | the step | what exists at the end of it |
|---|---|---|
| 0 | **The library stops storing filters** — DONE | The library holds frames at full coverage and the approved presets. Every chip reads its band. The engine owns the belt, the corners and the 96mm. |
| 1 | **The tests describe the library that exists** | A suite you can trust again. Today it is red and asserts a library we deleted, so nothing after it can be believed. Nothing on screen changes. |
| 2 | **The rule for where a magnet may sit moves out of the house being demolished** | Nothing visible. It is the one line of physical law living inside the function step 3 deletes; moved first, step 3 becomes a deletion instead of a rescue. |
| 3 | **The engine answers by knowing instead of by stumbling** | The bench shows, for a shape, every lawful layout its frame carries — each with its exact wrapped size, its magnet count, the band it lands in, and which magnets fell off. Unordered, unpreferred, nothing hidden. This is the MVP. |
| 4 | **Today's rulings live in the brief, not in a chat log** | Nothing is lost at the next context reset. Two invented rules already reached production by living only in a transcript. |

---

## DONE

### Step 0 · The library stops storing filters — `016292ba`

Canon-only library, square and rectangle as canon with diamond and triangle as presets, pitch-aware
frames, one band owner, catalogue v3 with a non-null band, aspect and perimeter/96 residue deleted,
bands on every frame chip.

Measured: 24mm 254 records (17/204/9/24) · 48mm 92 (9/54/5/24) · 96mm 47 (5/15/3/24). Zero without
a band, zero beyond the 384×480mm board.

**Not clear.** Production typechecks and the library lints at zero tolerance; the suite is red at
that commit. Step 1 closes it.

---

## OPEN

### Step 1 · The tests describe the library that exists

**Why.** 36 tests assert a contract that no longer exists. A red suite cannot gate anything after
it, so nothing else may start.

**Minimal diff.**
- Rewrite the 22 real invariants against the canon contract — square/rectangle/diamond frame sets
  derived from the pitch-aware limits, one population per frame, `nodes.length === cols * rows` for
  a box canon, 4×4 asserting **full 16 including its four interior nodes**, transpose truth on a
  non-square rectangle, band per chip from the canonical legal span, no catalogue legal box beyond
  384×480mm.
- Delete the tests of retired vocabulary — belt/perimeter as a spacing mode, the rectangle perimeter
  tables, the full/perimeter/perimeter-96/corners vocabulary, the 96 stride/tier/pair-balance
  family, triangle corners-as-layout and `trianglePerimeter96`. **Delete, not rename.**
- Manifest keyed by `id@pitch`: the pitches now legitimately hold different sets, so an
  identical-id-set assertion is false by construction.
- Delete the numeric-literal architecture gate **with its semantic replacements in the same
  change** — padding bounds, band table, pitch choices, retired controls, class/zone import matrix.
  It fired on the page's `11 * spanMM` font arithmetic; a number scan cannot prove domain ownership.
- Add `canon.ts` to zone 2 in the import matrix.

**Done when.** Full effect suite green · typecheck clean · scoped lint at zero warnings · the
catalogue oracle regenerated against the v3 producer with its add/drop mutations still dying.

**Not in it.** No new gate invented to replace one that is being deleted for being coincidence-based.

### Step 2 · The seat rule moves out before its house is demolished

**Why.** `registerLayout` dies in step 3, and the seat predicate inside it is physical law that must
survive. Extracting it first makes step 3 a deletion rather than a rescue.

**Minimal diff.** Move the seat predicate and its shared micron out of `registerLayout` into
`units/layout`'s own surface, unchanged. Nothing else moves.

**Done when.** Byte-identical engine answers across every shape, band and centre mode — the same
450-case capture used for the S2 moves — plus the existing tangency tests still dying under both
mutations.

**Not in it.** No behaviour change, no signature widening, no second seater.

### Step 3 · The engine answers by knowing instead of by stumbling

**Why.** The contract above. Today the engine is a size sweep with a hidden max-count winner:
on a 168mm square it builds four lawful registrations — 16, 12, 12, 9 magnets — and keeps only the
16 before wrap sees any of them, which Dan's brief forbids by name.

**Minimal diff, one change, no dual path.**
- `grid-magnet.ts` becomes the one door: segment → classify → match → wrap → an **unsorted attempt
  ledger**. No sort, no default landing, no witness, no judge.
- Classifier: ordered frame from the **union of all live masses** — never the outer box, never the
  governing mass. Ceiling derived from the live band table; the `1|2|3|4|5` type, the countdown and
  the clamp go. Returns **every** candidate on the frame with its evidence, marked ambiguous:
  8 of 24 frame groups are cross-class, so a single winner would be an invented filter.
- Matcher: candidates by band + ordered frame, returning `{ entry, view }` — one canonical record,
  the matcher says which way round. Fixes the landscape shape that cannot receive a portrait layout.
  `ROTATION_VIEWS` / `D4_VIEWS` proved through `transformLayout` before use.
- Placement: the **full** rigid population enters `wrapGroup`, which already computes the exact set
  of lawful origins. A null wrap is evidence — never permission to drop a node.
- `units/centring` stays as the governed anchor: `heldAt`/`pickOrigin` only break ties among origins
  that are already lawful, so it filters nothing.
- Deleted in the same change: `grid-magnet-wrap-compute.ts` (sweep, ladder, dedupe, judge
  composition), `units/judge.ts`, `defaultLanding`, `registerLayout` and its four-parity winner,
  `applyCoverage`, `fallbackRevealSizes`, `bestSeatedCandidate`, `grid-magnet-class.ts` and
  `shapeFamilyOf`, `grid-magnet-compute.ts` and `grid-magnet-logic.ts`, both stride decimators, the
  dead two-magnet floor, the silent invalid-band fallback, both contact thresholds, and the padding
  resolved at eleven sites — one resolver at the door returning the released 12, no unit defaulting
  or clamping.
- Shell: one card per attempt — class, frame, requested/placed/omitted, wrap size, the band it
  actually landed in, outcome. Auto-select only when exactly one attempt wrapped in the requested
  band.

**Done when.** The bench shows the raw attempt list for a shape whose right answer Dan knows, with
nothing ordering it · no production reference survives to any deleted symbol · full suite, typecheck
and scoped lint clean · the exact tree served and swept visually across pitches, classes and bands.

**Not in it.** Scoring, ordering, coverage, orientation preference, next-best, the belt as a visible
step, the 96mm garment lattice.

### Step 4 · Today's rulings live in the brief

**Why.** Rulings that live only in a transcript are lost at the next compaction, and two invented
rules already reached production this way.

**Minimal diff.** Into the existing pipeline brief, in the same commits as the work: canon is square
and rectangle with diamond and triangle as presets · the library holds canon and approved shapes
while the engine owns the filters · the board is the only cap and span is not weight · **mirrored
orientations are kept, with the reason** — on a scalene triangle the mirror is a different magnet
set, not a relabel, and hiding it would break the neutrality law; it is the same part turned over,
which is why it reads as duplication on screen.

**Done when.** No ruling from 2026-08-29 exists only in the transcript. **No new document.**

---

## DEFERRED — with the reason, not forgotten

| | why it waits |
|---|---|
| Next-best, and Dan's three probes (min count, max count, class canon) | Step 5 of Dan's own chain; the MVP is steps 1–4 and must be seen raw first |
| Scoring and ordering — coverage, orientation match, rule 4 | Dan: see the raw behaviour before anything sorts it |
| The belt, corners and 96mm as engine toggles | Dan: "96mm and perimeter are already in the engine, we just need to refactor and wire them later when the MVP is done" |
| The 96mm garment dice — half the lattice has no receiver | Dan: "solve the foundation first, the rest will be solved from here" |
| Mass depth as a dial | Dan: "we can keep it for now" |
| The eight absent slim frames (`6x1`–`9x1`, `6x2`–`9x2`) | Corpus decision, Dan's; the MVP returns an honest empty set and makes the hole visible |
| The engine's square-board size cap | `FIELD_POSITIONS_PER_AXIS` still reads one axis for both; filed, and it caps at 420mm |
