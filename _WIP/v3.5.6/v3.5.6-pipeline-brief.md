# v3.5.6 — PIPELINE BRIEF

> **What this file is.** The pipeline: what it is, what we intended with it, what each step contains,
> and specifically what the shape classifier was meant to deliver. Written from a full sequential read
> of the five s62/lead day-files — 2026-08-24 (6,090 lines), 08-25 (5,051), 08-26 (2,427), 08-27 (739),
> 08-28 (836) — by the lead, this session. Every rule below carries Dan's own words and the moment.
>
> **Authority.** Dan's verbatim wins over any sentence of mine. Requirements **accumulate**: a later
> ruling replaces an earlier one only where it explicitly conflicts.
>
> **Siblings — the only other live documents.** `v3.5.6-current-brief.md` = the engine's product authority
> (three laws, neutrality, safe area, Spec/module ownership, acceptance; its §5 Fit-mode policy belongs to
> the rolled-back v3.5.5 engine). `v3.5.6-delivery-plan.md` = the execution contract and task list.
> `grid-lab-architecture.md` = a structural record, not executable work. **This brief's §7 owns the
> open-question text, and this file is the pipeline.**
>
> Everything else has moved to `_WIP/_archive/` — history, executed task lists, superseded drafts and
> source artifacts. Its README says what each one is and why. Nothing there is an instruction; the
> directive history for 08-24/08-25 is `_archive/v3.5.6/v3.5.6-full-ledger.md` (read §§1–4 and §8 as
> record — its §5 plan is stale).

---

## 0a · PRECEDENCE (Dan, 2026-08-29)

> *"the later part of the day is superceding any conflicting part"*

Where two rulings conflict, **the later one wins**, and the earlier is marked superseded here rather
than left to be rediscovered. Requirements otherwise accumulate: a later statement replaces an earlier
one only where it actually conflicts. Applied today:

| earlier | superseded by |
|---|---|
| bands measured on the outer box (11:07) | bands on the **inner legal area**, every 48mm (11:13) |
| aspect ratio from the outer box (12:27) | **no aspect at all** — the ordered frame carries it (14:56) |
| class read from the outer outline, then from the governing mass (12:39) | the ordered frame from the **union of all live masses** (14:27) |
| "optimal = the frame's perimeter layout", §4b | **canon = the full frame**; there is no separate optimal population to declare (17:25, 18:06). §4b's *ordering* principle survives for the judge, its *mechanism* does not |
| the perimeter belt as a stored library layout | the belt is an **engine filter** applied on top, wired after the MVP (17:25, 18:00) |
| the library is written out, never generated | square and rectangle are **generated from rows and columns** (17:21) |

## 0 · When the pipeline was identified

The pipeline was named by Dan on **08-25 at 14:35, restated 14:38** — verbatim:

> *"yes one thing we need to make suyre is center defined first/ segments second/ classification third /
> the layout fourth / scale/shrink to wrap last --- scale does not affect center segment class - we have
> them known first after that each band provides layout based on the segments/class and centered -
> -final step locks the wrap and if needed may even shift the center slightly to dip the magnet disks in
> the coves"*

**Re-ordered by Dan the same day, 17:02** — class before centring, because you cannot centre a shape
before you know what it is:

> *"first step is the segmentation and 12mm erosion and defining the legal area and the class - second
> step defining based on pre-decided center to class match the centering mode > after that only fix the
> center spot"*

That re-order is the current order. It sits on top of the **axis reversal** ruled a few hours earlier
(08-25 11:03 and 11:08):

> *"we cant use this on the basis of the magnets we need to see what size>layout first"*
>
> *"we need to have sizes and wrap engine to reverse its method to wrap the size and layout first we
> cannot work on the number of magnets the nuber must come from size range"*

**Count is an output. Size and layout come first.** Everything below obeys that.

---

## 1 · What the pipeline is, and what we intended with it

> **SUPERSEDED for the MVP — see §4c/§4d.** What follows is the product-completion reasoning and
> Dan's verbatim evidence for it. It is not MVP procedure: the raw MVP applies no family, aspect,
> scoring, band gate or class-owned layout. Kept because the quotes inform the work that comes after.



**In one line:** shape in → know what it is → know where its centre is → look up the layouts its class
carries → wrap each one exactly → offer every lawful result, neutral.

**The intent** (from `v3.5.6-current-brief.md` §1 and §7): one calibratable, **neutral** engine that
automatically finds the layouts Dan can already produce by hand with the same controls — returning
*every* distinct lawful result with its evidence, hiding nothing because another result has a higher
count, appeared first, or scored better, and never reporting "no fit" while a lawful arrangement exists.

**Why a pipeline at all, rather than a search.** Dan's rejection of the alternative is explicit:

> *"why do we need to search for all 1m arrangements ? who designed that? we need to find arrangenments
> that filter through 3 laws"*
>
> *"compute must compute in the bounds of the task and procedure we set it is simple as that it doesnt
> tneed to compute all possible arrangements first"*

So each step **bounds** the next. Class bounds the layouts. Layout bounds the size. The wrap seals it.
Nothing generates a universe and filters afterwards.

**What is invariant across every step** (locked 08-24 14:34, *"yes only rigid rules are grid is static
and objects only appear in set seats and shape can only deform when we allow otherwise it is only
proportionally scaling"*):

1. **The 48mm lattice is rigid** — never rotates, never deforms, never re-registers per candidate.
2. **Magnets exist only at lattice seats.**
3. **The shape scales proportionally.** Deformation is an explicit permission, never a default.
4. **12mm padding is built into the magnet** (Dan, 08-26 14:25: *"it is built into the magnet so whatever
   shape is it is padded by that guaranteed inset"*; *"8mm is 12mm now"*). One constant, one home.
5. **Scale is the search action, not a law** (the standing three-law brief).

---

## 2 · The steps

### STEP 1 — Segmentation · 12mm erosion · legal area · CLASS

*Once per shape, at load, size-free in identity. Scale never changes what the shape **is**.*

**Contains**

1. Erode the traced outline by 12mm → the **legal area**: where a magnet centre may sit.
2. Measure it — islands, their masses, deep points, and the **segment box**.
3. Read the **class** off the segments — family, kind, and the frame per band.
4. Show it in the admin panel, by name, for validation by eye.

**The 12mm rim is physical and does not scale**, so the *legal area* genuinely differs at each size (a
100mm square's legal box is 76mm, not a scaled 88); only the **outline's own features** (centre,
silhouette, family) are scale-free. **Settled:** the legal area is measured at the current size — that is
universal eligibility, not a per-shape choice. **OQ1 is closed** (§7): there is no family to source —
triangle and diamond are layouts, and the frame comes from the union of all live masses.

**Where this stands in code:** `grid-lab-architecture.md` §3–§4. *(The old step-1 unit sheet is archived:
it marked `shapeFamilyOf` and the classifier panel "done" against the three-family target both documents
now delete.)*

---

### STEP 2 — CENTRE, named by the class

**Contains**

1. The class points at its **pre-decided centring mode** (Dan, 17:02). *The class → centring-mode table
   is still Dan's to rule — it is deliberately unfilled.*
2. Fix the centre spot **once per shape**, then scale it linearly with the shape.

**Why once.** Dan, 08-25 13:56: *"we just need to measure the center once and stick to it maybe this is
why bot with pair is also lopsided"* — and it was. The centre is a property of the shape, not of the
size; re-measuring it per size on the 2mm mesh produced 2mm jitter and 30mm governing-mass flips, and the
wobble was what made the BOT's pair lopsided. Fixed 08-25 14:01: worst grid movement per step went from
2mm pops + 20–36mm leaps to 0.69mm smooth, and the centred pair appeared by itself.

**What legitimately changes with size:** not the candidate points — only which of them **qualify**,
because the 12mm clearance is fixed while the shape scales. Positions never move; qualification does.

**Group centre vs shape centre** (Dan, 08-25 14:02):

> *"the moving center can be only the group center when we allow it to didviate from the center of the
> shape - the shape center must match with group center only when wrap applies and we optimise the wrap
> - we can evaluate group center shift from the shape center shift - but this must be an exception and
> part of the fine tune"*

And the four centring rules that govern that exception (Dan, 08-24 19:26 / 19:47):

> 1. Put the group centre on the shape centroid.
> 2. Shrink/wrap from that centred position.
> 3. Shift only by the **minimum** amount necessary to obtain a lawful tighter wrap.
> 4. Prefer the tight solution **closest to the centroid** — not the smallest shape at any centering cost.
>
> *"centroid guides selection; it never forces enlargement or overrides contact"* (19:26).

**Derived-centre proposal (Dan, 08-25 16:58, agreed in principle, not ruled):** define the centring from
the legal area itself — one mass → that mass's own centre; several masses → the **governed top mass**
(gravity first). A mass centre is by definition inside seatable material, so the void-anchor failure
(Box/Core/Weight landing in the duck's neck) becomes structurally impossible.

---

### STEP 3 — LAYOUT, from the library

**This is where the shape library belongs.** The class does not invent an arrangement; it **looks one up**.

**THE BAND IS THE LAYOUT** (Dan, 08-29). *"Skeleton is layout all same, the shape is just body."* A band
is measured on the **inner legal area** — the span the magnets occupy — in 48mm steps, because a pointed
or diagonal outline is far bigger than the region inside it that can hold anything. Every 48mm is a new
band, B1 starting at zero, and the table repeats to the board's own span instead of stopping at five.
Measured across the whole library, a record's legal band equals its frame's larger axis in **all 163
cases**: band and class agree by construction, not by luck.

**One skeleton, different bodies.** The same 3×3 layout is 120mm as a square, 130mm as a diamond,
135–159mm as a triangle. Dan: *"triangle and square in the band 3 will have different real sizes but
layout bands are the same inside."* So the band is defined once, on the legal area, for everything; the
**outline range** a band corresponds to is per class, read from that class's own outline producer.

**TRIANGLE AND DIAMOND ARE LAYOUTS, NOT SHAPES** (Dan, 08-29): *"the triangle is the layout not a shape …
same as diamond, all are subsets of the square and rectangles."* There is one frame type — `cols × rows`
on the lattice — and every class is that frame with points taken out. Verified: all 163 records sit on
their own frame's lattice, no exceptions; square, rectangle and diamond reach 100% of their frame,
triangle never above 75%. The taxonomy is naming, not structure.

**Bands belong to the catalogue** (Dan, 08-29: *"the bands belong to the catalogue"*), carried on the
record beside the size and the frame. The UI is a small shell addition to see them.

**Contains** *(the population list below predates §4d and is superseded by it: the library now holds
ONE population per frame — the canon at full coverage — and the ring, corners and 96mm are engine
filters applied on top. The reveal scan is not the MVP's fallback either; it leaves with the MVP and
its territory belongs to step 5, next best.)*

1. The recognised class + the band's frame → the **catalogue's layouts for that class**.
2. Each layout is a disk arrangement on the rigid lattice; under §4d, one canon population per frame,
   with the belt/corners/96mm as engine-side filters over it.
3. ~~The reveal scan survives as a fallback~~ — superseded: no catalogue match returns an honest empty
   result with the classification evidence; discovery is step 5's method, after the MVP.

**The ruling that made this the design** (Dan, 08-25 15:39, on whether a measured anti-flap judge should
rank layouts): the **class itself is the guard** — a correct catalogue makes a flapped layout impossible
to offer, rather than detected afterwards. *(Provenance: the collector kept only the screenshot half of
that Dan turn; the ruling survives as the quoted phrase "the class itself must be it" inside the lead's
same-turn reply, uncontested. The line below is verbatim from Dan's turn at 15:39:40.)* The Clipper
subtraction stays only as the **repair**:

> *"but if not enough use your clipper 2 fix that substracts and elipses show unprotected edge perimeter
> and forced to place magnet there with relevant wrap and layout"*

**What the library is** (Dan, 08-26 14:01 and 14:04):

> *"we dont care about the shapes this ios library of the disk layouts - outlines are for illustration
> and categorisation ease"*
>
> *"if we decide to release the shapes as presets to product we must have them stored - store layouts and
> shapes in the catalogue"*

So: **the disks are the product**; the outline is illustration *and* the categorisation handle; and every
entry is frozen as a **layout-and-outline pair** so a shipped preset stays put while the generator stays
live. Entry identity = **layout + outline in orientation** — never the layout alone (Dan, 08-26 15:39:
a 1×1 square and a 1×1 diamond are different products because the print's relation to the shape differs).

**The class taxonomy, as ruled — both layers**

*Primitive families* (Dan, 08-24 23:26):

> *"rectangle and square are just basic shapes made of square - circles and ovals are rounded versions of
> them - triangle and diamond are similar and Tand L shapes are all tringle group"*

→ **SQUARE** (fills its frame) · **ROUND** (same counts, sizes grown by corner padding) · **TRIANGLE**
(partial frame only — apex, corners, chains, diagonals; never the full field).

*Product classes in the library* (ruled 08-25 evening, and these supersede the family list **as the
library's class list**, not as the grouping):

- *"Square is square class period rectangle is rectangle class"* (18:44) — split apart.
- Diamond is its own class (18:42) — because its ring runs at **45° to the lattice**, a genuinely
  different node geometry. *"No rotated square, rotated square is not on the lattice. Lattice does not
  rotate."*
- Classes must be **pure** (18:50): *"Diagonal 2 magnets are not square it is diamond. You joking you
  really don't understand why we need different Classses to be pure?"* A class that admits foreign
  patterns cannot decide anything.
- **Round is not a separate class** (23:36): *"round is square and diamond fit so it is not real
  different the bbox is square"* — a circle takes square and diamond patterns; round-ness is a **corners
  qualifier** (`round | sharp | bevel`) on the one producer, not a taxonomy.
- Triangle is a class (mirror-symmetric about one axis only), built on the square/rectangle frames.

*Class shape* (Dan, 08-24 23:20 / 23:24): class = the **axis pair `cx × cy`**, band = the larger axis
class, **slim = minor 1–2, standard = minor 3–4**, square = `cx = cy`. Capacity = `cx · cy`, reduced by
the perimeter rule.

*Runs* (Dan, 08-25 14:50 and 23:52): thickness comes from width — under 48mm = one line, 48–96 = two,
each further 48mm adds one. A run either **holds its thickness** (rectangle), **tapers to a point**
(triangle/cone), or **meets another run** (an L at a corner, a T at a midpoint — two runs joined, not a
pattern of their own).

---

### STEP 4 — WRAP seals it, last

**Contains** the Clipper2 exact solve, unchanged since Dan accepted it on 08-24 17:11
(*"ok the wrapping works now congratulations"*):

1. Deflate the outline by the padding → exactly where a magnet centre may legally sit.
2. Translate that region by each magnet's offset and **intersect** → every grid position where the whole
   rigid group is seated. Non-empty ⟺ it fits.
3. **Binary-search the size** for the smallest that still fits — exact to 0.01mm.

The group **starts** on the centre and shifts only the minimum a lawful tighter wrap demands — the
cove-dip of Dan's own contract line. It cannot stop early or jitter, because the fit test is a boolean
and the size is solved, not crept toward.

**The model this implements** (Dan, 08-24 14:30): omnidirectional gravity — *"the shape wraps around it
and grows and wraps around it with every object addition."* For any magnet set there is exactly **one**
tight size.

**Band membership** (Dan, 08-24 18:27): a layout whose **true** wrapped size falls outside a band **does
not exist in that band**. No clamping to band floors.

**THE WRAP LAW IS PRESERVED, NOT REDEFINED** (Dan, 08-29): *"the wrap law cannot be relieved in any of
the cases — it manages flap and removes unnecessary arbitrary options where the scale could be adjusted
to wrap, otherwise we have dupes 1mm apart and options that are not fully wrapping the shape."* Shrink to
tightest, contact guaranteed, and an offer where nothing touches must not exist.

**Reaching a chosen layout does not require relaxing it.** Wrap takes a POPULATION and returns its
tightest size, so an ideal layout is reached by **handing wrap that population**, never by letting wrap
shrink onto whatever a scan happened to find. *(Learned the hard way on 08-29: a re-seat that added
magnets after wrap had solved, then re-ran the coverage rule, could drop the very magnet that was
touching — and shipped offers that wrapped nothing. Two invariants for any future re-seat: it may not
remove a magnet in contact, and it may not change a population without re-measuring wrap's own facts.)*

---

### STEP 5 — JUDGE and OFFER

> **SUPERSEDED for the MVP — see §4c/§4d.** What follows is the product-completion reasoning and
> Dan's verbatim evidence for it. It is not MVP procedure: the raw MVP applies no family, aspect,
> scoring, band gate or class-owned layout. Kept because the quotes inform the work that comes after.



**Contains**, in fixed order:

1. **The 96mm perimeter law** (Dan, 08-25 10:03):
   > *"unheld are between extreme perimeter is fine edges unprotected is bad every minimum 96mm of
   > perimeter must be pinned"*

   It is an **arc** measure along the outline, never an area. Ranking by unheld *area* is dead — it ranked
   Dan's correct 143mm duck **below** the wrong 125mm one. Exception: **1–2 magnets** are judged by
   centring/balance, not perimeter (Dan, 10:16).
2. **Gravity first** (10:08): *"gravity rule still stands top flap is priority always."* The upper mass is
   pinned before anything below. A single magnet in the middle of a mass pins nothing — a **pair across
   the width** breaks the run.
3. **Segment-edge priority** (10:16): *"prioritise segment edges and not leave any segment or edge
   unprotected"* — with the shape scaled to make the needed seat exist.
4. **Coverage dominance** — at the same size, the layout holding more wins.
5. **Best class match** — a native catalogue layout outranks a repaired one, which outranks a discovery.
6. **Landing** — fewest magnets that satisfy everything, then least shift. Every other lawful result
   stays offered; nothing is deleted to make the answer look tidy.

**Two hard constraints on this step**

- **Count is exact — no silent drop in the solver** (10:00): *"it must not be silent drop it must be exact
  number set just different layout and seat filled."* The frame and the size move to deliver N; the
  interior is never populated to make up the number.
- **Perimeter is the manufacturing default** (09:57): *"it is correct behavior w dont need gilling center
  magnets we need to pin perimeter along the edges - simple."* The perimeter belt's subtractive
  count-drop is **correct behaviour**; that is not in conflict with the line above.
  **The perimeter law and the 96mm law are two different rules** (Dan, 08-29: *"it is not 96mm — it is
  perimeter only, no mid internal range fill; 96mm is a different story"*). The perimeter law governs the
  POPULATION: no interior fill. The 96mm arc is the unpinned-edge MEASURE, and it feeds the judge's
  choice between offers — it is not what "perimeter" means.
- **More magnets ≠ a bigger shape** (08-24 22:18) — offer the next rung, not a 100mm leap.

---

## 3 · THE SHAPE CLASSIFIER — what it was meant to deliver

> **SUPERSEDED for the MVP — see §4c/§4d.** What follows is the product-completion reasoning and
> Dan's verbatim evidence for it. It is not MVP procedure: the raw MVP applies no family, aspect,
> scoring, band gate or class-owned layout. Kept because the quotes inform the work that comes after.



**Its role, as Dan put it on 08-29:** *"to equip the engine with decision making power — to see what
class of the shape it is and the optimal perfect layout count, and apply it first within each band; if
it is not possible, the next best one."* So the class does not merely narrow the search: it names the
answer the engine should be getting, and the engine goes and gets it. The catalogue is where that answer
lives, and it must state **aspect ratio, orientation and the optimal layout** for the classifier to read.

**It measures the MASS box, not the outline.** A shape's outer box is blind to limbs, and so is its
legal box — a 30mm arm eroded by 12mm on each side leaves a 6mm sliver that stretches the box the full
length while holding nothing. Only the mass survives the depth probe. Measured: a block with a dead
30mm arm and one with a usable 50mm arm both read 3×6 on the outer box AND on the legal box; the mass
box reads 3×3 and 3×6. **Aspect comes from the outer box** (Dan, 08-29) because a 1-wide legal box is a
line and has no ratio at all — but what a shape can HOLD is the mass, and both facts are needed.

**Its role in one sentence: the classifier BOUNDS THE SEARCH.** It converts a shape into *which layouts,
which counts, which sizes to try*, so the engine tries real buildable arrangements instead of generating
everything. That is the answer to Dan's *"why do we need to search for all 1m arrangements?"*

**Dan's own statement of the deliverable** (08-25 10:25):

> *"we classified shapes based on geometric shapes like square and rectangle etc - each actually has
> matching magnet coun x size (if not perimeter applied it is standard) so we can recount with the
> perimeter and understand what the bands are actually and what layout and number of magnets and sizes
> range is expected from classified shape."*

**→ classified shape → expected layout × count × size range, recounted with the perimeter rule.**

### What it must deliver

1. **The class of the shape** — read from the segments/legal area: family, the axis pair `cx × cy`,
   kind (slim / standard / square), and the frame per band.
2. **The candidate set that class implies** — the expected layouts, their counts and their size ranges.
   Dan, 08-28 07:59: *"we need classifier to recognise that matching shape is triangle rectangle diamond
   or square - the specifics like T and L is just same basic canon classes with some disck taken out"*
   (and *"Rectangle is H with mid taken out"*).
3. **Answers drawn FROM THE CATALOGUE, not from a hardcoded list.** Dan, 08-26 13:46: *"we need to build
   library in such a way that output catalogue is clean and can be fed in the standard format into the
   engine classifier."* Dan, 08-28 09:30: *"it was always the plan for classifier to answer from the
   catalogue."*
4. **Adaptable and expandable, automatically.** Dan, 08-28 09:27:
   > *"the library is always work in progress the classifier needs to be adaptable and expandable to
   > scale automatically not be hardcoded it scans catalogue of the library"*

   Add a shape to the library → it is in the candidate set. **No classifier edit, no plan rewrite.**
5. **A visible decision trace.** Same turn: *"we must be able to see each time when shape classified how
   it made the decision as verifier visually as well."* For any shape: the candidates considered, what
   discriminated them, why it landed where it did — on the surface, not in a log.
6. **A standing audit inside it.** Same turn: *"audit test inside it so we have no silent
   miss-classifications."* Every catalogue record must classify to its own class, and **ambiguity must be
   reported, never silently resolved.**

### What it is NOT

- **Not a lookup table replacing generation.** The solver already seats magnets in *any* region
  (`latticeOver` + seat predicate + `splitPerimeter`) — unknown shapes are handled by generation. Dan,
  08-28 08:01: *"we can make classifier automatic for any shape - beyond L and H there could be others we
  do not know about and basic shapes must automatically adapt to them."* Enumerating every possible shape
  is a race that cannot be finished.
- **Not a hardcoded family enum with tuned thresholds.** `shapeFamilyOf`'s `fill < 0.68` cut and its
  `0.08` / `minor <= 2` constants are salvaged, exemplar-tuned numbers with no ruling behind them.
- **Not a coarse pre-filter that can discard the right answer.** Today the hardcoded family gates the
  catalogue match — a wrong family silently removes the correct entry before anything else runs. The
  dependency runs backwards and must be inverted: the catalogue defines the classes; the classifier reads
  them.

### Measured state (08-28, my probes)

| | |
|---|---|
| Library class → what `classifyShape` answers | square (16) → `square` · rectangle (38) → `square` · diamond (13) → `triangle` · triangle (96) → `triangle` |
| Answering from the catalogue on the current coarse key | **123 of 163 decided · 40 ambiguous · 0 missed** — the baseline *before* the Pareto classifier, not the proposed result |
| Matcher key today | `(family, cx, cy)` — average **9.5** candidates returned, 16 for diamonds |
| `round` family | reachable by nothing in the library; a pill classifies as `square` |
| Engine consumption | **none** — `solve.worker` does not consume the catalogue matcher |

The catalogue path **never misses**; it is only sometimes undecided, and the 40 ambiguous cases are
exactly those where the key ignores the fill, aspect and frame it already computes. The hardcoded path
can *never* separate square from rectangle, or diamond from triangle.

The **proposed** classifier — exact axis-pair filter, then a Pareto frontier over fill and aspect error —
and its measured result at all three pitches live in `grid-lab-architecture.md` §4. This section is what
the code answers **today**.

**One genuine defect, open:** 4 of 38 rectangle records report `cx = cy` — a rectangle carrying a square's
axis pair. That is a silent misclassification of the kind the standing audit exists to catch.

**Honesty boundary, standing:** *the catalogue is not "fed into the engine" until the solver consumes the
matcher.* Until then the status is **"contract landed, runtime consumption pending"** — never
"integrated".

---

## 4 · The library's place in the pipeline

**The expected layouts and counts the classifier hands the search ARE the catalogue.** The classifier does
not own a list of shapes; it reads the shape, and the catalogue supplies the candidate arrangements for
it. **Library grows → search space grows → no classifier edit.**

The catalogue has a **second job** the generator cannot do for itself: it is the **certified set** — the
163 frozen records at 24/48/96mm are the cases where the right answer is known, mm-exact. A generator
with nothing to check against is guesswork with extra steps. (This is what the solver-vs-catalogue oracle
now asserts; it found the y-flip registration defect — 68 records / 204 pitch-cases — on its first run.)

Format: one record per entry — class, type, stable id, disk positions in mm, outline polygon in mm, size —
every class through the same producer, the outline computed from the disk layout and the built-in 12mm
padding so nothing is hardcoded.

---

## 4b · Optimal first, then scored (Dan, 08-29 14:12) — MECHANISM SUPERSEDED, ORDERING LIVE

> **Superseded in part by §4d (17:25, 18:06).** There is no separate "optimal" population to look up:
> the canon IS the frame at full coverage, so a per-frame optimal flag would be a constant. What
> survives is the ORDERING principle below — an exact fit ranks first and everything else is next-best
> and scored — and that belongs to the judge, which is deferred until after the MVP.


*"Each band search returns optimal layouts within range based on the class optimals — rectangles and
squares carry them. If they fit exactly = optimal; it can be 1, potentially 2 in a range. The rest are
the next best."*

And on whether wrap should mean shrink-to-tightest or smallest-fully-held: *"these become scoring
questions and we dont need to choose one or another — we need to select both and score."* **Neither
reading replaces the wrap law.** Both become scores on offers wrap has already produced.

The user is given an approximate size range; manufacturing picks the optimal from it. A band usually
carries one or two optimal sizes and the rest sit within ~12mm — a difference small enough to be
negligible to the user and decidable later.

## 4c0 · THE PIPELINE — Dan's five steps, verbatim (08-29 16:29, the controlling statement)

> *"the entire pipeline is the compute and selector because if each step is done it can arrive into
> result only within nerrow set boundaries:*
> *1. Class - filters the selection to classes*
> *2. Bands - provide defined anchors for measurement and ranges for limits - another filter*
> *3. Layout - the optimal layout found and attempted within shape*
> *4. Wrap - fine tuned the shape to fit optimal layout - anything falling off the layout is just
> omitted as not fitting the shape*
> *5. Next best - attempts to define other layouts that may fit the band/class and wrap law if we fine
> tune center, shift the grid vs shape panning it and rescaling it to adapt and fined snug and better
> smaller/larger magnet count inclusion - old system find the max magnet count and apply may apply
> here - but it must be adjusted to find min count > propose > find max count > propose and find if
> the optimal count based on the class canon fits > propose"*

This supersedes the six-step draft in §2 wherever they differ. **The MVP is steps 1–4** (§4c below);
step 5 is deferred until the raw behaviour has been seen, and its method is Dan's three probes above —
not the sweep. **Omission is step 4's own law, and its mechanism is Dan's 11:55 description** — settled, not open:
seat the layout at the governed registration, test **each position independently** against the real
material (the seat rule: 12mm clear of every edge and hole), **omit what the material refuses**, then
hand the surviving group to wrap for its exact tight size. Every omission is recorded on the result —
which positions, and that containment refused them. `wrapGroup` itself stays whole-group-in,
tightest-size-out; the per-position test happens at seating, before it, using the same physical law as
everything else. What remains dead: guessing one placement and dropping whatever misses there, and any
omission the result does not declare.

## 4c · THE MVP, and the ban on invented filters (Dan, 08-29)

> *"so model whereby we dont now how engine selects must not exist - step 1 - segmentation >
> classification > layout is the filter already we cannot apply anything here at all wrap applies to
> that and this is MVP - no other filters must be there at all"*

**The wire contract, in one place:**

```text
request:  contour + pitch + released padding + the current Centre settings
segment:  usable material, holes preserved
classify: ordered frame from all live masses
match:    every catalogue {entry, view} at that pitch + frame
wrap:     each full entry population, at the governed anchor
result:   an unsorted list of attempts; the band is a label on a result
```

**The pipeline IS the filter.** Segmentation says where material can hold a magnet, classification says
which frame the shape is, layout says which populations live on that frame, wrap says what size each one
needs. Each is constitutive — remove it and there is no candidate at all. **Nothing sits on top.**

Everything else leaves the MVP: the parity winner, the perimeter belt, the default landing, offer
ordering, the family gate, the five-band ceiling, both outline decimators, the dead two-magnet floor, the
silent invalid-band fallback, the two contact thresholds.

**Band becomes a label, not a rejection.** A layout belongs to the band of its true wrapped size — that
is a fact *about* the result. So the MVP says "this population wraps to 174mm, which is B4" rather than
deleting it because B3 was asked for. Deleting would be a filter; labelling is the classification already
ruled.

**The hidden filter this exposed, measured 08-29:** layout builds four lawful registrations and keeps one.
On a 168mm square at 12mm padding they seat 16, 12, 12 and 9 magnets, and only the 16 survives — chosen by
magnet count, before wrap sees any of them. §7 of the current brief forbids a max-count prefilter by name.

**The released rim is one number.** Where a caller omits it the engine falls back to the slider's floor of
10 rather than the released 12 — at eleven sites, each of which also raises a smaller value to 10. Dan:
*"well point it to the last locked number instead - is it not obvious?"* One resolver at the door, no unit
defaulting or clamping.

## 4d · CANON AND FILTERS — what the library holds (Dan, 08-29)

> *"canon full frame - we decide on the top what magnets stay or go based on the toggles of belt/corner/
> 96mm etc it is all just selection filters later the canon raw full coverage rows and columns"*
>
> *"store only pure library of canon and approved shapes - the engine will add anything we need on the
> top later"*
>
> *"the canon is square and rectangle the rest are layouts for us to have for potential presets"*

**Canon = square and rectangle frames at full coverage.** `cols × rows` on the lattice, every node
populated. Diamond and triangle are **presets** — approved shapes, not canon: the diamond is the square
patch under a Manhattan mask, the triangle its authored geometries.

**The ring, the four corners and the 96mm sampling are FILTERS**, applied on top, engine-side. Two of the
three could never be answered in the library at all: the belt depends on which magnets actually seat on
real material, and only the engine has seen the shape. Dan, 08-29: *"96mm and perimeter are already in the
engine we just need to refactor and wire them later when the mvp is done."*

**No aspect ratio.** Ruled 08-29: not stored, not computed, not matched on. The ordered frame carries
everything a ratio would have said and does not degenerate on a one-magnet-wide frame.

**Frames are pitch-aware.** The board is a fixed **384 × 480mm of legal area**, so it holds 17×21
positions at 24mm, 9×11 at 48 and 5×6 at 96. Reading the position count as pitch-free published 44
layouts at 96mm that cannot physically sit on the board.

**The board is the only cap** (Dan, 08-29): *"we must have 9x11 board as the biggest size ... we dont have
to limit the grid engine at all it calculates and spits out anything."* **Span is not weight** — a 7-wide,
2-row strip spans B7 on its long axis and carries the area of a small square, so capping generation by
band would delete legitimate light products. The connection-type ranges (twin fix B1–B3/B4, garment-bound
B2–B4/B5) stay product observation and never filter generation.

**Mirrored orientations are kept** (Dan, 08-29: *"if we need to keep them fine make the defense and
explain"*). The defence: on a scalene triangle the mirror is a **different magnet set**, not a relabel —
the 224×75 wedge reads `0,0 0,1 0,2 0,3 1,1` upright and `0,0 0,1 0,2 0,3 1,2` mirrored, one spur magnet
on the other side. Hiding it removes a lawful arrangement, which §7 of the current brief forbids. It is
also the same part turned over, which is why the row reads as duplication on screen.

**Two rules that were never Dan's, and are deleted.** `corpus-*.ts` carried *"written out, never
generated"* and `rules.ts` quoted it as *"(Dan: a readable table, no generation)"*. Traced to `5a449de8`,
25 Aug: the sentence is the lead's, and it read *"no generation AT SOLVE TIME"* — what was rejected then
was a solve-time generator filtering patterns per family. Separately, a test asserted *"4x4 carries NO
interior magnet in any layout (Dan, 08-25)"* while Dan's own canon list that day reads *"4x4 — full 16,
ring 12, corners 4"*.

## 5 · Expected result of the whole pipeline

For any supplied shape and admin settings: **every distinct lawful magnetic layout ONEMO can actually
build, shown neutrally, none hidden, each reproducible by hand with the same controls** — with its class,
its layout, its exact wrapped size, its centre deviation and its coverage evidence.

**The acceptance picture is Dan's duck** (08-25 10:07–10:10): 3 magnets → the third in the **head** at
~143mm, not the neck at 125. 4 magnets → a **pair per mass** at ~147mm. 6 magnets → **three stacked pairs**
at ~192mm, waist row skipped. Pictures back, not analysis.

---

## 6 · Dead — do not resurrect

Count as the engine's input axis · tightest-wrap seat selection · unheld **area** as the judge ·
count-bands (1-4/4-9/9-16/16-25) · canon docs as binding law (parts bin only) · the v3.5.5 blast-build and
its marking formula · Minkowski · SVGNest · physics engines · `deltaCallback` · `rectClip` · every dial I
invented (flap thresholds 400/2,000/20,000mm², hold-reach, centre-preference slider, shift cap, coarse
anchor cache, coarse walk) · flap as anything other than the magnet's own 12mm (the disk IS the
allowance) · a measured 96mm judge **replacing** the class as the guard (it is the repair, not the ranker).

---

## 6b · The garment canvas — DEFERRED, foundation first (Dan, 08-29)

*"This tshirt is the optimal canvas to work with bands 3-6, so it is a different game here — the same
lattice just sparser. We need to finish the current plan and focus on that; after that we just add the
proper 96mm lattice on a solid foundation. Do not jump to solve all problems — solve the foundation
first and the rest will be solved from there."*

**What the real canvas is**, from Dan's photo and the v1 law in `grid-core.ts` (§13.5d, locked): the
shirt receives on a **96mm dice** — a 96mm grid plus the same grid offset half a pitch both ways, so
nearest receivers sit 68mm apart on the diagonal. That is the quincunx, legal only at pitch 96 because
its centres land on 48-offsets; a 48 dice would need 24-offsets and nothing halves either atom.

**Why it matters, measured:** laid over the straight 48mm lattice this engine seats on, a node attaches
only where `col + row` is EVEN — **exactly half the positions have no receiver**. A 2×2 block of
adjacent 48mm seats is a lawful wrap and an unattachable product.

**It is attachment-dependent, not universal** (v1 §5/§10.2): `magnetic` registers on the garment's
96-dice · `twinfix` has NO garment constraint, its own mirror half is the counterpart · `velcro` has no
grid at all. That is also the reason behind the band ranges — twin fix sits low and free (B1-B3, B4 at
its lowest), garment-bound is constrained (B2-B4, B5 occasionally), and the shirt canvas is optimal for
**B3-B6**.

**The two canvas sizes** (Dan, 08-29): *"regular is 7x9"* · *"next step in 96mm grid is 9x11 — so
this can be max size for now."* Counted on the straight 48mm lattice, one 96mm step being two lattice
positions:

```
              lattice   legal span   receivers   rows
regular        7 x 9    288 x 384       32       4,3,4,3,4,3,4,3,4
max (for now)  9 x 11   384 x 480       50       5,4,5,4,5,4,5,4,5,4,5
```

Receivers are the even-parity half, so the alternating row counts are the dice pattern seen head-on.

**This corroborates the B3-B6 range rather than restating it.** A B6 effect's magnets span up to 287mm
— the last that fits inside the regular canvas's 288mm width. B7 begins at 288 and fits only exactly,
in one orientation. So "bands 3-6" is what a 288mm-wide canvas physically holds, not a preference.

**The board is the ONLY cap, and the engine is not limited below it** (Dan, 08-29, correcting his own
previous message): *"we must have 9x11 board as the biggest size — the reason being we don't have to
limit the grid engine at all, it calculates and spits out anything, so these decisions are moot and
unnecessary completely. I said wrong. It may not be planned in the near future to have a single effect
9x11 or 7x9 — but if you think about it, a slim rectangle 7 columns wide and 1-2 rows is not that
heavy, and overall area may equate to a B4 square or something similar. So making the sizes stretch to
the end of the grid board is fine."*

**Span is not weight — that is the correction.** A 7x2 strip spans B7 on its long axis and carries the
area of a small square. Capping generation by band would delete legitimate light products. So the
connection-type ranges (twin fix B1-B3/B4, garment-bound B2-B4/B5) remain PRODUCT OBSERVATION and never
become a filter on what the engine generates.

**It does settle the row count at 11**, which was open. Of its two spec consequences, one is done and
one is filed: the spec now carries the board as `FIELD_COLUMNS = 9`, `FIELD_ROWS = 11` and the two
millimetre spans, and the band table runs to **B11**. Still filed, not built: the engine's own size
ceiling reads one axis for both and caps every shape at 420mm on a 384×480 board. The engine generates
to the board edge; nothing below it is capped.

**The corpus gap this exposed — since closed.** At the time of the ruling the library held 24 frames
against the 99 a 9x11 board admits, and the eight slim frames of Dan's own example (`6x1`–`9x1`,
`6x2`–`9x2`) had no source. Generating square and rectangle from the board (Dan: *"just add them ...
complete the library"*) closed it: every frame the board holds at each lattice now exists, the slim
eight verified present at 48mm.

**Not built, deliberately.** v3.5.6 carries no attachment concept at all. This lands after the
classifier/optimal-first foundation, on top of it — not woven into it.

## 7 · Open — Dan's calls

This brief is the home for these. Quote an ID to rule on one. Only **OQ1** gates the authorised
S1–S4 build; the rest are re-derived when their deferred stage is authorised.

**OQ1 · ~~Class source split~~ — CLOSED 08-29.** It only had to be answered if a shape had a *family*.
It does not: triangle and diamond are layouts, everything is a subset of squares and rectangles, and the
frame comes from the **union of all live masses** — never the outer box, never the governing mass, because
only the mass union sees that a 30mm arm holds nothing while a 50mm one does. ID retained, never recycled.
**OQ2 · Class → centring-mode table** (step 2) — deliberately unfilled; the one mapping I will not guess.
**OQ3 · Which catalogue entries ship** as product — 45 shapes / 163 records are a review corpus.
**OQ4 · ~~The wire format~~ — CLOSED, not deferred.** It was parked pending a read of an external
caller. There is none in this lane's scope: Studio is a separate repo and integrates engines later.
The engine defines its output contract on its own terms. ID retained, never recycled.
**OQ5 · ~~Band boundaries~~ — CLOSED 08-29.** Bands are measured on the legal area, every 48mm, B1 from
zero, and the table runs to **B11** because the board is 9 columns by 11 rows and 11 rows spans 480mm.
Interior magnets: full coverage is the canon, and the belt that empties the middle is a filter applied on
top — so interior magnets always exist in the canon and are removed by a named, visible choice.
**OQ6 · Control wording** — degrees everywhere, or portrait/landscape stays on the rectangle.
**OQ7 · ~~The eight unauthored slim frames~~ — CLOSED 08-29.** Dan: *"well just add them it is not
rocket science complete the library."* Generating square and rectangle from the board produced them:
`1x6`–`1x9` and `2x6`–`2x9` are present at 48mm (54 rectangle frames, verified by probe), and their
landscape forms come from the matcher turning the canonical record.

**Settled 08-29:** the MVP is segment → classify → layout → wrap and nothing else (§4c) · canon is square
and rectangle, diamond and triangle are presets (§4d) · the ring, corners and 96mm are engine filters, not
library layouts · no aspect ratio anywhere · frames are pitch-aware from the board in millimetres · the
board is the only cap and span is not weight · band is a label on a result, never a rejection · mirrored
orientations are kept, with the reason recorded · the released rim resolves once at the door to 12mm.

**Settled earlier:** legal-area erosion at the current size (universal eligibility) ·
solver → catalogue wiring (authorised: *"finish properly wiring and completting the classifier the
pipeline and run it"*) · supplied holes (the engine must preserve every `Contour.holes` a caller
supplies — a defect to fix, not a decision; raster multi-ring *extraction* stays parked upstream) · the
96mm arc (a law in judge — eligibility and repair evidence, never an optional ranker).

---

## 8 · Current implementation truth (branch `session62-task/v3.5.6-lead`)

*The QA-CLEAR on this branch is the **library lane** — 45 shapes, the frozen catalogue, its 28 gates and
the solver-vs-catalogue oracle. The engine and pipeline below are explicitly incomplete.*

**Built:** the canon library — one population per frame at full coverage, square and rectangle generated
from the board at the asked lattice, diamond and triangle as presets, one outline producer (disks + 12mm,
corners as data) · **catalogue v3** — 254 records at 24mm (square 17, rectangle 204, diamond 9, triangle
24), 92 at 48 (9/54/5/24), 47 at 96 (5/15/3/24), every one banded, none beyond the 384×480mm board ·
`bandOfFrame` in `rules.ts` as the single band owner, read by the frame chips and by the materialiser ·
the Clipper exact wrap · the centre bake (measured once, scaled) · the solver-vs-catalogue oracle.

**Landed:** the canon library at `016292ba` · the suite translation at `18bf5bf4`/`e6dd0d6e` —
**582/582 passing, typecheck clean**. P0 awaits QA's re-gate; nothing beyond P0 has started.

**Not built:** the classifier reading the mass union (it still measures the outer box and clamps at five) ·
the matcher answering by band + frame and returning which way round (`catalogueCandidates` has no
production caller and still filters on the invented family) · the one headless door · the worker reduced
to transport · the shell showing the raw list · the decision trace · the standing misclassification audit.

**Still running the old way:** the size sweep with its hidden max-count parity winner, the judge, the
default landing, the perimeter belt inside generation, both outline decimators, and the rim resolved at
eleven sites. All of it is deleted in one change, not wrapped — that change is P1.3 of the delivery
plan and it has not started.

**Known engine defect, pinned:** the y-flip registration — 68 records / 204 pitch-cases where the engine
registers its lattice wrongly for a flipped view, frozen in a fixture asserted *equal* so it can only be
paid down.
