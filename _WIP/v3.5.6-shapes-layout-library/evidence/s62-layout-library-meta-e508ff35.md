# [Codex s62-qa][META-CLEAR] Layout-library lane — e508ff35

Strategic verdict: the library now matches the directive behind the build, not merely its local tests.

- One product surface: Bench and Library reuse the same page, canvas, lattice and single Stage. The Library tab swaps the model and controls, then returns to Bench intact.
- Clean boundary: the library owns class specification, selection, product options, authoring transitions, geometry and validation. The page holds React state and calls those operations; the panel renders resolved options; neither contains class policy. The bridge converts finished millimetre geometry into engine records and does not resolve, classify, transform or choose outlines.
- Portable contract: one class registry covers square, rectangle, diamond and geometry-derived triangle classes; engine geometry and admin controls are structurally separated without a second registry or empty triangle sentinel.
- Spec fidelity: 48/96 spacing remains shared class policy; custom remains the only hand-selection path. The triangle corpus preserves the exhaustive 79-layout geometry universe, exposes the ruled 76 active products across the ten authorised names, and derives exact three-line, 12mm-clear outlines from magnets after the selected view.
- Honest scope: the tab remains visibly `LIBRARY DRAFT · CANDIDATE REVIEW`. The library produces a pipeline-ready record, but this lane does not pretend Step-1 engine recognition policy has been decided or wired; that is the separately sequenced engine lane.
- Whole-lane visual evidence at exact `e508ff35`: four classes; ten triangle type tabs; all 76 active triangle offers; every outline exactly three vertices; saved registry and triangle drafts reopen through their emitted identity; one shared canvas; Bench return; zero errors. Evidence: `/tmp/s62-qa-e508ff35.png`.

NECESSITY: no unnecessary elements. The final architecture subtracts duplicated dispatch, sentinels, bridge policy and caller-owned frame truth; the remaining modules each own one domain concern.

SUFFICIENCY: delivers the library-authoring directive in full—same Centre Lab surface, portable modular class specification, clean UI shell, conversion-only bridge, complete ruled class corpus, and a finished geometry record ready for the separately scoped engine step.
