# [Kai][AGREEMENT] Adoption of /tmp/s62-library-root-fix-proposal-1fa641a5.md — block by block

Read in full (602 lines). Status per block, so we execute an AGREED prescription:

## Adopted verbatim
- **Order correction** — outline convergence before catalogue. Your reasoning accepted as stated.
- **Block 1 (pitch-authoritative frames)** — LANDED at d94152a1, essentially your code:
  RAW_CLASS_FRAMES + registryFramesAt(family, pitchMM), withSpacingModes(pitchMM), layoutAt
  deleted with both implementations and both call sites. Plus the resolved-equals-rendered
  counterexample (mine counts nodes; your block-6 verbatim-positions test supersedes it at
  step 2+). **This commit is submitted for your gate now** — 530/530, tsc/lint clean, live 4046
  four classes unchanged, zero console errors.
- **Block 2 (offsetPathMM + outlineFromLayout, fixed padding)** — adopted. Your unbounded-miter
  sharp claim I verified independently before accepting: 3-point corners on every triangle
  including the 3.2 deg worst case, clearance 11.9997-12.0000mm. hullOutlineMM dies.
- **Block 4 (CatalogueEntry + catalogue() + matcher)** — adopted INCLUDING your honesty clause:
  status is "contract landed; runtime consumption pending" until Dan authorises the solve.worker
  Step-1 wiring. Nobody claims engine integration off an uncalled helper.
- **Block 5 (librarySurface, geometry-only bridge)** — adopted.
- **Blocks 6-7 (proof gates, narrow barrel)** — adopted; your it.each verbatim-position test
  replaces my count-based one.

## Adopted with ONE amendment — Dan ruling, 2026-08-26
- **Block 3 (class modules, variantOf, delete LIBRARY_SHAPES)** — adopted, EXCEPT any reading
  under which same-disk entries collapse across classes. Dan: the 1x1 diamond STAYS. His reason
  is physical, not aesthetic: print an image upright on both and the square meets it edge-up,
  the diamond corner-up — same disk, two products. Standing principle now recorded: entry
  identity = disk layout + outline IN ORIENTATION; never dedupe across classes by disks alone.
  Mechanically this fits your own contract: the diamond class supplies its orientation through
  boundaryOf (exactly as H supplies its ordered boundary); the single producer still draws it,
  34x34 = pad*sqrt(2) derived, nothing hardcoded. If you see a cleaner mechanism for a
  class-stated orientation than boundaryOf, counter with it — the RULING (1x1 diamond stays,
  distinct) is Dan's and fixed; the mechanism is ours.

## Team structure, from Dan
Lead = planning/orchestration with Dan. You = peer QA + conceptual advisor (reports always
carry a verbatim proposal — as this one did). @s62-pixel-builder = executor; build specs go to
it once Dan releases it (it is hydrating). So: gate d94152a1, and ack or counter the block list
above so the builder receives an agreed prescription, not a contested one.
