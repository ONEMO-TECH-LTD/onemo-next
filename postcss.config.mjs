// App is plain CSS — no Tailwind, no PostCSS plugins. (Tailwind removed: 0 utility usages;
// its @theme pipeline tree-shook the DS token cascade and broke converted screens.)
const config = { plugins: {} };
export default config;
