# =============================================================================
# ONEMO Environment Variables
# Copy this file to .env.local and fill in actual values
# =============================================================================

# Supabase
NEXT_PUBLIC_SUPABASE_URL=          # https://<project-id>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=     # Supabase dashboard -> Settings -> API -> anon/public key
SUPABASE_SERVICE_ROLE_KEY=         # Supabase dashboard -> Settings -> API -> service_role key (NEVER expose to browser)

# Cloudinary
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=du6q2q4ab
CLOUDINARY_API_KEY=                # Cloudinary dashboard -> Settings -> API Keys
CLOUDINARY_API_SECRET=             # Cloudinary dashboard -> Settings -> API Keys (NEVER expose to browser)
CLOUDINARY_ENV_PREFIX=dev/         # "dev/" for DEV, "" (empty string) for PROD

# Shopify
SHOPIFY_APP_CLIENT_ID=             # From Shopify custom app (onemo-shop-next). Used by scripts/shopify-token.mjs for Admin API token exchange.
SHOPIFY_APP_CLIENT_SECRET=         # From Shopify custom app. Used by scripts/shopify-token.mjs. Keep secret.
SHOPIFY_STOREFRONT_ACCESS_TOKEN=   # From Shopify custom app (onemo-shop-next)
NEXT_PUBLIC_SHOPIFY_STOREFRONT_ACCESS_TOKEN=  # Storefront API token (safe for browser). From Shopify custom app.
SHOPIFY_ADMIN_API_ACCESS_TOKEN=    # From Shopify custom app (or client credentials grant — see SSOT 4.4)
SHOPIFY_WEBHOOK_SECRET=            # From Shopify webhook settings
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=  # onemo-dev.myshopify.com (DEV) or onemo-4865.myshopify.com (PROD)

# App
NEXT_PUBLIC_APP_URL=               # http://localhost:3000 (DEV) or https://onemo.fashion (PROD)

# =============================================================================
# Agent & Design Tooling
# Used by Claude Code, scripts, SDK — NOT by the Next.js app.
# =============================================================================

# --- Figma ---
FIGMA_ACCESS_TOKEN=                # Figma -> Settings -> Personal Access Tokens. Expires May 18, 2026. Used by figma-console-mcp.
# figma-console-mcp also reads this from ~/.claude.json mcpServers.figma-console.env
# Bridge port: 9223 (requires Figma Desktop launched with --remote-debugging-port=9222)

# --- Supernova (design system versioning/backup) ---
SUPERNOVA_API_KEY=                 # app.supernova.io -> Profile -> Settings -> Authentication. No expiry. Used by @supernovaio/sdk v2.3.2.
SUPERNOVA_DS_ID=728795             # ONEMO DESIGN SYSTEM (not a secret)
SUPERNOVA_WORKSPACE_ID=680319      # ONEMO TECH LTD (not a secret)
SUPERNOVA_VERSION_ID=766339        # Shared Draft — active working version (not a secret)
SUPERNOVA_BRAND_ID=773067          # Default brand (not a secret)
# MCP Relay (read-only): https://mcp.supernova.io/mcp/ds/728795 — configured in ~/.claude.json
# SDK: npm install @supernovaio/sdk — import pattern: import pkg from "@supernovaio/sdk"; const { Supernova } = pkg;
# Baseline snapshot v0.1.0 (ID: 768121) — locked Feb 17, 2026. First import from Figma.

# --- Figma Plugin IDs (not secrets, for reference) ---
# Supernova Figma Variables Sync: 1303357900761384370
# Variables Pro (Dan's manual export/import): 1264578192495051449
