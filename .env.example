# =============================================================================
# ONEMO App — Environment Variables
# =============================================================================
# Copy this file to .env.local and fill in real values.
# NEVER commit .env.local to git.
# =============================================================================

# --- Supabase ---
NEXT_PUBLIC_SUPABASE_URL=           # https://<project-id>.supabase.co — Supabase → Project Settings → API
NEXT_PUBLIC_SUPABASE_ANON_KEY=     # Safe for browser. Find: Supabase → Settings → API
SUPABASE_SERVICE_ROLE_KEY=         # SERVER-ONLY. Never expose to browser. Same location.

# --- Cloudinary ---
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=du6q2q4ab
CLOUDINARY_API_KEY=                # SERVER-ONLY. Find: Cloudinary → Settings → Access Keys
CLOUDINARY_API_SECRET=             # SERVER-ONLY. Same location. Never expose.

# --- Shopify ---
SHOPIFY_STOREFRONT_ACCESS_TOKEN=   # Create: Shopify Admin → Settings → Apps → Develop apps
SHOPIFY_ADMIN_API_ACCESS_TOKEN=    # Same location.
SHOPIFY_WEBHOOK_SECRET=            # Set when registering webhooks.
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=  # onemo-dev.myshopify.com (DEV) or onemo-4865.myshopify.com (PROD)

# --- Application ---
NEXT_PUBLIC_APP_URL=               # https://onemo.fashion (production) or http://localhost:3000 (local)

# --- Cloudinary Environment ---
CLOUDINARY_ENV_PREFIX=dev/         # Use dev/ for DEV; leave empty for PROD
