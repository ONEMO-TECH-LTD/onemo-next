// useLevaHighlight — placeholder for per-value Leva label highlighting
// TODO: reimagine for Camera/Environment only (materials now use custom sliders)

'use client'

export function useLevaHighlight() {
  // Leva is now only used for Camera + Environment
  // Per-value highlighting will be built into MaterialPanel directly
}
