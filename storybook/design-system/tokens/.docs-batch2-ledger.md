# Docs batch 2 — proof-of-work ledger

Scope: Storybook Documentation section updated for everything since the border batch
(border/, bg/ final, effects tier, aluminium material, /default + app-ordinal laws).

## Files touched

| File | Change |
|---|---|
| documentation/data.ts | + EFFECTS_COL/AL_FX constants, leafMeta(), numeric() |
| documentation/semantic.tsx | FolderTable tier widened to border; border swatch (stroked pill) |
| documentation/backgrounds.tsx | NEW — BgFolderTable (surface swatch pairs, frost demo) |
| documentation/effects.tsx | NEW — StrengthRange, BlurLadder, TintLadder, TintRange |
| documentation/materials.tsx | NEW — BaseMaterials, BehaviorGrid |
| documentation/Colour-Border.mdx | NEW page — contrast-role law + 4 folder tables |
| documentation/Colour-Backgrounds.mdx | NEW page — bg/ family, laws (card/sheet, hover, lazy-mint) |
| documentation/Effects.mdx | NEW page — alias-only law, strength range, ladders, native materials |
| documentation/Materials.mdx | NEW page — mono triad + aluminium behavior grid |
| documentation/Changelog.mdx | + 4 entries (2026-07-12), Pending updated |
| documentation/Overview.mdx | Sections note updated |

## Data state

- Pull attempted via bridge-peer: plugin pushed the WRONG FILE's catalog (iOS 27 kit was the
  active Figma tab) — clobber restored from git.
- Lane-sourced pull (figma-console driver, DS-file-guarded) blocked the same way: active tab
  was the iOS kit for the whole window; guard aborted before reading anything.
- Committed snapshot therefore predates today's bg/aluminium work (border + effects data ARE
  current in it). All new pages derive at render time and complete themselves on the next
  successful pull from the DS file: `node storybook/design-system/tokens/pull-ds-tokens.mjs`
  with the DS tab active (or the lane variant in the session scratchpad: pull-via-lane.mjs).
- Until then: Backgrounds page renders the pre-aluminium bg/ names; Materials page renders
  Ink/Snow rows only (aluminium rows appear after refresh). Flagged in-page via Notes.

## Pull stats (committed snapshot in repo)

23 collections; sem-col resolved rows and per-collection counts as in ds-tokens.generated.json
(fileKey Qdb9Kx98afJHxaCGAIxoMC).
