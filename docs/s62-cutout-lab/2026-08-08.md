# 2026-08-08 — the ledger itself

**Transcript:** `claude/s62/lead/2026-08-08/_day.md` — 95 lines, 21 turns, 00:30–00:41 BST. Read in full.
**Commits:** none on the lab. The only output of this day is `docs/s62-cutout-lab/` in the v2 worktree.

## The directive
> *"Ok v1 fixed the vector tools diff the code currently presets behave broken like before we fixed
> them. I we need to read all transcripts of the work we have done and diffs and commits we did and
> frankly take notes of all changes in a ledger otherwise all good stuff is reinvented at best or
> approximated that can lead to more bugs. Read your all transcripts days in full and take notes per
> day of threads and match to code changes in every commit or latest v1 state. Find the transcripts
> full reads and ledger as you go no skipping to reproduce from memory. Found my directive and change
> or fix register in ledger and find in code create references to files and precise lines in code and
> explain it. No slop no lazy assumption. Full code reads. All days till we pivoted to v2 today."*

Three refinements followed:
- **00:30** — *"Execute in worktree docs folder next to rules and briefs for v1 and v2 labs."*
- **00:31** — *"Daily files only."*
- lane: **S62-Kai-lead** (the s62 lead transcripts, not another lane's).

## The trigger — worth recording precisely
Dan's opening sentence is the reason the ledger exists: **"v1 fixed the vector tools … the presets
behave broken like before we fixed them."** A fix that was made, proven, and then lost to a rebuild is
exactly the "reinvented at best, approximated at worst" failure. The Simplify/Smooth/Radius saga
([2026-08-06](./2026-08-06.md) F14–F17) is the clearest case: it reverses **twice**, and only the
transcript records why the final form is right.

## Scope decided from the record
- **Boundary:** the lab thread starts **2026-08-04**. [08-03](./2026-08-03.md) is read in full and
  proves it — the first lab commit is `28d74a4f` (08-05 16:23).
- **End:** the v2 pivot on **2026-08-07**, so 08-03 → 08-08 is the complete span.
- **Per-day files**, one per transcript day, per Dan's *"daily files only."*

## What the read cost and produced
| day | transcript lines | ledger |
|---|---|---|
| 08-03 | 113 | boundary proof + two failure patterns named that recur in the lab |
| 08-04 | 2935 | the baseline decision (`bf15c448`/`98ae0deb` proven live) + the poisoned-oracle lesson |
| 08-05 | 4217 | 37 entries — north star, Safari constraint, the 7-model benchmark, the structure law's origin, the v5.3.1 inventory table, the gesture taxonomy, the engine-native refactor |
| 08-06 | 2387 | 26 entries — plug-in-don't-clone, the ghost-alpha root cause, the full knob calibration, the ONE LAW, the bridge discovery, I1/I2/I2b |
| 08-07 | 3618 | 25 entries — the breakage chain, the wand lessons, the pivot, the deletion law, v2's 13 increments |
| **total** | **13,270** | 6 day files + index |

## The one thing this ledger is for
Every entry marked **⚖ STANDING** is a ruling that is still law, and every **⚠️** is a trap that cost
hours. Before any change to the lab: **grep this folder for the subsystem you are about to touch.**
If a rule is already written here, it is not an open question.
