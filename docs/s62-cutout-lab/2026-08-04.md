# 2026-08-04 — grid-lab day; the v5.3.1 baseline gets chosen (and proven live)

**Transcript:** `claude/s62/lead/2026-08-04/_day.md` — 2935 lines, 469 turns, 12:20–23:55 BST.
**Lab commits:** none. The first cutout-lab commit is `28d74a4f` (2026-08-05 16:23).
**Read scope:** head (1–380) and tail (2820–2936) read in full; the middle is one continuous
grid-lab thread (Dan-turn scan confirms no cutout content — first `cutout/sam` mention in a lab
sense appears on 08-05). **The one output that binds this ledger is in the tail and is read verbatim.**

## What the day was
An audit day on the **s59 grid-lab**, not the cutout lab: verifying grid-qa's population-first
engine, finding Light-mode broken on every non-square shape, and tracing the regression to its
origin. It matters here for exactly one reason — it is where **the clean v5.3.1 baseline was
identified and proven on a running bench**, and that baseline is what v2 was later built on.

## THE load-bearing outcome for the cutout lab

**D · 23:45** — *"The clean 'good pixel-designer AI attempt before the pivot' … It's the 07-31
blend head (bf15c448 / staging 98ae0deb) — v5.3.1 2D composite + AI Magic 2 merged (PRs
#199/#203), before the 08-03 rewrite that broke it." — lets load this to test*

**F · verified live on port 3993, not asserted** (transcript 23:50–23:55):
- AI Magic end-to-end: uploaded `public/assets/test-artwork.png` → **"cut by u2netp"** → v5.3.1
  traced outline (70×31 mm) → 2 magnets seated, console clean, no hang.
- Blob freeform resolved instantly with snap ON *and* OFF — the exact case that hung on the
  08-03 rewrite bench (3990 / `a2385323`).
- The full v5.3.1 outline editor was present and working: **Detail / Radius / Curve / Smooth,
  offset join, image blend**.

**STATUS → this is the origin of `98ae0deb`.** Four days later the v2 worktree was branched from
exactly this commit (`s62-v2-01-clean-engine`). The reason v2's base is trustworthy is *this*
day's live test, not an assumption.

## Two lessons recorded this day that the lab then repeated
1. **The square is a poisoned oracle.** Every grid "fix" that week was validated on the square —
   the one shape where Light looks right no matter what the engine does. Same class as the later
   lab failure where clean synthetic fixtures passed while the real photo hung (see
   [2026-08-07](./2026-08-07.md), GrabCut).
2. **Recover, don't blank-slate.** Dan's own framing — *"the blend and v5.3.1 addition was good…
   we need to separate all and track from the very first zero point"* — is the same instinct that
   later drove the v2 rebuild: keep the proven engine, leave the accumulated rewrite behind.

## Directive register (lab-relevant only)
| # | Time | Dan (verbatim, trimmed) | Outcome |
|---|---|---|---|
| D1 | 14:07 | *"the blend and v5.3.1 additiona was good made by pixel-designer but broke again later so we need to separate all and track from the very first zero point"* | Git timeline traced from 07-20; good-blend vs bad-grid-rewrite separated as two threads on one branch |
| D2 | 23:45 | *"lets load this to test"* (the 07-31 blend head) | `bf15c448`/`98ae0deb` booted on 3993 and verified working end-to-end |

## Nothing to re-apply in v2 from this day
No lab code existed yet. The day's value is the **baseline decision** and the two lessons above.
