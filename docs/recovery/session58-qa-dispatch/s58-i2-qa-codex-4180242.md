# s58-qa I2 QA Gate — 4180242

Verdict: FAIL-with-findings

Target:
- Repo: /Users/daniilsolopov/Dev/onemo-dev/onemo-next-qa-i1-6e5e757
- HEAD: 4180242
- Branch: session58-task/react-figma-engine
- Global package repo: /Users/daniilsolopov/Dev/onemo-dev/onemo-component-library

Probe setup:
- Added temporary project-only component under `src/app/(dev)/react-figma-components/qa-i2-codex/`.
- Ran isolated dev server on `http://localhost:3035`.
- Removed temporary component after probes.
- Both repos ended tracked-clean.

Primary executed probe:
- Script: `/tmp/s58-i2-probe.mjs`
- Result: PASS 12/15 assertions.
- Covered:
  - `add-variant-axis` creates string-union prop with default.
  - Repeated `add-variant-axis` is idempotent.
  - `add-variant-value` extends the union once; repeated value is idempotent.
  - Two axes compose in `className` as `[styles.base, styles[\`size_${size}\`], styles[\`variant_${variant}\`]].filter(Boolean).join(' ')`.
  - Scrambled composite payload `{variant=primary, size=lg}` writes canonical selector order:
    `.base.size_lg.variant_primary[data-loading]:hover, :global([data-fc-preview="hover"]) .base.size_lg.variant_primary[data-loading]`
  - Reversed caller order does not leak into CSS.
  - CSS-only model-unreadable fallback path returns 200 and preserves caller order rather than throwing.
  - `variantAxes` read back from source union props in source order.

Additional checks:
- `/api/dev/editor-components` reported the probe component with two axes:
  - `size: sm | md | lg`, default `md`
  - `variant: primary | secondary`, default `primary`
- `/react-figma` returned 200.
- `/react-figma/components-canvas` returned 200.
- `npm run typecheck` exited 0.
- `npm run lint` exited 1 on pre-existing broad repo baseline; no probe-specific lint error was observed, but I did not prove zero-added beyond source/diff scope.

Finding 1 — MED: combinatorial scoped writes are not read back into ComponentModel.

Evidence:
- Source write path canonicalizes and emits the correct selector:
  - `src/app/api/dev/editor/lib.ts:519` `scopedSelector(...)`
  - `src/app/api/dev/editor/lib.ts:553` sorts composite `axisValues` by `parseComponentModel(...).variantAxes`
  - `src/app/api/dev/editor/lib.ts:565` passes the sorted scope into `scopedSelector`
- Source read path only classifies simple selectors:
  - `src/app/api/dev/editor/lib.ts:851` `classifyScopedSelector(...)`
  - `src/app/api/dev/editor/lib.ts:855` matches only `.base.<name>`
  - `src/app/api/dev/editor/lib.ts:856` matches only `.base:<pseudo>`
  - `src/app/api/dev/editor/lib.ts:857` matches only `.base[data-state]`
- Executed API proof after writing the combinatorial selector:
  - `variantAxes` were present and correct.
  - `variants` included only `.base.size_lg` and `.base.variant_primary`.
  - `states` was `[]`.
  - The written `.base.size_lg.variant_primary[data-loading]:hover` rule did not round-trip into the model.

Why this blocks:
- The signed blueprint says the source code is the document model and combinatorial rules decompose back to their `{axisValues, semantic, pseudo}` set.
- Current WRITE is correct for the specific I2 determinism fix, but READ is lossy for the same selector family.
- After a combinatorial edit, the editor cannot fully rehydrate that scoped target from source; that violates the bidirectional-compiler contract even though the CSS was emitted correctly.

Pass notes:
- The high-priority reversed-order canonicalization fix is closed on the write path.
- The model-unreadable fallback path does not throw.
- Multi-axis union prop creation/extension, defaulted destructuring, className composition, inventory axis metadata, routes, and typecheck passed in this probe.

Cleanup:
- Temporary project files removed.
- `git status --short` empty in onemo-next QA checkout.
- `git status --short` empty in onemo-component-library.
