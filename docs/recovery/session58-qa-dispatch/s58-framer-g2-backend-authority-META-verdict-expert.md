# META VERDICT — G2 backend-authority + revalidation + E2E-harness rework
**Reviewer:** @s58-expert (Meta, this cycle — Dan-assigned) · 2026-07-13
**Reviewed SHA:** `8d64fd3ede947aa1275e7896238bb3ce6f3aee4f` · branch `session58-task/s58-framer-architecture` · worktree `.codex/worktrees/s58-framer-architecture` (tree clean, HEAD confirmed).
**Binding docs (unchanged this cycle):** Hard Contract (470L, `5893…`), Architecture (672L, `a0ef…`). QA gate: PASS-TO-META, ledger `s58-framer-g1-rework-qa-gates.md`.
**Instrument:** no new user-visible UI in THIS backend cycle → live-Framer side-by-side isn't the instrument for these three items. Reviewed against Hard Contract + Architecture invariants, code-read + **own re-run** of the exact-behavior tests.

> ## ⚠ STATUS: FINAL META VERDICT **HELD** (2026-07-13, lead/Dan correction)
> This is **NOT a rendered final verdict.** Sequencing correction from Dan: **5 known unfixed UI findings** from the designer's earlier Framer-parity pass (F-P0 import-gate dead-end worst) must be **fixed BEFORE** any final audit — backend correctness must not be verified *around* them. This document is the **saved backend scope-1/2/3 findings** (lead confirmed "they're solid — save them"), scoped to the three backend items only. The **final Meta verdict is withheld** until: (a) the 5 UI findings are fixed, and (b) a genuine final-audit round runs where **every reviewer (QA, expert, designer) does the FULL independent triad** — Chrome click-through + code conformance + actual `/o-deslop` — not a code/visual split. On that dispatch I will run the full triad myself and render the real verdict then.

## BACKEND SCOPE FINDINGS (saved, not a final gate): the three items HOLD
The backend-authority + harness rework holds against the Hard Contract + Architecture invariants **for its scope**. Execution-backed, not eyeballed. (These stand as inputs to the eventual final audit — they do not by themselves clear the cycle.)

## Scope verified (3 items, all confirmed)
**1. Migration no longer borrows current-project tsconfig topology** — CONFIRMED.
- `authoring-sidecar-migration.ts`: reconstructs compiler authority from the graph's OWN recorded historical bytes; **pre-split w/ components + no recorded `tsconfig.json` → named 409 `AUTHORING_MIGRATION_HISTORY_SOURCE_UNAVAILABLE`** (refuse, no borrow); **post-split (`environmentFingerprint` present) → reconstruct environmentHashes from historical bytes + verify `environmentFingerprint === graph.environmentFingerprint`** (409 if mismatch) → default authority succeeds.
- Linchpin `parseExactCompilerConfigFromSources` (`authoring-tsconfig.ts`) is **strictly sources-map-bound**: `ParseConfigHost.readFile/fileExists` read only the provided historical bytes, `readDirectory` = `() => []`, **zero disk access**. A missing `extends` target throws named `SOURCE_TSCONFIG_INVALID` (refuse), never a disk borrow. Compiler options now derived from `parseExactCompilerConfigFromSources(...)` instead of stored `snapshot.compilerOptions`.
- Invariant fit: Hard Contract §9 (migration refuses without writing; no borrowing) + Architecture source-authority / no-fallback (same "refuse, don't fall back" philosophy as SourceAnchor).
- **Tested + re-run green (mine):** `refuses historical V1 migration rather than borrowing a current tsconfig authority`; `uses historical default compiler authority when post-split history predates tsconfig`; `migrates historical graphs with their own compiler options and dependency topology`; `refuses invalid ambient regeneration … without durable writes`.

**2. Revalidation retains graph-owned consumer/page source authority through extract→format-edit→revalidate→undo** — CONFIRMED (real bug fix).
- `authoring-session.ts revalidate-source`: previously validated only the TARGET component's source and rebuilt hashes from the target snapshot → dropped authority over other graph-owned sources → broke undo. Now it collects the FULL graph-owned source set (`before.components` + `before.instances` source roots + `before.sourceProperties` module-css stylesheets), per-root: reads exact snapshot, **guards non-target drift with `SOURCE_HASH_STALE` 409**, asserts environment fingerprint, re-runs type semantics, re-projects + re-asserts every component owned by that file, accumulates `exactSourceHashes`; module-css files hashed from disk with the same staleness guard. Commits `expectedSourceHashes/sourceFiles = exactSourceHashes` with **`replaceSourceHashes: true`** (authoritative full replacement, via the new `authoring-transaction.ts` flag `sourceHashes = replaceSourceHashes ? {} : {...before}`).
- Invariant fit: Architecture §4 (graph-owned source authority) + §8 (undo = new revisioned txn applying inverse against exact current hashes — requires the full authoritative hash set retained).
- **Tested + re-run green (mine):** `revalidates changed source authority without hiding the latest variant undo`; `replaces a component dependency hash set instead of retaining a deleted dependency`; `rebases generated compiler-environment drift without changing authored authority or semantic undo`; the pre-write drift-refusals (type-invalid / structural / CSS cascade / base declaration / nested media).

**3. Cold-start E2E harness — no generic network waits** — CONFIRMED.
- `tests/e2e/react-figma-authoring.spec.ts`: all 3 `waitForLoadState('networkidle')` replaced with **causal deterministic readiness** — `expect.poll(tokenResponses.length === editorDocumentRequests.length)` + all-`200` assertions, plus explicit DOM signals (`__engineWired`, `contentDocument.readyState`, `data-authoring-busy='false'`, `data-authoring-resume-phase`, frame `src`, layer rows). Zero `waitForTimeout` in the spec. Wrapped in a bounded `.toPass({intervals})` retry.

## Execution-backed evidence (my own, this SHA)
- Re-ran **7 focused test files → 110 tests PASS** at `8d64fd3` (authoring-session, component-session, tsconfig, transaction, migrations, import, editor-authoring/route). The route suite (27) directly encodes the scope claims above.
- Relied on QA's full-suite (455 pass + 10 declared skips) + typecheck/lint/`o-deslop` clean for breadth; I did not re-run the full suite or the 2×~1.1m E2E (QA's evidence + my focused re-run + code-read cover the risk surface).

## Meta notes — LOW, non-gating (backlog; engineer credit-blocked until Jul 19 → none require action before Dan sign-off)
- **N1 (naming, LOW):** the migration's tsconfig-`extends`-missing path refuses as `SOURCE_TSCONFIG_INVALID`, not the migration-family `AUTHORING_MIGRATION_HISTORY_SOURCE_UNAVAILABLE`. Behaviour is correct (named refusal, no borrow); the pre-check gates on `tsconfig.json` presence only, not the extends chain (caught downstream). Error-family consistency is a cosmetic cleanup.
- **N2 (perf, LOW):** revalidate-source now reads a fresh snapshot + type-checks EVERY graph-owned source root per revalidate (O(sources)). Correct; could grow on large graphs — fold into the deferred Dan performance-budget decision (Hard Contract §14).
- **N3 (defensive, LOW):** `Object.assign(exactSourceHashes, snapshot.sourceHashes)` across per-root snapshots resolves a shared-dependency hash by assign-order rather than asserting cross-snapshot equality. Low risk given the staleness + environment-fingerprint guards; a `same-file → same-hash` assertion would harden it.
- **N4 (visibility, agree w/ lead):** E2E port-3045 appears in exactly 2 assertion prefixes as `PLAYWRIGHT_BASE_URL ?? 'http://localhost:3045'` — portability nit (assertion keys off `PLAYWRIGHT_BASE_URL`, not `PLAYWRIGHT_PORT`); doesn't weaken the passing proofs. Architecture-doc stale status banner/§0/§11/L672 = doc-hygiene; the semantic clauses/hashes QA used remain authoritative. Both confirmed nonblocking.

## Credit-block note
The engineer being Codex-credit-blocked until Jul 19 is **NOT a blocker for this cycle**: all Meta notes are LOW/non-gating, so nothing requires the engineer to act before Dan's sign-off on this backend-authority cycle. N1–N3 fold whenever the engineer is unblocked (or log as debt); N4 is visibility-only.

**Bottom line:** the three backend items are correct, contract-aligned, and execution-verified **for their scope** — saved as solid inputs. But the **final Meta verdict is HELD** (see ⚠ STATUS above): not Ready-for-Dan until the 5 UI findings are fixed and the full-triad final audit runs. Nothing marked Done — Dan's gate.
