# s58-qa I4 connectors QA @ 7239646

Verdict: FAIL-with-findings.

Scope:
- Repo: `/Users/daniilsolopov/Dev/onemo-dev/onemo-next-qa-i1-6e5e757`
- Target: `7239646`
- Server: `NEXT_PUBLIC_SUPABASE_URL=http://localhost NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy npm run dev -- --webpack -p 3035`
- Fixture: temporary project component `src/app/(dev)/react-figma-components/qa-i4-codex/ConnectorCard.tsx` plus `/qa-i4-codex`, removed after QA.

Finding 1 - HIGH - switch connector emits generated TS that fails typecheck.
- Repro: promote fixture root, add union axis `variant: 'primary' | 'secondary'`, click the actual editor `Tap-cycle variant` button.
- Generated line: `ConnectorCard.tsx:8` emitted `setVariantInternal((v) => { const vals = ['primary', 'secondary']; return vals[(vals.indexOf(v) + 1) % vals.length] })`.
- Typecheck failure:
  `TS2345: Argument of type '(v: "primary" | "secondary") => string' is not assignable to parameter of type 'SetStateAction<"primary" | "secondary">'.`
- Source cause: `src/app/api/dev/editor/lib.ts:1095-1097` builds `vals` as an untyped string array, so the updater returns `string`, not the axis union.
- Impact: I4 gate requires `tsc 0`; generated clean component fails.

Finding 2 - MED - state connector readback loses trigger/to, so write-read-write is not lossless.
- Repro: write/click state connector with `trigger:'hover', to:{state:'hover'}, spring 260/20/1`.
- CSS output is correct for transition: `/* @fc-transition: spring 260 20 1 */` and `.base { transition: all 0.733s linear(...) }`.
- ComponentModel output after re-read:
  `{"mode":"state","trigger":"state","to":{},"transition":{"kind":"spring","stiffness":260,"damping":20,"mass":1}}`
- Source cause: write comment stores only spring params at `src/app/api/dev/editor/lib.ts:1051-1067`; read hard-codes `trigger:'state', to:{}` at `src/app/api/dev/editor/lib.ts:1274-1277`.
- Impact: blueprint/lead gate says connectors round-trip mode/to from side-channel comments. Switch connector does; state connector does not.

Pass evidence:
- State transition output: actual UI `Add spring transition` click changed label to `Spring 260/20/1` and wrote `@fc-transition` plus base `linear(...)` transition.
- Runtime computed transition: browser computed `transition` as `all 0.733s linear(...)` on both instances.
- Switch runtime behavior: uncontrolled instance clicked in live `/qa-i4-codex` switched to and held `variant_secondary` (`rgb(34, 170, 102)` after mouse moved off hover); controlled `<ConnectorCard variant="primary" />` stayed primary after click.
- Actual editor button path: entered component edit mode through Components rail double-click, clicked `Add spring transition` and `Tap-cycle variant`; labels updated to `Spring 260/20/1` and `Tap-cycles variant`; generated files changed accordingly.
- Switch connector readback: `{"mode":"switch","trigger":"tap","to":{"axis":"variant","value":"secondary"},"cycle":true}`.
- Baseline after cleanup: fixture removed, stale `.next/dev/types/app/(dev)/qa-i4-codex` generated cache removed, `npm run typecheck` exits 0, app repo tracked-clean, package repo tracked-clean.

Cleanup:
- Removed temporary fixture component, route, and `/tmp/s58-i4-probe.mjs`.
- Closed browser and stopped dev server.
- Final tracked status: clean in both repos.
