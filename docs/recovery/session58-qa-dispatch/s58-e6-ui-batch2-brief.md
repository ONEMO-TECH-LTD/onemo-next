# E6 UI batch-2 dispatch — @s58-engineer (standing role per Dan)

**From:** @s58-designer (Kai). **Report to:** @s58-designer. Pipeline per Dan: build → SELF-VALIDATE (gate 1) → request review DIRECTLY from @s58-meta-qa → my meta review after its PASS. Same as your last run — that run was clean, keep the discipline.

**Dan's standing mandate for you:** you own the side panel, small builds, and **full-mission Figma UI states & behavior conformance** — continue cloning Figma: rest/hover/focus/active/disabled states, popover behaviors, exact chrome. Truth source = Figma's real DOM. You cannot reach authenticated figma.com; the extraction loop is: check `/Users/daniilsolopov/Dev/onemo-dev/__qa-dispatch/s58-figma-ui3-extracted-specs.md` first (measured specs live there and I keep appending), and for anything missing DM me `[EXTRACT-REQUEST] <control + which states>` — I measure it from Dan's live authenticated tab within minutes, same as the Publish disabled spec last time.

## Where
Fresh branch off the CURRENT engine HEAD: `git -C ~/Dev/onemo-dev/onemo-next worktree add .claude/worktrees/s58-e6-ui2 -b session58-task/react-figma-e6-ui2 origin/session58-task/react-figma-engine` (fetch first; base is 87b26fa or later). Own dev server on :3026. Never touch :3025 or my branch. Push your branch after each task.

## Tasks (Linear, claim → Building)
1. **KAI-9360 — Link section in the inspector (Framer behavior).** New inspector section for the selected element: link target = internal page (list from /api/dev/editor-fs routes + /react-figma-pages/*) or external URL; "open in new tab" toggle. Writing: wrap/attach an anchor via the EXISTING server ops only — `insert-jsx-child` / `set-jsx-style` are available; if you need a dedicated wrap-in-link op, STOP and DM me [QUESTION] — server ops are mine (Dan's split: you visuals/behavior, me code-heavy). UI anatomy: Framer's Link control per Dan's screenshot intent — labeled section, target dropdown, URL field; fields must ride the FigmaField component (it's in page.tsx now — use it, do not hand-roll inputs).
2. **KAI-9359 — File menu, UI half.** The top-left file selector becomes Figma's file menu clone: dropdown lists RECENT builds (persist recents in localStorage keyed by route; current build checked), entries for Publish/Version history/Duplicate/Delete/Branch — render the menu structure faithfully (extract row anatomy from the specs file / request extraction), but ONLY wire what has a real backend today: switching builds (switchCanvas) and Publish (commitOverrides). The rest render disabled with honest tooltips ("wired in E6.9 backend") — Dan-approved investigation for branch/fork is MINE and pending. Also: the raw filesystem path line under the selector must go — replace with the Finder-style picker: `<input type="file" webkitdirectory>` is client-side and allowed; on pick, resolve whether the folder is a loadable build (has src/app) and DM me [QUESTION] if resolution needs a server route (it likely does — that route is mine, tell me the contract you need).
3. **Standing conformance sweep (fold into both):** every control you touch gets Figma-true states (hover/focus/active/disabled) from measured specs — no invented colors, Phosphor-light icons only.

## Bar (unchanged)
Real-input Playwright verification per task; tsc 0; zero pollution outside page.tsx (+ engine.ts only if unavoidable); commit+push per task; evidence in commit messages; self-validation note → meta-qa request → verdicts DM'd. Done = both tasks Ready for QA + [SELF-VALIDATED + QA-REQUESTED] DM to me.
