# T8 grok-qa hydration ledger — 2026-08-17

Hydrated. No T8 verdict. Waiting for lead to re-send the gate.

## Plan (2e6bd212) — T0 through T9

- T0 compile settled authority from the two designated briefs. No questionnaire.
- T1 restore 60656152, subtract condemned selector policy.
- T2 independent oracle. Observed frames cannot pin winners.
- T3 one backend. Empty approximation is not certified infeasible.
- T4 continuous F = A ∩ ⋂(C_r − o_i). Envelope carried. Quantise then re-prove through construction door.
- T5 certified descriptors over full F (area components ∪ exact witnesses). Interval or DECISION_INDETERMINATE. No vertex/projection/extremum completeness.
- T6 Product Base funnel. Sequential restrict-in-order. Canonical is start + final tie-break only. Complete identity. No fallback.
- T7 replacement gate: prove the new selector before deleting the old one.
- T8 ship one selector: delete sweep / auto-search / growth / heuristic comparator / topology-only dedupe / fallback. Verification: no parallel selector; T7 suite still green; no unrelated cleanup.
- T9 wire existing UI. No redesign.

T8 only parses after T6 (one funnel in judgement) and T7 (that funnel proved). grid-core becomes delivery only.

## Defect class the last QA lane taught

Nothing returns a bare number. Every measure is an interval with direction and status. Comparison decides only when two intervals are certifiably separated. Undecided is first-class. Anything that converts uncertainty into a winner is the hunt.

Caught last lane, and why:
- Mass-graph collapse: T6 took distinct masses from the 24 mm deep hierarchy level. That level was INDETERMINATE_WITHIN_TOLERANCE + collapsed on BAT/PILL → distinctMassCount 0. Fix: safe-core (level 0) is always the mass graph.
- Lattice round-trip: multiply-round-divide is the encoding decode uses; the inverse drifted off the micron grid.
- Open vs closed set: `pointInPreparedContour` is open even-odd; lawful tangency is distance-zero. Re-price that treated the boundary as outside contradicted the bracket (PILL B2 vertex).
- Centre-space vs material-space: a major-support region is already eroded by r. Comparing raw core bounds to the padded box labels a solid body a limb. Reconstruct envelope as core ⊕ disc(r).
- Overclaim withdrawals: a passing suite is not a completeness proof; a reused T7 sweep artefact is not a T8 measurement; first-of-set is not a winner under CERTIFIED_SET / UNRESOLVED_SET.

## Source — what each file is

**spec.ts** — values only, three write levels. RELEASED padding 12, maxSize 310, pitch 48, registration `point`. RELEASED_CALIBRATION peelToleranceMM3 1 / peelMaxEvaluations 2000 / distributionMaxCells 64. P4 switch 12 default, options 12|24 only (`selectUnsupportedExtentLimit` refuses anything else). Axis classes independent of offered bands. Class×band cells: deferred cells permit nothing. Node classification levels [12, 24], strong at index 1.

**structure.ts** — certified descriptors.
- `globalAnchor`: phase-two restrict. Unresolved component that can still win → whole descriptor indeterminate. Envelope is direction-relevant min/max of hi/lo.
- `certifiedDominance`: unit+direction must match; either side undecided → false; minimize only if a.hi < b.lo − tol.
- P8 balance: interval-refined incumbent (interior t* possible). Not exact argopt.
- P3 hanging: monotone in t_y; complete one-quantum strip, interval covers the whole strip.
- P4: lattice sublevel rectangle; per-region in material space (+r). Compute applies no policy.
- P5: dual INNER/OUTER rectangles. high moves only on inner non-empty; low only on outer empty; unproved probe stops. Segment/ambiguous contact → honest indeterminate, not empty. Finite-F fast path. Witness closure absorbs straddling witnesses into `high`.
- P2 coverage / P7 distribution: exact certified partition. P7 secondary variance only over primary-max ties; overflow → indeterminate, not estimated. Closed-set membership: in-contour OR distance === 0.

**judgement.ts** — THE selector.
- APPROVED_ORDER: P2 coverage → P3 hanging → P4 extent → P5 peel → P7 count → P7b variance → P8 balance.
- `decide`: first uncertified or non-identical interval → undecided; later priorities may not speak. Distinct identities survive even when mechanically tied. Size and canonical only inside one arrangement.
- `restrictInOrder`: measure on surviving set, restrict to argopt, stop on indeterminate or empty restrict.
- `chooseRegistration`: canonical proximity only if chain complete; else deterministic coordinate order.
- Masses: safe-core always, not the deep level.
- P4 policy at published registration: major-over-limit rejects; outline-over-limit with no major = reported TRIVIAL_LIMB_EXEMPT.
- Bracket check is direction-relevant containment + 2-ulp slack. Published worse than certified bound is a violation.
- Exact-witness retry: 3×3 neighbourhood minus origin, same construction door, same chain. Comment says only when first answer misses brackets.
- `decisionState`: NONE / CERTIFIED_WINNER / CERTIFIED_SET / UNRESOLVED_SET. First variant is the winner only under CERTIFIED_WINNER.
- `variantFrom` is a compatibility projection and may not veto.

**grid-core.ts** — T8 deliverable. Delivery only.
- Magnetic without `construction` throws RangeError.
- Validates basis, pad floor, no overlapping spots. Delivers declared population. Coverage reported, never used to choose.
- `candidates` always `[]`.
- Velcro still empty-grid (no selection). Twinfix flag only.
- Helpers remain: `scaleContour`, `nearestAnchorPair`, hold-coverage geometry.

**selection-funnel.test.ts** — falsifier set. What it REFUSES:
- Does not import Logic helpers (selector cannot prove itself).
- Interval compare written locally — does not borrow `certifiedDominance`.
- Does not scan “first priority someone dominates” (would skip an overlap at a higher key).
- Does not pin an output winner on the size-seat test; follows its own comparator, and UNDECIDED keeps both seats.
- Does not claim sequential conflict on a shape that never completes the chain (trapezoid/stepped all stop at P5 segment).
- Does not treat CERTIFYING_P5 as released calibration.
- Does not assert emptiness when structure cannot certify (24×84 → DECISION_INDETERMINATE, not SAFE_CORE_EMPTY).
- P4: refuses non-released switch values; exemption must be announced; major-over-limit is policy not geometry.
- Retry test: PILL B2 must move off [12.95, 12.167]; BAT B2 CERTIFIED_WINNER + byte-identical double run.

## Lead correction — dirty tree (do not treat as T8)

`judgement.ts:1090` `if (true)` is lead's uncommitted symmetric-trigger probe. It produces hash `01d79a1aa97a5cdf` / BAT B2 `tri-48-sw@98`. HEAD is `bde4be2b6bd57b1e` / `pair-diag@98`. My earlier sweep measured the probe. WITHDRAWN. No re-sweep until lead says the tree is at a known commit. Next measurement: `git status --short` + `git rev-parse HEAD` in the same command.

## Waiting

No re-sweep. Tree still dirty.
