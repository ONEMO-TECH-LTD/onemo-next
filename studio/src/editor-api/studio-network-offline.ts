/**
 * ONEMO Studio: when true, realtime and relay must not open outbound WebSockets.
 * Toggle here (or wire from env) to re-enable hosted collaboration.
 */
export const STUDIO_NETWORK_OFFLINE = true;
