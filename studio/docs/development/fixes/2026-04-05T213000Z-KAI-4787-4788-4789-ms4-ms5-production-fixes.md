# MS-4 + MS-5 Production Fixes

**Date:** 2026-04-05T21:30Z
**Session:** Session 43 (KAI-4398) / Sprint 43.8 (KAI-4768)
**Branch:** s43-ms4-ms5-fixes

---

## KAI-4787: Rename localStorage keys to remove "playcanvas" branding

**File:** `studio/src/editor/viewport/effect-viewer-mount.tsx`

- Renamed `onemo.playcanvas.last-scene` → `onemo.studio.last-scene`
- Renamed `onemo.playcanvas.grid-divisions` → `onemo.studio.grid-divisions`
- Added one-time migration: reads old key value, writes to new key (if new key not already set), deletes old key
- Migration is wrapped in try/catch for private browsing / storage access errors

## KAI-4788: Make artwork default configurable (remove test-artwork.png)

**Files:**
- `studio/src/editor/viewport/effect-viewer-mount.tsx` — removed `DEFAULT_ARTWORK_URL` constant, passes `undefined` as `artworkUrl` prop
- `studio/src/editor/viewport/StudioViewport.tsx` — removed `DEFAULT_ARTWORK` constant, passes `artworkUrl` through without fallback

**Rationale:** In Studio, artwork comes from the loaded scene file, not a hardcoded test asset. `loadOptionalTexture` in EffectModel already handles `undefined` by returning null (no texture applied). The prototype's EffectViewer retains its own default for standalone use.

## KAI-4789: Gate console.info in production save flow

**File:** `studio/src/editor/viewport/effect-viewer-mount.tsx`

- Wrapped `console.info('[onemo:scene] saved', ...)` in `if (import.meta.env?.DEV)` guard
- Vite statically replaces `import.meta.env.DEV` with `false` in production builds, so the log is tree-shaken away

---

## Verification

- `vite build` passes cleanly with no errors
- All changes are backward-compatible (migration handles existing localStorage keys)
